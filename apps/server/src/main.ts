/**
 * @ctb/server — Fastify API + Telegram gateway + scheduler.
 * Real boot lands in P0-T4. This file is the P0-T1 placeholder entrypoint.
 */
import { NODES_DEPEND_ON_CORE } from '@ctb/nodes';

console.log(`CTB server placeholder (deps chain ok: ${NODES_DEPEND_ON_CORE})`);
