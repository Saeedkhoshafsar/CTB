# CTB Roadmap

> Principle: every phase ends with something **demoable end-to-end** — a real bot doing a real conversation through the visual editor. No phase ships engine work without a way to see it.

## Phase 0 — Foundation (repo & contracts)

**Goal:** monorepo skeleton compiles, contracts frozen.

- [ ] npm workspaces: `apps/server`, `apps/editor`, `packages/{shared,core,nodes,sandbox}`
- [ ] `packages/shared`: FlowGraph / FlowItem / NodeDef / Execution types + Zod schemas
- [ ] Drizzle schema + migrations (SQLite), `CTB_SECRET` crypto helper
- [ ] Fastify server boots, health endpoint, admin auth (env user/pass), editor served
- [ ] CI: typecheck + test on push; Dockerfile + docker-compose
- [ ] docs/PROTOCOL.md stub

**Demo:** `docker compose up` → login page → empty dashboard.

## Phase 1 — Engine core + Telegram gateway

**Goal:** the pause/resume engine works against a real bot — flows defined in JSON (no editor yet), driven by tests + one sample flow.

- [ ] `core`: executor loop (step, edges, ports), execution persistence, checkpointing
- [ ] WAIT mechanics: serialize → store → resume on matching update; timeout scanner
- [ ] Expression engine (sandbox-backed `{{ }}`, full `$` scope)
- [ ] grammY gateway: register bot (token → webhook/polling), normalized TgEvent, centralized rate-limited sender
- [ ] Update router: waiting-execution match → resume; else trigger match → start
- [ ] Nodes (minimal set, code-defined): Telegram Trigger, Send Message, Wait for Reply, IF, Set Fields, Stop & Error
- [ ] Unit tests: engine stepping, wait round-trip serialization, expression eval

**Demo:** register a bot token via API → sample "ask name → ask age (validated, retry) → greet with both" flow JSON → works on real Telegram, survives a server restart mid-conversation.

## Phase 2 — Visual editor (MVP) ✨

**Goal:** build that same flow with the mouse. This is the moment CTB becomes a product.

- [ ] React Flow canvas: add/drag/connect/delete nodes, multi-port edges, zoom/minimap
- [ ] Side panel auto-generated from node `paramsSchema` (text, multiline, select, button-grid builder, condition rows, expression hints)
- [ ] Flow CRUD + versioning (save = new version, rollback), draft vs active
- [ ] Bots page: add token, set webhook, status
- [ ] Executions page: list, status, per-node log inspector (input/output JSON)
- [ ] Manual Trigger ("test with sample data")
- [ ] Remaining MVP nodes: Menu (button ports), Switch, Wait/Delay, HTTP Request, Storage (KV), Code (JS) with sandbox + code editor (CodeMirror)
- [ ] RTL-friendly UI, fa/en i18n scaffold

**Demo:** non-programmer builds "menu → branch → form (3 questions) → HTTP POST results → confirmation" entirely in the browser.

## Phase 3 — Flow power features

**Goal:** modularity and the rest of the Telegram surface.

- [ ] Execute Sub-Flow + Return node
- [ ] Loop, Merge
- [ ] Edit/Delete Message, Answer Callback, Send Chat Action
- [ ] Credentials store (encrypted) + credential selector in HTTP node
- [ ] User Profile node + Users page (per-bot user list, tags)
- [ ] Execution policies per flow (replace/queue/ignore concurrent), global error-handler flow
- [ ] Import/export flow as JSON; starter template gallery (generic: feedback form, quiz, FAQ menu, reminder)

**Demo:** a reusable "collect contact info" sub-flow used by two different parent flows; flows exported/imported as JSON.

## Phase 3.5 — Collections: structured data + auto-generated admin panel 🗂️

**Goal:** a non-technical operator manages structured data (e.g. products with variants) through an auto-generated CRUD panel; flows consume the same data through one generic node. CTB core stays domain-agnostic — see ARCHITECTURE §13.

- [ ] Collections data layer: `collections`/`records`/`files` tables, Zod `CollectionSchema` (field types incl. `group`, `relation`, `image`), JSON-document storage + `json_extract` query builder + computed indexes
- [ ] Visual schema builder in editor (create/edit collections, field rows, validation, indexed toggle, display hints)
- [ ] Auto-generated CRUD panel: list view (search/filter/sort/columns) + record form generated by the same form engine as node panels; image upload; `group` editor (repeating rows); `relation` picker
- [ ] Records REST API (admin session + API token) sharing the same filter shape
- [ ] `data.collection` node (find/get/insert/update/delete/count) + `collection.recordChanged` trigger with loop guard — specs in NODES.md
- [ ] Operator role: second auth role that sees only the Data section (minimal slice of P6 multi-user)
- [ ] Starter templates updated: generic "catalog + order intake" flow pair wired to two sample collections

**Demo (the manager test):** an operator logs in with the operator role, creates a `products` collection (name, photos, base_price, variants group: color/size/price_delta/stock; relation to `shipping_methods`), adds two products with variants — *never seeing the canvas*. A pre-built flow then serves `/products` in Telegram: lists records, menus for color/size from the chosen record's variants, KV cart, shipping method menu from the related collection, `insert` into an `orders` collection — and a `recordChanged` flow notifies the customer when the operator flips an order to "shipped".

## Phase 4 — Open protocol (n8n & the outside world)

**Goal:** CTB triggers and is triggered by anything.

- [x] Webhook Trigger (async + sync with Respond to Webhook), per-flow secrets, HMAC option
- [x] Schedule Trigger (cron, timezone, for-each-user fan-out with rate limits)
- [ ] REST API (token-auth) for external systems: trigger flow, send message, query users/executions
- [ ] Outgoing instance webhooks (execution.finished, user.first_seen, …)
- [ ] docs/PROTOCOL.md complete + n8n recipe examples (n8n→CTB and CTB→n8n)

**Demo:** an n8n workflow triggers a CTB flow that messages a user, waits for their reply, then POSTs the answer back to n8n.

## Phase 5 — AI nodes

**Goal:** AI-native bot building.

- [ ] LLM Chat (OpenAI-compatible credential: base_url+key), conversation memory via KV
- [ ] AI Classify (LLM-powered Switch), AI Extract (schema-constrained)
- [ ] MCP Client node (list/call tools)
- [ ] AI Agent node (tool loop; tools = MCP tools + flows-as-tools), budget caps

**Demo:** support bot: AI Classify routes intent → FAQ branch answers from LLM with memory → "human" branch notifies admin chat and bridges messages.

## Phase 6 — Hardening & 1.0

- [ ] Postgres support, execution pruning/retention settings
- [ ] Sandbox v2 evaluation (isolated-vm / out-of-process) for hostile multi-tenant
- [ ] Metrics (per-flow runs, errors, latency) + simple dashboard
- [ ] Multi-admin accounts & roles
- [ ] Plugin/custom-node loading spec (community nodes)
- [ ] Docs site, screencast, sample flow library

## PLAN3 — n8n-parity & editor polish (the "make it usable" track) 🎯

**Goal:** after PLAN1+PLAN2 built a deep, working engine (61 nodes, pause/resume, AI, open API), close the gap between *capability* and *usability*. An end-to-end audit found the engine is feature-complete for v1 but the editor under-surfaces what exists. PLAN3 is **editor + UX only** — no engine rewrite, no pivot, no n8n fork. Full task breakdown + acceptance criteria live in **`docs/PLAN3.md`**. See Decision Log #17.

- [x] **UX-T1** — progressive-disclosure node forms ("+ Add option") — *shipped (PR #69)*
- [ ] **Phase F** — first-run & discoverability: guided empty state + quick-start (F-T1), "hello bot" first-run template (F-T2), surface import/export/templates in the toolbar (F-T3)
- [ ] **Phase G** — field ergonomics: Fixed | Expression toggle per field ⭐ (G-T1), live expression preview (G-T2), explicit simple/advanced field grouping ⭐ (G-T3)
- [ ] **Phase H** — canvas power: sticky notes (H-T1), node rename/title (H-T2), canvas error surfacing (H-T3), add-node-on-edge + wire-drop (H-T4)
- [ ] **Phase I** — run & iterate: pin data (I-T1), execute a single node (I-T2), keyboard-shortcut help overlay + coverage (I-T3)

**Demo:** a brand-new user reaches a working bot reply in under 5 minutes, always finds import/export/templates, and a first-timer sees a minimal node with depth one click away. (⭐ = user explicitly asked for it. H-T1/H-T2/I-T1 touch the stored flow schema → each needs its own Decision Log entry when it starts.)

## PLAN4 — Go-live readiness: admin RBAC, setup checklist & live-trigger test runs 🎯

**Goal:** after PLAN3 polished the editor, a real go-live attempt surfaced two blockers and a management ask. Translate the user's field report into the next track. Full task breakdown + acceptance criteria + decision-log seeds live in **`docs/PLAN4.md`**. See Decision Log #24. **No engine rewrite, no second backend language (I1), no domain nodes (I2)** — PLAN4 wires existing seams (the session primitive, the `triggerMatches` matcher, the durable execution state, the `testRun` engine seam).

- [ ] **Phase J** — live-trigger test runs (the "I can't even test" blocker ⭐): listen-for-one engine+server seam (J-T1), editor "Test run" listens on the REAL `tg.trigger` and the "use Manual trigger instead" alert is removed (J-T2), trigger-node parity audit + n8n-style listen docs/e2e (J-T3)
- [ ] **Phase K** — admin identity & RBAC ⭐: durable `panel_admins` store + owner/admin/operator roles with owner-invariants enforced in the store (K-T1), Telegram-ID login + bootstrap-first-owner + admins REST (K-T2), editor Admins page + role-gated UI (K-T3)
- [ ] **Phase L** — go-live setup checklist ⭐: pure `computeChecklist` derived from real state + `/api/setup/checklist` (L-T1), first-run checklist UI that hides each item as it clears and shows a "bot ready" gate when empty (L-T2)

**Demo:** an operator hits "Test run" on a flow whose entry is a real Telegram Trigger, it waits for the next live message and pipes the sender's data downstream (n8n parity); the panel shows only to configured Telegram IDs with owner > admin > operator roles (owner never removable, only owner can transfer); and a first-run checklist guides setup, clearing each item as it's satisfied until the bot is "ready to go public." (⭐ = user explicitly asked for it. J-T1 touches the engine/execution-state schema and K-T1 adds a new table + role → each needs its own Decision Log entry when it starts.)

---

## Cross-cutting rules (every phase)

1. **Every code change → commit → PR** (`genspark_ai_developer` branch workflow).
2. New node = spec in NODES.md + Zod schema + contract tests, *then* implementation.
3. Engine changes require a pause/resume serialization round-trip test.
4. No business-specific nodes in core — ever. Generic primitives only.

## Decision log

| # | Decision | Why |
|---|---|---|
| 1 | TypeScript everywhere, drop PHP | one language across engine/editor/sandbox; ecosystem (grammY, React Flow); the old PHP code is reference-only |
| 2 | SQLite default, Postgres later | zero-config self-hosting first |
| 3 | Code node = JS only (v1) | matches stack & n8n habit; Python via HTTP/sub-process possible later |
| 4 | Conversation state via durable `executions` table, not in-memory sessions | survive restarts; long waits (days) are first-class |
| 5 | Editor-first roadmap (editor at Phase 2, not last) | the product *is* the visual builder; engine without UI is invisible |
| 6 | No domain nodes (shop/VPN) | CTB is a general tool; domains live in flows/templates/plugins |
| 7 | Agent-resumable development protocol (CLAUDE.md + PLAN.md + STATE.md) | any AI agent must be able to resume after a sandbox reset using only the repo |
| 8 | Pin TypeScript ^5.9 (not 6.x) and lock stack versions in PLAN.md | avoid tooling churn mid-build; majors revisited in P6 |
| 9 | Admin UI for business data = **Collections** (schema-driven CRUD, Directus/PocketBase pattern), NOT "UI nodes" on the canvas | a panel is a layout tree, a flow is a logic graph; node-built forms would be worse than code. Keeps I2: "product" is user data, not a CTB concept. New Phase 3.5 (ARCHITECTURE §13) |
| 10 | Collection records = JSON documents in one `records` table (+ `json_extract` computed indexes), not dynamic DDL per collection | instant schema edits, native nested `group` fields, no migration engine inside CTB; bot-scale data doesn't need per-table DDL |
| 11 | Pull a minimal **operator role** forward from P6 into P3.5 | the entire point of Collections is a panel for a non-technical operator; without a role that hides bots/flows it isn't demoable |
| 12 | Expression evaluator swapped from `new Function` stub to the worker sandbox (P1-T2 as planned); evaluator API became **async**; dependency chain extended to `shared ← sandbox ← core ← nodes ← server` | preemptive timeout kill + real isolation (I6) need worker_threads; `@ctb/sandbox` stays driver-free (only `node:worker_threads` + `@ctb/shared`) so `core` importing it keeps I3 intact |
| 13 | Reply validation rules (`regex/min/max` + `invalidMessage`) live INSIDE the reply WaitSpec and are **enforced by the update router**, not by re-running the wait node | resume() routes from a wait node's ports — it never re-executes the node itself, so the node can't validate its own reply. The WaitSpec is the only durable artifact the router has at match time (I4); NODES.md `validation`/`invalid_message`/`max_retries` params map 1:1 onto it |
| 14 | `save_to` rides the reply WaitSpec as `saveTo`; the router applies it on resume through a new `Executor.resume({ varsPatch })` that merges entries into durable `$vars` before the loop continues | same root cause as #13: the wait node never re-executes on resume, so it can't save its own reply. Patching vars through resume() keeps the write durable (I4) and keeps the router free of state-mutation hacks. `NodeCtx.now()` added in the same task so wait nodes compute `timeoutAt` from the injected executor clock (testable deadlines) |
| 15 | `ExecutorServices.tg` and `.kv` became **per-bot factories** (`tg(botId, chatId)`, `kv(botId)`) instead of single capabilities | one Executor instance serves MANY bots (one engine per server, P1-T8 wiring): the right rate-limited TgSender and the right kv namespace can only be resolved per execution, and the executor knows the execution's botId. Alternative (executor per bot) would duplicate stores/registries and break the single timeout scanner |
| 16 | Per-step I/O snapshots live in `exec_logs` (executor `StepLogEntry.input/output`, **20-item cap** per port) rather than a new table or full-fidelity capture; NDV (P2-T3.5) reads them via `GET /api/executions[/:id]`. The detail DTO **nulls `output` when `input` is null** — generic debug rows reuse the output column for non-FlowItem data and must not masquerade as port maps. `FIELD_DRAG_MIME` lives in `form/expression.ts` (pure half) | the columns already existed (input was always null), so no migration; the cap bounds row size for loops over big batches while still showing representative data — n8n similarly truncates. Honest DTO beats a schema change; the MIME constant in form/* keeps the form engine standalone (PLAN P2-T3 note) while canvas re-exports it |
| 17 | **PLAN3 reframes the remaining roadmap from "more capability" to "discoverability + ergonomics."** An end-to-end audit (server + DB + 61-node catalog booted; import/export + template gallery confirmed already shipped) showed the engine is feature-complete for v1, but the editor under-surfaces what exists — the user "couldn't build a single workflow" and "couldn't find how to import/export." PLAN3 (`docs/PLAN3.md`) is editor/UX only — Phases F (first-run & discoverability), G (field ergonomics, incl. Fixed\|Expression toggle), H (canvas power), I (run & iterate) — with **no engine rewrite, no pivot, no n8n fork**. Each editor improvement must be **structural** (like UX-T1) so all 61 nodes benefit at once. The only PLAN3 items that touch the stored flow schema — **sticky notes (H-T1), node title (H-T2), pin data (I-T1)** — each require their own future Decision Log entry when they start | the lived-experience gap was never missing features; it was that CTB modelled its UI on n8n but never finished the *feel* of n8n. Surfacing an existing power (e.g. F-T1/F-T3 expose import/export/templates) is cheaper and higher-impact than building new capability, and structural form-engine work avoids 61× per-node UI code (invariant I2 + UX-T1 precedent) |
| 18 | **G-T3 "advanced" param grouping = a structural `z.meta({ advanced: true })` annotation, read by the form engine; NOT a stored flag and NOT a schema/engine change.** A node author marks a power-user param advanced in its Zod schema; `z.toJSONSchema` carries `advanced:true` to the editor (proven, like `ctbWidget`), where `partitionFields` routes it to a third bucket rendered under a native `<details>` "Advanced" collapsible (auto-opens when any advanced field already has a value). First applied to `tg.sendMessage.parse_mode`. The param's type, validation, and persisted value are byte-identical → the engine, stored flows, and exports are unchanged | mirrors the existing `ctbWidget` annotation pattern (a property OF the schema, never a node-type lookup), so it is purely presentational and Collection forms reuse it for free; keeps the form engine the single structural place all 61 nodes inherit from (UX-T1/G-T1 precedent, invariant I2), with zero risk to durability (I4) since nothing about the params changes |
| 19 | **H-T1 sticky notes live in `FlowGraphSchema.notes` (a NEW optional `StickyNote[]`), NOT as a node type and NOT in a separate store.** A sticky note has `id`/`text`/`position`/`size`/`color` and is a CANVAS-ONLY annotation: it has no node type, no params, no ports, and the executor never reads it (the engine touches only `graph.nodes`/`graph.edges`). `notes` defaults to `[]` so every existing stored flow, fixture and export parses byte-identically. Notes get undo/redo, autosave and export/import for free because they ride the one flow document. The editor renders them as a React Flow custom node kind (`sticky`) derived per-render from `graph.notes`, with their own store actions (`addNote`/`updateNote`/`removeNote` + note-aware move) — the SAME controlled-view pattern as nodes, so the document stays the single writer | a note is not executable, so making it a node would force a fake type/params/ports and pollute the registry + executor paths (violates I2's "generic primitives only" and risks the engine treating it as work); a separate side-table would lose undo/redo + export/import + the single-document durability guarantee (I4). Putting it in the graph but OUTSIDE `nodes` keeps the engine provably inert (it already ignores `position`, the same way) while the canvas treats it as first-class. This is the schema-touching item Decision Log #17 said H-T1 would need |
| 20 | **H-T2 node title is an OPTIONAL `FlowNode.title?` (`string`, ≤120) — a PRESENTATIONAL human name, NOT a routing key.** The editor head/panel/NDV show `title` when set (otherwise the type's i18n label), but the executor NEVER reads it — it routes solely by `id`/`type`/edges, exactly as it already ignores `position`/`note`. The precedence rule lives in a pure, DOM-free helper `nodeDisplayName(node, typeLabel)` (a non-blank trimmed `title` wins, else the type label) so it is unit-tested directly and the React surfaces are thin glue (the F-T3 pattern). `title` is `.optional()` (no default) so every existing stored flow, fixture and export literal stays byte-identical — a node with no title simply omits the field | n8n's "node rename" is the obvious gap (G7); the only question was whether the human name should become an expression-addressable handle (n8n's `$('Node Name')`). It must NOT in v1: CTB's executor is a linear cursor that does not retain every node's output keyed by name, so `$('…')` would be a deep durability change far outside an atomic H-T2 — and a node id ALREADY uniquely addresses a node. So H-T2 ships only the presentational rename; an expression-by-name handle, if ever wanted, is a separate engine task with its own Decision Log entry. This is the schema-touching item Decision Log #17 said H-T2 would need |
| 21 | **I-T1 pin data lives on the node as an OPTIONAL `FlowNode.pinnedData` (`FlowItem[]`, ≤50), and is honoured ONLY in a TEST run via a persisted `ExecutionState.testRun` flag — never in production.** A pinned node carrying `pinnedData` short-circuits in the executor's `runLoop` (BEFORE the disabled-branch): it EMITS the stored sample on the universal `main` port (a `structuredClone`, so the stored pin can't be mutated by downstream nodes) INSTEAD of executing. The behaviour is gated by `state.testRun === true`; `start()` stamps it from a new `StartInput.testRun`, and ONLY the editor's "Test run" endpoint (`/api/flows/:id/run`) sets it — production runs (router/scheduler/webhook) omit it, so a pin is invisible to a live bot. The flag is PERSISTED on the execution state (not a transient `start()` arg) so it SURVIVES pause/resume (I4) — a flow that WAITs and resumes mid-run keeps honouring pins. `pinnedData` is `.optional()` (no default) and capped at 50 items (a pin is a sample, not a dataset), so every existing stored flow/fixture/export parses byte-identically. The editor pin/unpin UI flattens a node's last run output via a pure DOM-free `flattenOutputForPin` (main-port first, capped at `PIN_ITEMS_CAP`=50; F-T3 pattern, unit-tested directly) | n8n's "pin data" is gap G4 (the "test fast" cure): re-running a flow shouldn't re-hit a slow/expensive/side-effecting node when you've already captured its output once. The pin must sit ON the node (not a side-table) so it rides the one flow document for undo/redo + export/import + durability, exactly like sticky notes (#19) and titles (#20). It must be TEST-only and the flag must be DURABLE: a transient runtime arg would be lost on the first WAIT/resume, silently turning pins off mid-run (an I4 violation) — persisting `testRun` on the state is the only correct seam. Emitting on `main` (CTB's universal port) rather than per-port keeps the engine contract simple and matches how a captured run output is flattened for the pin. This is the schema-touching item Decision Log #17 said I-T1 would need |

| 23 | **I-T3 keyboard shortcuts are driven by a PURE, DOM-free matcher + catalog (`apps/editor/src/canvas/shortcuts.ts`); the `?` help overlay is GENERATED from the same `SHORTCUTS` list the handler matches against, so the help can never drift from the live bindings — and `duplicate` is a PURE `duplicateNodes(graph, ids)` graph edit.** The risky part of a shortcut system is the matching logic (key + Ctrl/⌘ cross-platform + yielding to text fields), so it lives in a pure `matchShortcut(ev, inTextField)` over a structural `ShortcutEvent` and is unit-tested directly; the single `window` keydown handler now lives in `FlowCanvas` (inside `ReactFlowProvider`) where it has React Flow (`fitView`) + the selection store, and the old FlowEditorPage handler was removed. New bindings: `Ctrl/⌘+D` duplicate, `Ctrl/⌘+A` select-all, `Shift+1` fit-view, `?` toggle help, `Esc` close overlay (the only `allowInInput` shortcut); existing `Ctrl/⌘+Z/Y/(Shift+Z)/S` + RF's Delete/Backspace are folded into the catalog. `duplicateNodes` clones the selected nodes (fresh unique ids, a `DUPLICATE_OFFSET` px offset, deep-cloned params, copied title/disabled/note) and copies ONLY edges whose BOTH ends are inside the selection (re-wired to the clones; boundary-crossing edges dropped), as ONE undoable/autosaved store edit re-validated against `FlowGraphSchema`; an empty/unknown selection is a no-op (returns `[]`, no history). The help overlay open-state is a tiny `useShortcutHelp` zustand store so both the handler (`?`) and a toolbar `?` button drive it. EDITOR-ONLY — no schema/server/registry/engine change; stored flows unchanged | n8n's shortcut help (`?`) + full shortcut coverage is gap G11. Generating the overlay from the catalog (not a hand-maintained list) is the structural choice that keeps docs honest as bindings grow (principle 2); a pure matcher keeps the cross-platform/text-field logic testable without jsdom (the F-T3/H-T2 pattern). The handler had to MOVE into the canvas because select-all/fit-view need React Flow + the selection store, which only exist inside `ReactFlowProvider`. Duplicate copies only internal edges because a clone silently fanning-in/out to existing nodes would surprise the user; it sits on the same one undoable store edit as the other H-T4 graph edits so it rides undo/redo + autosave + export for free |\n\n| 22 | **I-T2 single-node run is an OPTIONAL `StartInput.stopAfterNode?: NodeId` mirrored onto a persisted `ExecutionState.stopAfterNode?` — the executor runs the ONE target node and then ENDS the run, never routing downstream.** The boundary is checked in `runLoop` AFTER the per-step I/O snapshot log (so the node's output IS captured for the editor to read back) and ONLY short-circuits an `items`/`goto` result — a `wait`/`end`/`error` result already terminates the step naturally, so a single-node run of a WAIT node correctly reports `waiting`. The boundary is PERSISTED on the execution state (not a transient `start()` arg), consistent with the I-T1 `testRun` seam (#21), so it survives pause/resume (I4). The server exposes `POST /api/flows/:id/run-node` which ALWAYS starts a TEST run (so pinned data from I-T1 is honoured) with `entry` at the target node and `stopAfterNode` = that node; it validates the body (`RunNodeBodySchema` = `{ nodeId, input?: FlowItem[]≤200 }`) and rejects a missing (404 `node_not_found`) or disabled (422 `node_disabled`) node; the node's output is read back from `exec_logs`, not returned inline. `stopAfterNode` is `.optional()` (no default) so every existing persisted execution state parses byte-identically. The editor's NDV gains a “▶ Run this node” button (`api.runNode`) that `saveNow()`s, runs only that node with its last-captured input (or one empty item), and refreshes the run-data overlay | n8n's “execute a single node” is gap G16 (the “test fast” cure, sibling to I-T1's pin data): while authoring you want to fire ONE node and inspect its output without running — or side-effecting — the whole flow. Reusing the executor's real step path (resolve → eval → validate → execute → route) instead of a bespoke “run one node” code path guarantees the single-node output is byte-identical to what a full run would produce (no second engine to drift). The stop must be a DURABLE boundary on the state, not a transient flag, for the same I4 reason as #21 — a single-node run that hits a WAIT must still resume-and-stop correctly. Always running it as a TEST run makes single-node iteration compose with pinned data. This needs no node/registry change and every field is optional, so it is purely additive || 24 | **PLAN4 reframes the next track from "editor polish" (PLAN3) to "go-live readiness" — admin RBAC, a setup checklist, and live-trigger test runs — REUSING existing seams rather than building parallel machinery.** Driven by a real bring-up report (`docs/PLAN4.md` §0) that surfaced two blockers and a management ask. **Phase J (J-T1):** a NEW optional `StartInput.listenMode` mirrored to a persisted `ExecutionState.listening` parks a flow's REAL `tg.trigger` in a durable "armed" state (like a WAIT, durable across restart — I4); the NEXT matching live update (via the existing `triggerMatches`) resumes THAT execution exactly-once with the real Telegram item, and a non-matching update doesn't consume the arming. The CRITICAL contract (user item 9): the `tg.trigger` node TYPE is identical in a trial and a production run — only the run MODE differs — so the editor's "use the Manual trigger instead" alert is REMOVED. Sibling seam to `testRun` (#21) and `stopAfterNode` (#22). **Phase K (K-T1):** a NEW append-only `panel_admins` table keyed by Telegram user id + a third `SessionRole = 'owner'` (precedence owner ⊇ admin ⊇ operator via a pure `roleAtLeast`); the OWNER INVARIANTS — exactly one owner, owner never removable, only an owner can transfer ownership and the transfer atomically demotes the old owner to admin — are enforced in the STORE, not just the UI. Panel admins are panel identities (separate from the per-bot `SqliteUserStore` end-users — I2); the legacy env username/password becomes the bootstrap owner ONLY when the table is empty (first bring-up). **Phase L (L-T1):** the setup checklist is a PURE `computeChecklist(state)` derived from already-stored facts (owner set? ≥1 admin? ≥1 bot token? ≥1 active flow? `CTB_SECRET` set?…) — a task "disappears" when its predicate over real state becomes true, and `ready` = no open predicates; NO separate mutable checklist table | the lived gap was never missing capability — CTB has a durable engine, a session/role primitive, and a generic users table — but go-live was UNDER-WIRED: auth was a single shared password unrelated to Telegram identity, and the editor's test run was hard-coded to `flow.manualTrigger` instead of letting a real `tg.trigger` listen for ONE live update (n8n's "listen for test event"). Reusing the matcher + the durable-state seam (not a second executor) keeps the trial output byte-identical to a production run (no engine to drift, the #22 rationale). Enforcing owner invariants in the store (not the UI) is the only safe place — a UI-only guard is bypassable. Deriving the checklist from real state (not a checklist record) keeps the single source of truth in the actual configuration, so the list can never lie about readiness. J-T1 (engine/state schema) and K-T1 (new table + role) are the schema-touching items that earned this entry |
