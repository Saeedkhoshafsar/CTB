# STATE2 — Current truth of the CTB repository (PLAN2 / Phase B onward)

> **Read me first, every session — this is the ACTIVE truth file.**
> I am the single source of "where are we" for **PLAN2 / Phase B and later**.
> I am updated in the SAME COMMIT as the code I describe (CLAUDE.md §4).
>
> **Why this file exists:** `docs/STATE.md` grew very large (PLAN1 / Phase 0–6 +
> PLAN2 Phase A history) and re-reading/editing it on every turn cost many
> tokens. So PLAN2 / Phase-B reports now live HERE; `docs/STATE.md` is frozen as
> the Phase-A-and-earlier archive (read it only when you need pre-Phase-B history).

## Current position

```
Phase     : PLAN2 / **PHASE E COMPLETE.** **PLAN3 IN PROGRESS (docs/PLAN3.md — n8n-parity & editor-polish, Phases F/G/H/I; ROADMAP Decision Log #17). F-T1 (guided empty state + Quick-start CTAs + empty-canvas hint) DONE — the "I couldn't build anything" cure; F-T2 (\"Hello bot\" first-run template + 5-minute quickstart doc) DONE — the smallest flow that actually replies; F-T3 (Export button in the editor toolbar + shared flow-export helper) DONE. FLAKY-FIX (expression-budget host seam — `WireOptions.expressionBudgetMs` + `CTB_EXPRESSION_BUDGET_MS` env, tests raised to 5s; cures the "e2e flaky in this sandbox" 50ms cold-start budget timeout) DONE — full server suite 450 GREEN ×2 deterministic.** **UX-T1 (progressive-disclosure node forms) DONE — interim UX task between PLAN2 and PLAN3.** **PLAN3 Phase G IN PROGRESS — G-T1 (Fixed | Expression toggle per field) DONE: every "simple" field (number / select / duration) gains a small `Fixed | Expression` pill switch so `{{ }}` can drive a number/enum/duration param; mode is INFERRED from the value (no stored flag → params unchanged for the engine); lossless Fixed↔Expression round-trip; editor 190 GREEN. G-T2 (live expression preview) DONE: while editing a `{{ }}`, a read-only result row resolves it CLIENT-SIDE against the latest run's first input item via a tiny SAFE path-resolver (no engine/sandbox, no server round-trip — never evals arbitrary code, never throws; out-of-subset ⇒ "preview unavailable"); editor 201 GREEN. G-T3 (explicit simple/advanced field grouping) DONE: a node author flags a power-user param `z.meta({ advanced:true })` (proven to survive `z.toJSONSchema` like `ctbWidget`); `partitionFields` routes it to a THIRD `advanced` bucket (precedence over required/isSet/added) the `SchemaForm` renders under a native `<details>` "Advanced" collapsible that AUTO-OPENS when any advanced field already holds a value (`anyAdvancedSet`); purely presentational — the param's type/validation/stored value are byte-identical so the engine + exports are unchanged (ROADMAP Decision Log #18). First applied to `tg.sendMessage.parse_mode`. New `isAdvanced`/`anyAdvancedSet` helpers; i18n `form.advanced.title` (en+fa); `.form-advanced*` CSS; +5 tests against the REAL shared schema; editor 206 GREEN; shared 74 + server 450 GREEN (the schema-meta touch is inert). PLAN3 Phase H IN PROGRESS — H-T1 (sticky notes) DONE: a NEW optional `FlowGraphSchema.notes: StickyNote[]` (id/text/position/size/color) — a CANVAS-ONLY annotation the executor never reads (it touches only `graph.nodes`/`graph.edges`), so `notes` is `.optional()` and every existing stored flow/fixture/export parses byte-identically (ROADMAP Decision Log #19). The editor renders notes as a React Flow custom node kind (`sticky`) derived per-render from `graph.notes` with their own store actions (addNote/updateNote/removeNotes + note-aware moveNote/resizeNote reusing the preMove/commitMove drag coalescing), an `AddNote` toolbar button, in-place edit + `NodeResizer` + 5-colour picker; notes get undo/redo + autosave + export/import for free by riding the one flow document. Pure mapping in `canvas/graph.ts` (notesToRfNodes / nextNoteId / rfIdForNote↔noteIdFromRf under a `note:` rf-id prefix, notes mapped behind nodes with zIndex:0). i18n `editor.note.*` (en+fa); `.sticky-*` CSS. Verified: shared flow.test +8, flow-export +1 (notes survive export→import), editor canvas-graph +5 + canvas-store +5; shared 83 + editor 216 + core 50 GREEN, all 4 workspaces typecheck, editor build GREEN.** PE-T1 (voiceConnection credential) + PE-T2 (Call Session Service + `ctx.call`) + PE-T3 (`trigger.callEvent`) + PE-T4 (`call.*` action nodes) + PE-T5 (🎬 demos + docs) DONE.** **PHASE D COMPLETE — PD-T1 + PD-T2 + PD-T3 + PD-T4 DONE.** **PHASE C COMPLETE — PC-T1 + PC-T2 + PC-T3 + PC-T4 + PC-T5 DONE.** **PHASE B COMPLETE (PB-T1…PB-T8).** **PHASE A COMPLETE (PA-T1…PA-T8).** **PHASE E (live voice AI — real-time Telegram calls)** serves BOTH user scenarios — a **group/channel live voice chat** members hear (with a `lineup` Q&A queue), AND a **1:1 call** e.g. from a channel's Direct so an AI answers in real time (`support` mode) — through the SAME generic nodes via the `target`/`mode` settings. **PE-T1 (`voiceConnection` credential) DONE:** a NEW encrypted credential (I7) with a `kind` setting (`userbot` shipped; `companion`/`external` forward-shaped) — the Bot API has NO call methods, so audio rides MTProto via a USER session. **PE-T2 (Call Session Service + `ctx.call`) DONE:** the shared `CallCapability` interface + a nullable `ctx.call` cap (`packages/shared/src/node-def.ts`); `ExecutorServices.call?` factory + `buildCtx` wiring (core); the long-lived **`CallSessionService`** (`apps/server/src/triggers/call-session.ts`, a sibling to the Scheduler) holding the connection + per-call turn state (support/lineup, sequential/random order, optional turn auto-advance) with HARD CAPS (max concurrent calls host-wide + per-bot, max call duration → auto-leave); the dependency-free **`LoopbackVoiceConnector`** default (`apps/server/src/engine/loopback-connector.ts`) so it's testable without MTProto and the native engine swaps in at the composition root (I3); wired in `wire.ts` (factory + Engine handle + `main.ts` shutdown `stop()`); tests `call-session.test.ts` (24); docs NODES.md §"Call Session Service + ctx.call". **PE-T3 (`trigger.callEvent`) DONE:** a NEW generic live-call entry node (`trigger.callEvent`) that serves BOTH scenarios via config — a host **`CallEventBus`** (`apps/server/src/triggers/call-events.ts`, sibling of the record-write bus) subscribes to the Call Session Service's `onUtterance` + new `onLifecycle` streams and, for each event (`callJoined`/`utteranceFinal`/`turnOpened`/`callLeft`), scans active flows for a matching `trigger.callEvent` (PURE `matchCallEvent` on target kind+id + events list) and fires it chatless via `router.fireCallEvent`; `utteranceFinal` persists the segmented PCM ONCE as a CTB file id (`audio/l16`) so `ai.speechToText` reads it (I6). `CallSessionService` gained `CallLifecycleEvent`/`onLifecycle`/`emitLifecycle` (fired on connect/grantTurn/leave). Node `trigger.callEvent` (pass-through, registry 54→55) + `CallEventTriggerParamsSchema` (connection/targetKind/targetId/events/mode/order/maxTurnSeconds/autoAdvance) + i18n en/fa; wired into `wire.ts` (`Engine.callEventBus`) + `main.ts` start/stop. Tests: `call-event-trigger.test.ts` (7 node-contract) + `call-events.test.ts` (15 host — pure matcher, dispatch/one-per-flow, audio-persist-once, lifecycle items, no-match/disabled/invalid skip, never-throws, end-to-end start/stop via real service+loopback). ****PE-T4 (`call.*` action nodes) DONE:** the six live-voice ACTION nodes a flow drives a call back with — `call.connect`/`call.speak`/`call.grantTurn`/`call.endTurn`/`call.mute`/`call.leave` (`packages/nodes/src/call/*.ts`, registry 55→61), all plain flow-category nodes going through the one typed `ctx.call` (I3/I4/I6) and FAILING LOUD when `ctx.call===null`; `target`/`mode`/`source` are SETTINGS so the same six serve both scenarios; `call.speak source:'file'` plays a CTB file id (e.g. `ai.textToSpeech` output, read host-side, I6); tests `call-nodes.test.ts` (19). **PE-T5 (🎬 demos + docs) DONE:** two scripted fake-transport e2e demos — (a) **AI voice support** (1:1: `utteranceFinal` → STT → agent(model+memory+tools) → TTS → `call.speak`; memory keyed by the CALL target + replayed across utterances) and (b) **channel Q&A moderator** (two listeners queue; `call.grantTurn` → TTS greeting → `call.speak` → `call.endTurn` advances the line `[111,222]`→`[222]`→`[]`) — both built from the SAME nodes, differing only in settings (a 4th static test asserts the shared node set + support-vs-lineup mode). Both drive the **real** Executor + **real** `CallSessionService` + **real** `LoopbackVoiceConnector` (a connector `speak` spy records what's played). Fixtures `packages/shared/test/fixtures/phase-e-{voice-support,qa-moderator}-flow.json`; test `apps/server/test/e2e-phaseE-voice-demo.test.ts` (4, stable). Supporting fixes: `call.grantTurn`/`call.mute` `userId` → `z.coerce.string()` (accept a numeric `{{ $json.speakerId }}`); `CallSessionService.grantTurn` explicit-user queue filter unified by `String()`. Docs: `docs/demos/phase-E-voice.md` walkthrough + NODES.md §Live-voice cross-ref + **PROTOCOL.md "Live voice" chapter** (why a separate transport — Bot API has no call methods; connector kinds userbot/companion/external/loopback; the `voiceConnection` credential; hard caps; ToS posture). **Phase E is COMPLETE.** Phase D hardening recap: PD-T1 (DB pooling+safety), PD-T2 (agent cost governance), PD-T3 (MCP/API rate-limit+audit), PD-T4 (node-library docs site) — all DONE. **PLAN4 IN PROGRESS (docs/PLAN4.md — go-live readiness; ROADMAP Decision Log #24). Phase J (live-trigger test runs) OPENED — J-T1 ("listen for one live update") DONE: a real `tg.trigger` now powers BOTH a production run and a TEST run — a NEW optional `StartInput.listenMode` parks the run in a durable `WaitSpec{kind:'trigger'}` (+ persisted `ExecutionState.listening` marker) instead of a new top-level status; the next MATCHING live update resumes it exactly-once on `main` with the REAL Telegram item (sender id/name/text) and downstream nodes run; arming survives a process restart (I4); a non-matching update doesn't consume it; a production run (no `listenMode`) is byte-identical to before. Store gains a bot-keyed `findListening(botId)`; the router resumes armed listens BEFORE `tryTriggers` and disarms timed-out ones; server exposes `POST/GET-status/DELETE /api/flows/:id/test-listen`. ROADMAP Decision Log #25 — the n8n "listen for test event" model. **J-T2 (editor "Test run" drives the live-listen + shows the captured update; the "use Manual trigger instead" alert is GONE) DONE: a NEW PURE `decideTestRunMode(graph)` (F-T3 pattern) routes Test-run to `manual` (a `flow.manualTrigger` → synchronous `/run`, byte-identical to before, takes precedence when both triggers exist), `listen` (no manual trigger but an enabled `tg.trigger` → arm `POST /test-listen`, show a "Waiting for a message to your bot…" banner with a Cancel button, poll status every 1.2s, on `captured` load the run-data so the NDV shows the REAL sender input, on expiry/gone notify), or `none` (the ONLY remaining hint — "add a trigger", which now names BOTH trigger kinds, never the old "use Manual instead" dead-end). `ApiClient` gains `testListen`/`testListenStatus`/`testListenCancel`; i18n `editor.testRun.listen.*` (waiting/cancel/expired) + broadened `editor.testRun.noTrigger` en+fa parity. Editor-only (no schema/server/engine touch). Verify: editor 271 GREEN (+11: test-run-mode 7 + test-listen-client 4) + editor `tsc` GREEN.** Next: J-T3 (trigger-node parity audit + docs + an arm→inject→capture e2e).**
Roadmap   : PLAN.md is DONE (through P5-T4; full Phase 0–6 history in docs/STATE.md). The active roadmap is **docs/PLAN2.md**; **Phase A complete (PA-T1…PA-T8); Phase B COMPLETE (PB-T1…PB-T8); Phase C COMPLETE (PC-T1 ✅ + PC-T2 ✅ + PC-T3 ✅ + PC-T4 ✅ + PC-T5 ✅); Phase D COMPLETE (PD-T1 ✅ + PD-T2 ✅ + PD-T3 ✅ + PD-T4 ✅); Phase E COMPLETE (PE-T1 ✅ + PE-T2 ✅ + PE-T3 ✅ + PE-T4 ✅ + PE-T5 ✅)**. Phases: A (media + data-transform) ✅ → B (AI tier) ✅ → **C (open builder API/MCP surface) ✅** → **D (hardening) ✅** → **E (live voice AI — real-time Telegram calls): PE-T1 voiceConnection credential ✅ → PE-T2 Call Session Service + `ctx.call` ✅ → PE-T3 `trigger.callEvent` ✅ → PE-T4 `call.*` actions ✅ → PE-T5 demos+docs ✅ — COMPLETE**. **PLAN2 (A–E) COMPLETE; now executing docs/PLAN3.md (editor polish). PLAN3 Phase F (discoverability) COMPLETE: F-T1 ✅ + F-T2 ✅ + F-T3 ✅ done. Phase G (field ergonomics) COMPLETE: G-T1 (Fixed | Expression toggle per field) ✅ + G-T2 (live expression preview) ✅ + G-T3 (explicit simple/advanced field grouping) ✅ done. Phase H (canvas power) COMPLETE — H-T1 (sticky notes) ✅ done (a new `FlowGraphSchema.notes: StickyNote[]?` canvas-only annotation — ROADMAP Decision Log #19 — rendered as a React Flow `sticky` node; engine provably inert). H-T2 (node rename / human title) ✅ done (a NEW optional `FlowNode.title?` `string`≤120 — ROADMAP Decision Log #20 — a PRESENTATIONAL human name shown on the canvas head / param panel / NDV via the pure `nodeDisplayName(node, typeLabel)` helper, editable via a "Name" field; the executor NEVER reads it so the field is optional/no-default and every existing flow parses byte-identically; the n8n `$('Node Name')` expression-handle was deliberately NOT shipped — CTB's linear cursor doesn't retain per-node outputs by name, so it's a separate engine task). H-T3 (canvas error surfacing) ✅ done (a node that failed on the LATEST execution glows red on the canvas — a pulsing `.ctb-node.errored` outline, motion-reduced — and shows a clickable "Failed on last run" flag that opens the NDV, which renders the failing node's error as a banner under its head; the message comes from a NEW pure `mapRunErrors(logs)` that keeps exactly the `level:'error'` rows `mapRunData` SKIPS, prefers the structured `error` column over `message`, defaults to `'error'`, last-error-per-node-wins; the run-data store gained `errorsByNode: Map<string,string>` next to `byNode`; read-only overlay — no schema/server/executor change). H-T4 (add-node-on-edge + wire-drop-to-palette) ✅ done (a custom `CtbEdge` with an inline "+" splits an edge `A→B` into `A→N→B` via the pure `insertNodeOnEdge`; an `onConnectStart`/`onConnectEnd` wire dropped on the empty pane opens a shared `NodePicker` pre-targeted at the dangling end via the pure `nodeFromDangling`; both go through new undoable store actions, every edge on the universal `'main'` port, result re-validates against `FlowGraphSchema`; editor-only, no schema/server/engine change). **Phase H (canvas power) COMPLETE (H-T1…H-T4).** **Phase I (run & iterate) COMPLETE — I-T1 (pin data on a node — ROADMAP Decision Log #21) ✅ + I-T2 (execute a single node — Decision Log #22) ✅ + I-T3 (keyboard shortcut help overlay + coverage sweep — Decision Log #23: a PURE `matchShortcut` matcher + `SHORTCUTS` catalog, the `?` overlay generated from it, new `Ctrl/⌘+D` duplicate / `Ctrl/⌘+A` select-all / `Shift+1` fit-view bindings, the keydown handler moved into FlowCanvas; editor-only) ✅.** **ALL of PLAN3 (Phases F → G → H → I) COMPLETE.** **PLAN4 PROPOSED (docs/PLAN4.md — DRAFT, ROADMAP Decision Log #24): "go-live readiness" — Phase J (live-trigger test runs: a real `tg.trigger` listens for ONE live update via a NEW durable `ExecutionState.listening` seam, sibling to `testRun`/`stopAfterNode`, so a trial run and a production run use the SAME node and the "use Manual trigger instead" alert is removed — user Report B), Phase K (admin identity & RBAC: a NEW append-only `panel_admins` table keyed by Telegram id + a third `SessionRole='owner'` with store-enforced owner invariants — exactly one, never removable, owner-only atomic transfer — user Report A items 1/4/5/6), Phase L (a PURE `computeChecklist` setup checklist derived from real state that hides each item as it clears and gates "bot ready" — user Report A item 2). NO engine rewrite, NO second backend language (I1), NO domain nodes (I2): PLAN4 wires the existing session primitive, the `triggerMatches` matcher, and the durable execution-state seam.** Next: PLAN4 is a DRAFT plan only (no code shipped this turn) — when work starts, execute it one atomic task at a time (J-T1 first, the test-run blocker), each ending green with STATE2 updated in the same commit.
Task      : J-T2 **Editor "Test run" listens on the real trigger; the "use Manual trigger instead" alert is GONE (PLAN4 Phase J — user Report B item 9).** The user-facing half of J-T1: before this, the editor's "Test run" button only ever hit `flow.manualTrigger`, so a flow whose entry is a real Telegram Trigger gave a dead-end "add a Manual Trigger" alert. **DESIGN (the F-T3 pure-module pattern — EDITOR-ONLY, no schema/server/engine change; J-T1 already shipped the engine + server seam):** a NEW PURE, DOM-free `decideTestRunMode(graph)` in `apps/editor/src/lib/test-run.ts` returns `'manual' | 'listen' | 'none'` — `manual` when an enabled `flow.manualTrigger` exists (takes precedence so the historical synchronous sample-payload `/run` path is byte-for-byte unchanged), `listen` when there is NO manual trigger but an enabled `tg.trigger` (the flows that used to hit the dead-end), `none` otherwise. **TestRunButton (`FlowEditorPage.tsx`):** decides over the LIVE canvas graph; on `manual` runs `api.runFlow` then loads run-data (unchanged); on `listen` calls `api.testListen` (J-T1 `POST /test-listen`), shows a `data-testid="test-listen-banner"` "Waiting for a message to your bot…" alert with a Cancel button, and a `useEffect` polls `api.testListenStatus` every 1.2s — on `captured` it loads the run-data so the NDV shows the REAL captured update, on `expired`/`gone` it surfaces a gentle notice; Cancel (or unmount) calls `api.testListenCancel` (J-T1 `DELETE`) and aborts the poll; on `none` it shows the broadened "add a trigger" hint (which now names BOTH trigger kinds — the old "use Manual instead" string is deleted). The button is disabled while a listen is armed. **Client:** `ApiClient.testListen`/`testListenStatus`/`testListenCancel` (POST arm / GET status with url-encoded `executionId` / DELETE disarm). **i18n:** NEW `editor.testRun.listen.waiting`/`.cancel`/`.expired` + broadened `editor.testRun.noTrigger`, en+fa parity. **Verify:** editor **271 GREEN** (+11 new: `test-run-mode.test.ts` 7 covering manual/listen/none + manual-precedence + disabled-trigger fall-through, `test-listen-client.test.ts` 4 pinning verb/path/executionId-encoding via a fetch stub) + editor `tsc --noEmit` GREEN. Server/core/shared UNCHANGED by this editor-only task (invariant I3) so they stay at the J-T1 merge-green baseline (shared 100 / core 64 / server 466). Next: J-T3 (trigger-node parity audit + NODES.md/demos + an arm→inject-one-update→assert-it-reached-node-2 e2e).
Task-prevJ1: J-T1 **Listen for one live update — a real `tg.trigger` test run (PLAN4 Phase J, live-trigger test runs — user Report B; OPENS Phase J).** The cure for "I can't even test a flow": the editor's "Test run" was hard-wired to `flow.manualTrigger`, so a flow whose entry is a real Telegram Trigger could not be trialled without swapping in a Manual trigger. **DESIGN (ROADMAP Decision Log #25 — the n8n "listen for test event" model; reuses the durable pause/resume seam, NOT a new engine):** a NEW optional `StartInput.listenMode` (`{ triggerParams, timeoutAt? }`) that PARKS the run in a durable `WaitSpec{kind:'trigger'}` instead of a new top-level `status:'listening'` (which would ripple through ~58 status sites), plus a persisted `ExecutionState.listening` marker. **Engine (`packages/core/src/engine/executor.ts`):** when `start()` gets `listenMode` it does NOT execute the entry node — it stamps `testRun:true` (so I-T1 pins are honoured on capture) + `listening:true`, saves `status:'waiting'` with a `'trigger'` wait carrying the entry node's `triggerParams` snapshot + optional `timeoutAt`, and returns `{status:'waiting'}` — surviving a process restart exactly like any WAIT (I4). **Shared:** the `'trigger'` `WaitSpec` kind + `ExecutionState.listening?` (both optional → a production run persists byte-identically) + `TestListenArmed`/`TestListenStatus` API types. **Store:** a bot-keyed (NOT chat-keyed — a test-listen has no chat yet) `findListening(botId)` on the interface + MemoryExecutionStore + SqliteExecutionStore. **Router (`apps/server/src/engine/router.ts`):** `handleSerialized` gains step (1.5) `tryResumeListen(event)` BEFORE `tryTriggers` — finds armed listens for the bot, matches with the SAME production `triggerMatches`, resumes the FIRST match exactly-once on `main` with the real `triggerItem(event)` (sender id/name/text); a non-matching update doesn't consume the arming, and `resumeTimedOut` DISARMS (cancels) an expired `'trigger'` wait. **Server (`apps/server/src/api/flows.ts`):** `POST/GET-status/DELETE /api/flows/:id/test-listen` (arm→`TestListenArmed`; poll→`TestListenStatus` `listening`/`captured`/`expired`/`gone`; cancel) gated on a `tg.trigger` entry (422 `no_telegram_trigger`). **Verify:** shared 100 / core 64 GREEN; FULL server suite **466 GREEN** (incl. the two new files router-test-listen +5 / api-flows-test-listen +5); also fixed a pre-existing FLAKY `e2e-phaseB-voice-demo` (the real-engine voice demo lacked the `evalOptions.budgetMs` host tuning seam the sibling Phase-E demo already uses, so under heavy parallel load the 50ms expression cold-start budget timed out → raised it to 5_000ms, matching Phase-E). Server `tsc` typecheck cannot complete in this ~1 GB sandbox (OOM/thrash), but vitest type-strips every modified file and shared/core/nodes `tsc` pass clean. Next: J-T2 (editor "Test run" drives the live-listen + shows the captured update; remove the "use Manual trigger instead" alert).
Task-prevI3: I-T3 **Keyboard shortcut help overlay + coverage sweep (PLAN3 Phase I, run & iterate — gap G11; COMPLETES Phase I and the PLAN3 roadmap).** The "test fast"/"work fast" cure: a `?` overlay that lists every editor shortcut, plus the obvious missing bindings (duplicate / select-all / fit-view). **DESIGN (ROADMAP Decision Log #23 — the F-T3/H-T2 pure-module pattern; EDITOR-ONLY, no schema/server/registry/engine change):** the risky matching logic is a PURE, DOM-free `matchShortcut(ev, inTextField)` + a `SHORTCUTS` catalog in NEW `apps/editor/src/canvas/shortcuts.ts` (a structural `ShortcutEvent` subset of `KeyboardEvent`, cross-platform Ctrl/⌘ via `ctrlKey||metaKey`, redo special-cased so `Ctrl+Shift+Z`→`redo` not `undo`, `allowInInput` only for Escape) → unit-tested directly; the React glue is thin. **Handler MOVED into `FlowCanvas` (inside `ReactFlowProvider`):** the single `window` keydown handler now lives where React Flow (`fitView`) + the `useSelection` store are available; the old FlowEditorPage handler was deleted. NEW bindings folded in beside the existing `Ctrl/⌘+Z/Y/(Shift+Z)/S` + RF Delete/Backspace: `Ctrl/⌘+D` duplicate (selects the clones), `Ctrl/⌘+A` select-all, `Shift+1` fit-view, `?` toggle help, `Esc` close. **Duplicate = a PURE `duplicateNodes(graph, ids)` in `canvas/graph.ts`** (`DUPLICATE_OFFSET=40`px; fresh unique ids via `nextNodeId`; deep-cloned params; copied title/disabled/note; copies ONLY edges whose BOTH ends are inside the selection, re-wired to the clones with fresh `nextEdgeId`s; boundary-crossing edges dropped) → store `duplicateNodes` action commits ONE undoable/autosaved edit re-validated against `FlowGraphSchema`, returns `[]` (no history) on an empty/unknown selection. **Overlay:** NEW `apps/editor/src/canvas/ShortcutHelp.tsx` — a `useShortcutHelp` zustand store (`open/show/hide/toggle`) + a `<ShortcutHelp>` modal GENERATED from `SHORTCUTS` (grouped edit/canvas/general, `kbd` hints) so help can't drift from bindings; rendered in `FlowEditorPage` + a toolbar `?` button. **i18n** `editor.shortcut.*` (title/close/3 group titles/7 labels) en/fa parity; **CSS** `.shortcut-help*`/`.shortcut-help-btn`/`.shortcut-row kbd`. **Verify:** editor typecheck GREEN + build GREEN + **260 tests GREEN** (+20: shortcuts.test.ts 13 matcher/catalog/parity + canvas-graph 5 duplicate + canvas-store 2 duplicate); shared/core/nodes/sandbox typecheck GREEN. Server is UNCHANGED by this editor-only task (no server/shared file touched; server has zero editor dependency, invariant I3) so its typecheck/tests stay at the I-T2 merge-green baseline — the full-server tsc cannot run in this sandbox without freezing it. **PLAN3 Phase I COMPLETE (I-T1 ✅ + I-T2 ✅ + I-T3 ✅); ALL of PLAN3 (Phases F/G/H/I) COMPLETE.**
Task-prevI2: I-T2 **Execute a single node (PLAN3 Phase I, run & iterate — gap G16).** The “test fast” cure, sibling to I-T1's pin data: from the editor, run ONE node with its last-captured (or empty) input and see the output, WITHOUT running — or side-effecting — the whole flow. **DESIGN (ROADMAP Decision Log #22):** a NEW optional `StartInput.stopAfterNode?: NodeId` mirrored onto a NEW persisted `ExecutionState.stopAfterNode?`. **Engine (`packages/core/src/engine/executor.ts`):** `start()` stamps `stopAfterNode` on the state when set (omitted on every normal run → persisted state byte-identical to pre-I-T2); in `runLoop`, AFTER the per-step I/O snapshot log (so the node's output IS captured) and BEFORE `switch(result.kind)`, if `state.stopAfterNode === node.id` AND the result is `items`/`goto`, the run is finalized `done` and ENDS — it never routes downstream. A `wait`/`end`/`error` result terminates the step naturally, so a single-node run of a WAIT node correctly reports `waiting`. The boundary is PERSISTED (not a transient arg), matching the I-T1 `testRun` seam, so it survives pause/resume (I4). **Shared:** `ExecutionStateSchema.stopAfterNode` (NodeId, optional) + NEW `RunNodeBodySchema` (`{ nodeId, input?: FlowItem[]≤200 }`) + `RunNodeBody` in `api.ts`. **Server (`apps/server/src/api/flows.ts`):** NEW `POST /api/flows/:id/run-node` — ALWAYS a TEST run (`testRun:true`, so pinned data is honoured) entering at the target node with `stopAfterNode` = that node; 404 `not_found`/`node_not_found`, 422 `invalid_graph`/`node_disabled`, 400 `invalid_body`, 503 when no engine; the output is read back from `exec_logs`, not returned inline. **Editor:** `api.runNode(id, nodeId, input?)`; the NDV (`NodeDetail.tsx`) gains a “▶ Run this node” button that `saveNow()`s, runs ONLY this node with its last input (or empty), then refreshes the run-data overlay; i18n `editor.node.runNode.*` en/fa parity; CSS `.ndv-run-node`. All optional/no-default → every existing flow + persisted state parses byte-identically. **Verify:** typecheck all 6 workspaces GREEN (`apps/server` with `--max-old-space-size=3072`); tests core +5 (runs only the target / target executes via the full path / `stopAfterNode` persisted + chosen input reaches the node / single-node run of a WAIT node → `waiting` / normal run unaffected) + server run-node 6 + editor api-client +1 — shared 96 / core 60 / editor 240 / nodes 535 / server 456 all GREEN. **Next: I-T3 (Keyboard shortcut help overlay + coverage sweep, gap G11).**
Task-prevI1: I-T1 **Pin data on a node (PLAN3 Phase I, run & iterate — gap G4; OPENS Phase I).** The "test fast" cure: a node can carry a pinned sample so a TEST run reuses it instead of re-executing (re-hitting a slow/expensive/side-effecting node). **DESIGN (ROADMAP Decision Log #21):** a NEW optional `FlowNode.pinnedData` (`z.array(FlowItemSchema).max(50)` — a pin is a sample, not a dataset) + a NEW persisted `ExecutionState.testRun?` flag. **Engine:** the executor's `start()` stamps `testRun` from a new `StartInput.testRun`; in `runLoop`, BEFORE the disabled-branch, a node carrying `pinnedData` IN A TEST RUN short-circuits — it EMITS the stored sample on the universal `main` port (a `structuredClone`, so a downstream node can't mutate the stored pin) INSTEAD of executing, never in production. The flag is PERSISTED on the state so it SURVIVES pause/resume (I4 — a WAIT/resume keeps honouring pins; a transient `start()` arg would be lost on the first resume, silently disabling pins mid-run). **Server:** ONLY the editor's "Test run" endpoint `/api/flows/:id/run` passes `testRun: true`; production runs (router/scheduler/webhook) omit it → pins invisible to a live bot. **Editor:** `updateNode` accepts `pinnedData`; NEW pure DOM-free `flattenOutputForPin(output)` + `PIN_ITEMS_CAP=50` in `canvas/run-data.ts` (flatten a node's last run output, main-port first, capped — F-T3 pattern, unit-tested directly); NDV gains a pin bar (📌 Pin output / Unpin / "Pinned (n) — used in test runs"), `CtbNode` shows a 📌 Pinned badge; i18n `editor.node.pin.*` en/fa parity; CSS `.ndv-pinbar`/`.ndv-pinned`/`.ndv-pin-hint`/`.ctb-node-pinned`. All optional/no-default → every existing stored flow/fixture/export parses byte-identically. **Verify:** typecheck all 6 workspaces GREEN (`apps/server` with `--max-old-space-size=3072`); tests core +5 (pin used in test run, ignored in prod, feeds downstream, empty pin→no items, flag survives WAIT/resume) / shared +7 (schema) / editor +4 (`flattenOutputForPin`) — shared 96 / core 55 / editor 239 / nodes 535 / server 450 all GREEN. **Next: I-T2 (Execute a single node, gap G16).**
Task-prevH4: H-T4 **Add-node-on-edge + wire-drop-to-palette (PLAN3 Phase H, canvas power — gaps G8/G9; COMPLETES Phase H).** Two "wire-first" authoring affordances so a user grows a flow without the drag-from-palette-then-rewire dance. **DESIGN (F-T3 pattern — risky structural edits are PURE + DOM-free in `apps/editor/src/canvas/graph.ts`; the React/RF layer is thin glue):** **(G8) add-node-on-edge:** a NEW custom edge `CtbEdge.tsx` (registered as `edgeTypes.default`) renders an inline "+" at the wire midpoint via `EdgeLabelRenderer` (revealed on hover/selection); clicking it opens a small floating `NodePicker.tsx` popup pre-targeted at THAT edge, and choosing a type runs the pure `insertNodeOnEdge(graph, edgeId, type, pos)` — an edge `A.outPort → B.inPort` becomes `A.outPort → N.main` + `N.main → B.inPort`, original removed. **Subtle bug the tests caught + fixed:** the two new edge ids must be minted against the graph that STILL contains the original edge (then drop the original LAST), else `nextEdgeId` reuses the just-freed id and silently resurrects the old A→B edge. **(G9) wire-drop-to-palette:** `onConnectStart` records the dragged handle (`OnConnectStartParams`); `onConnectEnd`, when the drop lands on `.react-flow__pane` (not a handle), opens the SAME `NodePicker` at the drop point, and the pure `nodeFromDangling(graph, pending, type, pos)` creates the node already wired to the dangling end (a pending SOURCE → `A.outPort → N.main`; a pending TARGET → `N.main → B.inPort` — RF's `handleType` tells us which end was fixed). **Store (`stores/canvas.ts`):** NEW `insertNodeOnEdge` (returns the new node id, or `null` for an unknown edge — no-op, no history) + `addNodeFromDangling`, each a single `commit(...)` so the whole rewrite is ONE undoable, autosaved edit; every produced edge lands on the universal `'main'` port and the result re-validates against `FlowGraphSchema`. **Shared pure helpers added:** `buildNode`, `insertNodeOnEdge`, `nodeFromDangling`, `PendingConnect`. **i18n (`en.ts`+`fa.ts`, parity):** `editor.edge.insert` + `editor.picker.title`. **CSS (`styles.css`):** `.ctb-edge-tools`/`.ctb-edge-add` (the hover-revealed "+"), `.node-picker*` (the popup); also fixed a latent UNDEFINED `--panel` var (referenced 4×, never declared) → `--bg-raised`. **Tests:** editor `canvas-graph.test.ts` +5 (`insertNodeOnEdge` split semantics + branch-port preservation + unknown-edge→null; `nodeFromDangling` source/target wiring; `buildNode` uniqueness/shape) + `canvas-store.test.ts` +4 (insert commits one undoable A→N→B & undo restores the original edge; unknown-edge no-op leaves history untouched; dangling source/target produce the correct single edge). **Verify:** all 6 workspaces typecheck GREEN (server with `--max-old-space-size=3072`); shared **89 GREEN**; editor **235 GREEN** (was 226, +9); i18n-parity GREEN; editor build GREEN. Editor-only — no schema/server-route/registry/engine change; stored flows unchanged. **Phase H COMPLETE (H-T1 sticky notes → H-T2 node title → H-T3 error surfacing → H-T4 wire-first add). Next: Phase I (run & iterate) — I-T1 (pin data on a node; touches the stored flow schema + engine → needs its own Decision Log entry).**\\nTask-prevH3: H-T3 **Canvas error surfacing (PLAN3 Phase H, canvas power — gap G15).** The cure for "a run failed but the canvas doesn't show me WHERE": a node that failed on the LATEST execution now visibly glows red and surfaces its error in one click. **DESIGN (F-T3 pattern — risky logic in a pure module, React is thin glue):** the decision logic is a NEW pure, DOM-free `mapRunErrors(logs)` in `apps/editor/src/canvas/run-data.ts`, sibling to the existing `mapRunData`. The key insight: a FAILED step's `exec_logs` row carries `level:'error'` but usually has NO input/output snapshot, so `mapRunData` deliberately SKIPS it — `mapRunErrors` keeps EXACTLY those rows. It prefers the structured `error` column, falls back to the human `message`, defaults to a stable non-empty `'error'`, and keeps the LAST error per node (a loop-revisit reports its most recent failure, matching mapRunData's "latest visit" semantics). **Store (`apps/editor/src/stores/run-data.ts`):** NEW `errorsByNode: Map<string,string>` populated by `mapRunErrors(detail.logs)` right beside `byNode` on `load`, and cleared on the no-execution path and `reset`. **Canvas (`CtbNode.tsx`):** reads `useRunData(s => s.errorsByNode.get(id))`; when set, the outer div gets an `errored` class (red pulsing glow) and the body renders a clickable `.ctb-node-error` `<button>` ("Failed on last run") whose `onClick` `stopPropagation`s (so it doesn't trigger RF selection/drag) and calls `useNodeDetail.open(id)` to open the NDV. **NDV (`NodeDetail.tsx`):** reads the same `errorsByNode` entry and, when present, renders a `.ndv-error` banner (`role=alert`) under the head so the cause sits next to the failing node's INPUT + params. **i18n (`en.ts`+`fa.ts`, parity-enforced):** NEW `editor.node.runError` ("Failed on last run" / "خطا در آخرین اجرا"). **CSS (`styles.css`):** `.ctb-node.errored` (danger outline + `ctb-node-error-pulse` keyframe, disabled under `prefers-reduced-motion`), `.ctb-node-error` (button reset → looks like the existing flags), `.ndv-error` (danger banner). **Tests (`apps/editor/test/run-data.test.ts`, +8):** a `mapRunErrors` describe (collects error-only rows mapRunData skips; error-column-over-message fallback; empty error+message→`'error'`; last-wins + ignores non-error & nodeless rows) + store tests (a FIXTURE execution where one node throws while another succeeds → `errorsByNode.get('fetch')` is the message, the OK node is absent from errors but keeps its I/O snapshot in `byNode`; `errorsByNode` cleared on never-ran + reset). **Verify:** all 6 workspaces typecheck GREEN (server with `--max-old-space-size=3072`); shared **89 GREEN**, editor **226 GREEN** (was 221, +5); i18n parity GREEN; editor build GREEN. Read-only run-data overlay — no schema/server-route/registry/engine change; stored flows unchanged. **Next: Phase H — H-T4 (add-node-on-edge + wire-drop-to-palette).**\\nTask-prevH2: H-T2 **Node rename / human title (PLAN3 Phase H, canvas power — gap G7).** The cure for "a big flow is a wall of identical type labels": a user can give a node a custom human name (e.g. "Welcome message" instead of "Send a message"). **DESIGN DECISION (ROADMAP Decision Log #20):** a NEW optional `FlowNode.title?` (`z.string().max(120).optional()`) — a PRESENTATIONAL human name, NOT a routing key. The editor head / param panel / NDV show `title` when set (else the type's i18n label), but the executor NEVER reads it — it routes solely by `id`/`type`/edges, exactly as it already ignores `position`/`note`. `title` is `.optional()` (NO default) so the OUTPUT `z.infer` keeps `title?` optional → every existing stored flow, fixture, template and export literal parses byte-identically. **Scope note:** "expressions can reference a node by title" (n8n `$('Node Name')`) was deliberately NOT shipped — CTB's linear cursor executor does not retain every node's output keyed by name, so it would be a deep durability change far outside an atomic H-T2 (a node id already uniquely addresses a node); if ever wanted it's a separate engine task with its own Decision Log entry. **Shared (`packages/shared/src/flow.ts`):** `title` added to `FlowNodeSchema` (≤120, optional, documented as canvas-only). **Editor pure helper (`apps/editor/src/canvas/graph.ts`):** NEW `nodeDisplayName(node, typeLabel)` — a non-blank TRIMMED `title` wins, else the type label (DOM-free → unit-tested directly; the React surfaces are thin glue, the F-T3 pattern). **Canvas head (`CtbNode.tsx`):** the head shows `displayName`; when a custom title differs from the type label, the type label drops to a `.ctb-node-type` sub-line so the user still sees WHAT the node is. **Panel (`ParamPanel.tsx`):** header shows `displayName`; a NEW "Name" text field (placeholder = the type label, ≤120, `dir=auto`) edits `title` via `updateNode(id, { title })` (blank → `undefined`). **NDV (`NodeDetail.tsx`):** modal header shows `displayName`. **Store (`stores/canvas.ts`):** `updateNode` patch type widened to include `'title'`. **i18n (`en.ts`+`fa.ts`, parity-enforced):** `panel.title` ("Name" / "نام") + `paramDesc.panel.title`. **CSS (`styles.css`):** `.ctb-node-type` (dim italic sub-line under a custom title). **Tests:** shared `flow.test.ts` +6 (title optional/absent; sample flow byte-id no-title; custom title preserved; RTL/Persian title; >120 rejected; =120 accepted); editor `canvas-graph.test.ts` +5 (`nodeDisplayName`: fallback when unset/undefined, title-wins, blank/whitespace→fallback, trims, RTL verbatim). **Verify:** all 6 workspaces typecheck GREEN (server with `--max-old-space-size=3072`); shared **89 GREEN** (was 83), editor **221 GREEN** (was 216); i18n parity test GREEN. Additive — no server/registry/node/engine change; stored flows unchanged. **Next: Phase H — H-T3 (canvas error surfacing — a failed node glows red, click → its error from `exec_logs`).**\nTask-prevH1: H-T1 **Sticky notes on the canvas (PLAN3 Phase H, canvas power — gap G3; OPENS Phase H).** The cure for "a big flow is an unlabelled wall of nodes": a user can drop free-floating notes on the canvas to annotate regions, document intent, or leave TODOs. **DESIGN DECISION (ROADMAP Decision Log #19):** a sticky note lives in a NEW optional `FlowGraphSchema.notes: StickyNote[]` (`id`/`text`/`position`/`size`/`color`) — NOT a node type and NOT a separate store. It is a CANVAS-ONLY annotation: no params, no ports, and the executor never reads it (the engine touches only `graph.nodes`/`graph.edges`, exactly as it already ignores `position`). `notes` is `.optional()` (NOT `.default([])`) so the OUTPUT `z.infer` type keeps `notes?` optional → every existing stored flow, fixture, template and export literal parses byte-identically with zero edits; editor consumers read `graph.notes ?? []`. Notes ride the one flow document, so they get undo/redo, autosave and export/import for free. **Shared (`packages/shared/src/flow.ts`):** NEW `NoteIdSchema` (`note_*` alphabet), `NoteColorSchema` (`yellow|green|blue|pink|gray`), `StickyNoteSchema` (text ≤5000, size clamped 80..2000 × 60..2000, defaults 240×160 yellow), `notes` added to `FlowGraphSchema` + a note-id-uniqueness check in `superRefine`. **Editor pure mapping (`apps/editor/src/canvas/graph.ts`):** `StickyRfNode`/`AnyRfNode` types, a `note:` rf-id namespace (`rfIdForNote`/`noteIdFromRf`), `notesToRfNodes` (maps notes to `sticky` RF nodes BEHIND flow nodes with `zIndex:0` so they never intercept connection drags), `nextNoteId` (smallest free `note_<n>`, independent of node ids). **Store (`apps/editor/src/stores/canvas.ts`):** `addNote`/`updateNote`/`removeNotes` + note-aware `moveNote`/`resizeNote` reusing the SAME `preMove`/`commitMove` coalescing as node drags (one gesture = one undo entry); empty-graph init now seeds `notes: []`. **Component (`apps/editor/src/canvas/StickyNote.tsx`, NEW):** a React Flow custom node with `NodeResizer`, in-place text edit (dblclick/✎, Esc/Ctrl+Enter), and a 5-colour picker, all committing via the store; `nodrag` on interactive bits. **Canvas (`FlowCanvas.tsx`):** registers `sticky` in `nodeTypes`, renders `[...notesToRfNodes(...), ...flowToRfNodes(...)]` (notes first/behind), routes note move/resize/remove changes through `noteIdFromRf`, and guards `onDbl` so notes don't open NodeDetail. **Toolbar (`FlowEditorPage.tsx`):** an `AddNote` button (jittered placement). **i18n (`en.ts`+`fa.ts`, parity):** `editor.note.*` (add/edit/empty/placeholder + 5 colour labels). **CSS (`styles.css`):** `.sticky-*`. **Tests:** shared `flow.test.ts` +8 (no-notes parse, defaults, round-trip, dup-id rejected, note-id may equal a node id, unknown colour rejected, size clamps) + `flow-export.test.ts` +1 (notes survive export→serialize→import byte-for-byte); editor `canvas-graph.test.ts` +5 (notesToRfNodes payload/zIndex/selection, rfId↔noteId round-trip, nextNoteId namespacing) + `canvas-store.test.ts` +5 (add/update/remove + nodes-untouched + move/resize gesture coalescing & undo). **Verify:** all 4 workspaces typecheck GREEN (server with `--max-old-space-size=3072`); shared **83 GREEN** (was 74), editor **216 GREEN** (was 206), core **50 GREEN** (engine inert); editor build GREEN. Additive — no registry/route/engine change; stored flows unchanged. **Next: Phase H — H-T2 (node rename / human title; touches the stored flow schema → its own Decision Log entry).**\\nTask-prevG3: G-T3 **Explicit "simple vs advanced" field grouping (PLAN3 Phase G, field ergonomics — ⭐ gap G5; COMPLETES Phase G).** The cure for "the node still looks busy even after `+ Add option`": a node author can now demote a power-user param so it lives under a collapsible **Advanced** section instead of the always-shown set / add-menu. **DESIGN DECISION (ROADMAP Decision Log #18):** the flag is a STRUCTURAL `z.meta({ advanced: true })` annotation on the param's Zod schema — NOT a stored flag, NOT a schema/engine change — mirroring the existing `ctbWidget` annotation pattern. `z.toJSONSchema(..., { io:'input' })` carries `advanced:true` to the editor as a plain field property (verified empirically, exactly like `ctbWidget`), so the form engine reads it purely structurally and Collection forms reuse it for free (invariant I2). The param's type, validation and persisted value are byte-identical → engine, stored flows and exports are unchanged (server 450 + shared 74 stay GREEN, proving the meta touch is inert). **Pure layer (`apps/editor/src/form/schema.ts`):** `JsonSchema` gains `advanced?: boolean`; NEW `isAdvanced(spec)` (reads `spec.schema.advanced===true`); `PartitionedFields` gains a third `advanced: FieldSpec[]` bucket; `partitionFields` routes an advanced field to it FIRST (precedence over required/isSet/added — an author's "demote it" wins); NEW `anyAdvancedSet(advanced, value)` (true iff an advanced field already holds an `isSet` value → drives auto-open). **UI (`apps/editor/src/form/SchemaForm.tsx`):** the shown-field row markup is extracted into a `renderField(spec, removable)` helper shared by the shown set and the Advanced section (advanced fields are author-exposed, not user-added → `removable:false`, no "×"); when `advanced.length>0` a native `<details class="form-advanced">` "Advanced" `<summary>` renders the advanced fields, `open` (and `key`-remounted) per `anyAdvancedSet` so editing an existing node never hides the user's own data behind a closed flap. **Author change (`packages/shared/src/node-params.ts`):** `tg.sendMessage.parse_mode` → `.meta({ advanced:true })` — the first param demoted (a power-user HTML/MarkdownV2 toggle beginners never touch). **i18n (`en.ts`+`fa.ts`, parity-enforced):** `form.advanced.title` (en "Advanced" / fa "پیشرفته"). **CSS (`styles.css`):** `.form-advanced` / `.form-advanced-summary` (custom ▸ caret rotating on `[open]`) / `.form-advanced-body`. **Tests (`form-engine.test.ts`, headless): +5** — `isAdvanced` true/false off the real schema; an advanced field routed to `advanced` even when SET (never shown/optional); default (unannotated `tg.trigger`) split byte-identical → empty advanced bucket; `anyAdvancedSet` truth table (advanced-set vs non-advanced value vs blank vs no-advanced-fields); `parse_mode='HTML'` STILL validates against the REAL `TgSendMessageParamsSchema` (presentational only) + all three buckets cover every prop exactly once; the existing `tg.sendMessage` progressive-disclosure test updated to assert `parse_mode` lands in `advanced` (not optional) and to sum all three buckets. **Verify:** editor typecheck GREEN; editor tests **206 GREEN** (was 201, +5); editor build GREEN; shared **74 GREEN**; server **450 GREEN** (schema-meta touch inert). Additive — no registry/route/engine change; stored params unchanged. **Phase G COMPLETE. Next: Phase H (canvas power) — H-T1 (sticky notes; touches the stored flow schema → needs its own Decision Log entry).**\nTask-prevG2: G-T2 **Live expression preview (PLAN3 Phase G, field ergonomics — gap G6).** While editing an expression, a thin READ-ONLY result row under the `ExpressionInput` shows the resolved value against the latest run's FIRST input item, so a user can SEE what `{{ $json.x }}` evaluates to without running the flow. **DESIGN DECISION (logged in the module header):** the preview resolves CLIENT-SIDE with a tiny SAFE path-resolver — NOT the real engine and NOT a server endpoint. Why: (a) the engine's evaluator (`packages/core/src/expression`) runs every segment inside the `@ctb/sandbox` Node-vm worker pool → cannot run in a browser; (b) a `/preview-expression` round-trip would re-introduce the exact 50ms sandbox-budget cold-start flakiness just cured in PR #73, for a feature whose whole point is instant keystroke feedback. So we support the COMMON, side-effect-free subset drag-to-map produces and users type: scope-root access (`$json`/`$vars`/`$items`/`$now`), dotted/bracketed property paths, array indexing, the nullary `$now.iso()/ts()/date()` helpers, and bare literals. Anything outside that (arithmetic, method calls, function args, unknown roots) ⇒ `unsupported` → the row shows "preview unavailable — runs live"; a path past missing/null data ⇒ empty string (engine parity). It NEVER evaluates arbitrary code and NEVER throws — the real engine stays the single source of truth at run time; this is advisory UI only. **Pure layer (`apps/editor/src/form/preview.ts`, NEW, DOM-free):** `PreviewScope` + `buildPreviewScope()` + `previewExpression(template, scope)` (lone `{{ }}` → RAW value per engine; mixed → concatenated string) + `resolveExpr()` (the safe chain walker) + `renderValue()` (display rendering, clips at 200 chars). `makeNowHelper`/`NowHelper` added to `form/expression.ts` (a DOM-free copy of the engine's `$now`, so the editor needn't import `@ctb/core`). **UI (`apps/editor/src/form/widgets.tsx`):** NEW `PreviewContext`/`PreviewScopeProvider`/`usePreviewScope`; `ExpressionInput` computes `previewExpression(value, scope)` (only when there IS a `{{ }}` and a scope) and renders an `.expr-preview` row (`aria-live=polite`, `=` arrow + `<code>` value, or the dim italic unsupported note). **Wiring:** `SchemaForm` gains an optional `previewScope` prop wrapping `FormBody` in the provider (default `null` → generic Collection forms unchanged); `NodeDetail` builds the scope from `run.input[0].json` (+ `$items` = the run's input) via `useMemo` and passes it down. **i18n (`en.ts`+`fa.ts`, parity):** `form.expr.previewEmpty` + `form.expr.previewUnsupported`. **CSS (`styles.css`):** `.expr-preview`/`.expr-preview-arrow`/`.expr-preview-value`/`.expr-preview-note`. **Tests (`form-engine.test.ts`, headless): +11** — lone-value/raw parity, dotted/bracket/index paths, nested→JSON, mixed-string, missing-path→'' , `$now` nullary helpers, the WHOLE unsupported set (arithmetic/method/args/unknown-root/mixed-with-bad-segment) never-throws, bare literals, no-expr/empty=empty, `renderValue` clipping, and empty-scope tolerance. **Verify:** editor typecheck GREEN; editor tests **201 GREEN** (was 190, +11); build GREEN. Additive — no schema/server/registry change. **Next: Phase G — G-T3 (explicit simple/advanced field grouping).**
Task-prevG1: G-T1 **Fixed | Expression toggle per field (PLAN3 Phase G, field ergonomics — ⭐ user ask, gap G1).** The cure for "every node is confusing": a "simple" field could only hold a literal, so a user who wanted `{{ $json.age }}` to drive a NUMBER / SELECT / DURATION param had no UI affordance. **Pure layer (`apps/editor/src/form/expression.ts`, DOM-free):** NEW `FieldMode = 'fixed' | 'expression'` + `fieldModeOf(value)` (a string containing `{{ }}` ⇒ `expression`, everything else — literal / empty / undefined / non-string / unclosed-brace ⇒ `fixed`) + `convertFieldMode(to, value)` (→ expression seeds the editor with the literal as text, nullish→`''`, never auto-wraps `{{ }}`; → fixed keeps the expression string verbatim, non-strings pass through). The mode is **INFERRED from the value, NOT stored** as a separate flag → the persisted params stay byte-identical to what the engine already accepts (a lone `{{ … }}` resolves to the RAW value BEFORE Zod runs — `core/engine/params.ts`), so NO schema change and Fixed↔Expression is a lossless string/value conversion. **UI (`apps/editor/src/form/widgets.tsx`):** NEW `FieldModeToggle` (a two-button `role=group` pill, `aria-pressed`) + `ModedWidget` (wraps a child widget: Fixed renders the native widget, Expression renders the existing expression-aware `ExpressionInput`, flipping calls `convertFieldMode`); `SchemaWidget`'s `number` / `select` / `duration` cases are wrapped in `<ModedWidget>` (text/multiline/expression are already inline expression-aware → intentionally NOT wrapped, the toggle would be redundant). **i18n (`en.ts` + `fa.ts`, parity-enforced):** `form.fieldMode.label` / `.fixed` / `.expression`. **CSS (`styles.css`):** `.moded-widget` + `.field-mode` + `.field-mode-btn` (a compact pill mirroring the `.union-tabs` segmented control). **Tests (`apps/editor/test/form-engine.test.ts`, headless):** NEW "field mode (Fixed | Expression) — G-T1" describe (5 — mode inference incl. unclosed-brace=fixed, Fixed→Expression seeds literal, Expression→Fixed keeps verbatim, lossless round-trip both directions, and an expression-on-a-NUMBER-field that resolves to a number VALIDATES against the REAL `TgWaitForReplyParamsSchema` `max_retries: z.number().int().nonnegative()`). **Verify:** editor typecheck GREEN; editor tests **190 GREEN** (was 185, +5); `npm run build -w apps/editor` GREEN. Additive — no schema/server/registry change. **Next: Phase G — G-T2 (live expression preview).**
Task-prevFLAKY: FLAKY-FIX **Expression-budget host seam — the real cure for the "e2e tests are flaky in this sandbox" worry (test hardening, between F-T2 and G-T1).** The diagnosis: the flakiness was NOT polling — every `{{ }}` is evaluated in a sandbox worker with a HARD 50ms time budget (strict by design), and on a loaded/contended host the worker's COLD START alone can exceed 50ms → an otherwise-correct flow errors with `expression exceeded 50ms budget` (confirmed by dumping `done.error` from the failing `e2e-phaseA` run: `node failed at "compose": expression exceeded 50ms budget: sandbox timeout after 50ms (worker killed)`). Tests like `e2e-phaseA`/`e2e-nodes-flow`/`e2e-phase4` failed even run ALONE; others only under single-fork contention. The pre-existing `ExecutorServices.evalOptions.budgetMs` seam (used by `e2e-phaseE` since PE-T5) was the proven fix, but `wireEngine` did not expose it. **Production seam (NOT a test-only hack):** NEW `WireOptions.expressionBudgetMs?: number` (`apps/server/src/engine/wire.ts`) → forwarded to `services.evalOptions = { budgetMs }` ONLY when set (omitted ⇒ the strict 50ms default is untouched); NEW env var `CTB_EXPRESSION_BUDGET_MS` (`z.coerce.number().int().min(1).optional()`, `lib/env.ts`) wired through `main.ts` — so an operator on a slow/contended box can raise the budget with zero code change. **Tests:** every server test that builds an executor now passes `expressionBudgetMs: 5_000` (the 16 single-line `wireEngine({ db, ctbSecret })` calls + the `sqlite`/`fetchImpl`/`dbPoolFactory`/multi-line variants), and the two direct-`ExecutorServices` e2es (`e2e-phaseA`, `e2e-nodes-flow`) set `evalOptions: { budgetMs: 5_000 }` directly — mirroring `e2e-phaseE-voice-demo`'s long-standing pattern. **Verify:** the 7 previously-failing e2e files now 20/20 GREEN single-fork (the exact "hard case" that gave 6 failures before); the FULL server suite **49 files / 450 tests GREEN ×2 consecutive single-fork runs** (deterministic — no flakiness). Additive — no node/registry/flow change; production default behaviour unchanged. **Next: Phase G (field ergonomics) — G-T1 (Fixed | Expression toggle per field).**
Task-prevF2: F-T2 **"Hello bot" first-run template + 5-minute walkthrough doc (PLAN3 Phase F, discoverability — COMPLETES Phase F).** F-T1 gave the new user a guided empty state whose PRIMARY CTA is "Start from template"; F-T2 makes that land on a template that WORKS in one click. **Shared (`packages/shared/src/flow-templates.ts`, I2/I5):** NEW `hello` template — the smallest possible flow: `flow.manualTrigger` (with a `sample` of `{ "chat": 123456789 }`) → `tg.sendMessage` (`chat: '{{ $json.chat }}'`, a fixed greeting text) — placed FIRST in `FLOW_TEMPLATES` so it leads the gallery (`[hello, feedback, quiz, faq, reminder]`). It is a GENERIC greeting (no business domain, I2) and a `FlowExport` envelope identical to what import accepts. The manual trigger's `sample` seeds `$json.chat`, so the greeting has a destination with ZERO wiring from the user — the whole point of the quick-start. **i18n (`apps/editor/src/i18n/{en,fa}.ts`, parity-enforced):** `templates.hello.label` + `templates.hello.desc` in both locales (fa default). **Tests:** `packages/shared/test/flow-export.test.ts` — added `FlowManualTriggerParamsSchema` to the per-type PARAM_SCHEMAS coverage, updated the stable-id list to lead with `hello`, and a NEW test pinning the hello template to a minimal `flow.manualTrigger → tg.sendMessage` shape with a `sample` that contains `chat` (uses `.find(type===…)` not `nodes[0]` for `noUncheckedIndexedAccess`). NEW e2e `apps/server/test/e2e-phaseF-quickstart-demo.test.ts` (2, harness mirrored from `e2e-phaseC-authoring-demo`): import-template `hello` → 201 draft of exactly `[flow.manualTrigger, tg.sendMessage]` → `/activate` → `{ok,status:'active'}` → the editor's `/run` (\"Test run\" button) reaches `done` and the (fake) bot ACTUALLY sends the greeting to chat `123456789`; a 2nd test pins the delivered text to the template's exact greeting (guards the doc against drift). **Robustness fix:** the e2e gives the engine its (fake) Telegram sender via `gateway.registerBot(...)` directly — NOT the HTTP `/start` endpoint — because `/start` fires grammY's fire-and-forget long-poll (`bot.start()`), which with a fake transport races to an \"Aborted delay\" crash and makes a panel `/run` flaky (the same flake the older polling-based e2es exhibit in this sandbox). Registering directly yields the SAME rate-limited `TgSender` the live gateway uses (I6) with none of the polling noise — so this e2e is deterministic (verified 3× alone + with `api-bots-flows`). Two existing tests that hardcode the template-id list were updated to include `hello` first: `apps/editor/test/api-client.test.ts` + `apps/server/test/api-bots-flows.test.ts`. **Doc:** NEW `docs/demos/quickstart.md` — a 5-minute walkthrough (what you'll build → setup → start from the hello template → see it reply via Test run → activate → the reusable loop → where to go next), referencing the auto-exercising e2e as the proof. **Verify:** shared typecheck + 74 tests GREEN; editor typecheck + 185 tests GREEN; the new quickstart e2e 2/2 GREEN (deterministic ×3). Additive — no schema/registry/server-route change (only a new template + docs + i18n + tests). **Phase F is now COMPLETE. Next: Phase G (field ergonomics) — G-T1 (Fixed | Expression toggle per field).**
Task-prevF1: F-T1 **Guided empty state + "Quick start" — the "I couldn't build anything" cure (PLAN3 Phase F, discoverability).** The PLAN3 diagnosis was discoverability, not missing capability: a brand-new user hit an empty flow list / empty canvas with no idea what to do, even though import / templates / a blank canvas all already existed. **Editor (`apps/editor`):** NEW PURE module `src/lib/empty-state.ts` — `emptyStateActions()` returns the three CTAs in recommended order (`template` PRIMARY → `import` → `blank`), `isCanvasEmpty(graph)` gates the editor hint, `CANVAS_HINT_KEYS` names the hint's i18n keys; NEW `src/components/EmptyState.tsx` — `<FlowsEmptyState onAction>` (three CTA cards) + `<CanvasEmptyHint onOpenPalette>` (thin React glue over the pure layer, F-T3 pattern). **Wiring:** `FlowsPage.tsx` replaces the bare `flows.empty` line with `<FlowsEmptyState>`; `onEmptyAction` maps each CTA onto an EXISTING affordance the page already owns (`template`→`openTemplates()`, `import`→`setShowImport(true)`, `blank`→`setShowForm(true)`) — no second flow-creation path (principle 1, discoverability over capability). `FlowEditorPage.tsx` adds `<CanvasHintOverlay>` inside `.editor-canvas` (now `position:relative`); it subscribes to the canvas store, shows the hint while `isCanvasEmpty(graph)`, and its CTA scrolls the always-visible palette into view + flashes it (`palette-flash` keyframe), disappearing the moment any node is placed. **i18n:** `flows.emptyState.*` (title/lead + template/import/blank title+desc) + `editor.canvasHint.*` (title/body/cta) in BOTH `en.ts`/`fa.ts` (parity tested). **CSS:** `.empty-state*` card grid + `.canvas-hint` centred overlay + `.palette-flash` animation appended to `styles.css`; `.editor-canvas` got `position:relative`. **Tests:** NEW `test/empty-state.test.ts` (8 — CTA set/order, single-primary, fresh-array, every title/desc key resolves in fa+en, heading/lead exist, `isCanvasEmpty` null/undefined/empty=true & any-node=false, canvas-hint keys resolve in both locales). **Verify:** editor typecheck GREEN; editor tests **185 GREEN** (was 177, +8); `npm run build -w apps/editor` GREEN. Additive — no schema/server/registry change. **Next: F-T2 ("Hello bot" first-run template + 5-minute walkthrough doc).**
Task-prevF3: F-T3 **Surface export in the flow EDITOR toolbar (PLAN3 Phase F, first shipped PLAN3 task).** An **Export** button in the editor toolbar (next to Save / Test run) — the cure for the #1 complaint ("how do I extract a workflow?"). It `saveNow()`s pending edits, fetches `GET /api/flows/:id/export`, and downloads the portable envelope (graph + settings, no identity) as `<name>.json`. Filename/blob logic extracted to a PURE tested helper (`flowExportFilename`/`flowExportBlob` in NEW `apps/editor/src/lib/flow-export.ts`) shared by the editor toolbar + the FlowsPage list (de-duplicated); `downloadFlowExport` is the thin DOM glue. i18n en/fa; NEW `apps/editor/test/flow-export.test.ts` (6). Editor 177 GREEN. Additive.
Task-prevE5: PE-T5 **🎬 Demos + docs — the FINAL Phase-E task; two live-voice scenarios from ONE node set.** Both scenarios are built from the SAME generic nodes, differing only in SETTINGS (invariant **I2** — config, not forks). **Demo A — AI voice support (1:1):** `phase-e-voice-support-flow.json` (8 nodes) — `trigger.callEvent(mode:support,targetKind:user)` → `ai.speechToText(source:file)` → `ai.agent` (+ `ai.modelOpenai`/`ai.memoryPostgres` keyed `call-{{ $json.target.id }}`/`tool.think`/`tool.httpRequest` providers) → `ai.textToSpeech(pcm)` → `call.speak(source:file)`; proves the WHOLE Phase-B AI tier (PB-T4/5/6/7) reused verbatim for live calls. **Demo B — channel Q&A moderator (lineup):** `phase-e-qa-moderator-flow.json` (5 nodes) — `trigger.callEvent(mode:lineup,order:sequential,targetKind:channel)` → `call.grantTurn(userId:{{ $json.speakerId }})` → `ai.textToSpeech` greeting → `call.speak` → `call.endTurn`; proves grant→speak→endTurn line advancement from generic nodes + settings only. **Test (`apps/server/test/e2e-phaseE-voice-demo.test.ts`, 4):** drives the **real** Executor + **real** `CallSessionService` + **real** `LoopbackVoiceConnector` (a `connector.speak` spy records the played PCM) with fake scripted AI/db/http/files/tg + `evalOptions:{budgetMs:5_000}` (the host expression-budget tuning seam, to dodge the slow-sandbox 50ms cold-start flake) — (1) SUPPORT full loop (asserts STT bytes, model from the `ai:model` provider, the order_status HTTP tool ran with `orderId=B-42`, TTS reply played to `user:900`, memory persisted; the memory `session_key` is asserted as the LITERAL `call-{{ $json.target.id }}` because the slot resolver validates provider params but does NOT evaluate their `{{ }}`), (2) SUPPORT memory replay across two utterances, (3) LINEUP queue `[111,222]`→grant/speak/endTurn→`[222]`→`[]`, (4) static "same nodes, support-vs-lineup mode" invariant. **Supporting fixes:** `call.grantTurn`/`call.mute` `userId` → `z.coerce.string()` (a numeric resolved id passes Zod); `CallSessionService.grantTurn` explicit-user queue filter unified by `String()` (numeric queue entry vs string userId). **Docs:** NEW `docs/demos/phase-E-voice.md` (both demos, mirroring the phase-B walkthrough) + NODES.md §`call.*` cross-ref + the new **PROTOCOL.md "Live voice" chapter** (why a separate transport, connector kinds userbot/companion/external/loopback, the `voiceConnection` credential, hard caps, ToS posture); PLAN2.md PE-T5 ✅. **Verify:** shared typecheck GREEN; shared 73 / nodes 535 / call-session+call-events 39 GREEN; the PE-T5 demo 4/4 stable ×3. **Phase E is COMPLETE — all of PLAN2 (A–E) is done.**
Task-prevE4: PE-T4 **`call.*` action nodes — the six live-voice ACTION nodes a flow drives a call back with.** All serve BOTH scenarios via config (invariant **I2**): a `lineup` Q&A broadcast AND a 1:1 `support` AI call, through the SAME six nodes via `targetKind`/`targetId`/`mode`/`order`. **Shared (`packages/shared/src/node-params.ts`, I5):** SIX NEW schemas after `CallEventTriggerParams` — `CallConnectParamsSchema` (`connection` voiceConnection ref + `targetKind`/`targetId` + `mode` support/lineup + `order` sequential/random + `maxTurnSeconds`), `CallSpeakParamsSchema` (`targetKind`/`targetId` + `source` file|pcm + `fileId` | `pcmBase64`+`pcmSampleRate`), `CallGrantTurnParamsSchema` (+ optional `userId` line-jump + `save_as` default `granted`), `CallEndTurnParamsSchema`, `CallMuteParamsSchema` (+ required `userId` + `muted` default true), `CallLeaveParamsSchema`; a shared private `CallTargetKindSchema` enum (chat/channel/user). **Nodes (`packages/nodes/src/call/`):** `callConnect` (icon `phone`), `callSpeak` (icon `megaphone`; `source:'file'` plays a CTB file id e.g. `ai.textToSpeech` output read host-side I6, `source:'pcm'` base64-decodes to `{samples,sampleRate}`), `callGrantTurn` (icon `mic`; merges the granted id under `save_as`), `callEndTurn` (icon `mic-off`), `callMute` (icon `volume-x`), `callLeave` (icon `phone-off`) — each fails LOUDLY when `ctx.call` is null (I6) and drives the shared `CallCapability`; registered in `index.ts` (registry **55→61**; all 15 count guards bumped). **i18n:** 6 `nodes.call.*.label` + 6 `nodeDesc.call.*` in BOTH `en.ts`/`fa.ts` (parity compile-enforced). **Tests:** NEW `call-nodes.test.ts` (19 — per-node `ctx.call` recording, speak file/pcm decode, grantTurn `save_as`+explicit user, mute flag, fail-loud per node); nodes suite **535 GREEN**. Docs NODES.md (`call.*` section) + PLAN2.md (PE-T4 ✅). **Next: PE-T5 (demos + docs — AI voice support + channel Q&A moderator).**
Task-prevE3: PE-T3 **`trigger.callEvent` — the generic live-call entry node + its host event bus.** One node serves BOTH live-voice scenarios via config (invariant **I2**): a `lineup` Q&A broadcast AND a 1:1 `support` AI call. **Shared (`packages/shared/src/node-params.ts`, I5):** NEW `CallEventTriggerParamsSchema` — `connection` (a `voiceConnection` credentialRef), `targetKind` (chat/channel/user), `targetId`, `events` (≥1 of `callJoined|utteranceFinal|turnOpened|callLeft`, default `[utteranceFinal]`), `mode` (`support`/`lineup`), `order`, `maxTurnSeconds`, `autoAdvance`. **Node (`packages/nodes/src/flow/call-event-trigger.ts`):** `callEventTrigger` — a pure PASS-THROUGH trigger (icon `phone`, ports `[]→main`, `rawParamKeys:['connection','targetId']`) like `schedule.trigger`; registered in `index.ts` (registry **54→55**; all count guards bumped). **Host (`apps/server`):** NEW `triggers/call-events.ts` — `CallEventBus` (sibling of the record-write bus) subscribes to the Call Session Service's `onUtterance` + the NEW `onLifecycle` stream; for each event it scans `flowSource.activeFlows(botId)` for a matching `trigger.callEvent` (the PURE `matchCallEvent` — target kind + id string-unify + `events.includes(kind)`; `mode` is NOT a match key, it's carried into the item) and fires it CHATLESS via the new `router.fireCallEvent` (chatId/userId null — a voice flow answers over `ctx.call`, NODES.md). `utteranceFinal` persists the segmented PCM **once** (lazily, reused across matching flows) as a CTB file id (`audio/l16`) via `fileStore.putLocal` so `ai.speechToText` reads it (I6); the item carries `{event,target,mode,speakerId?,audioFileId?,audioMime?,audioSampleRate?,currentTurn?,queue?}`. `emit` NEVER throws (a dispatch failure can't break the live call). `call-session.ts` gained `CallLifecycleKind`/`CallLifecycleEvent`/`lifecycleListeners`/`onLifecycle`/`emitLifecycle`, fired on connect (`callJoined`), grantTurn (`turnOpened`), and leave (`callLeft`). **Wiring:** `wire.ts` builds `CallEventBus` (Engine handle `callEventBus`); `main.ts` calls `callEventBus.start()` after the scheduler and `callEventBus.stop()` in shutdown. **i18n:** `nodes.trigger.callEvent.label` + `nodeDesc.trigger.callEvent` in en/fa (parity). **Docs:** NODES.md gained a §"Live-voice Trigger (`trigger.callEvent`)" under Triggers + the PE-T1 note updated. **Tests:** NEW `call-event-trigger.test.ts` (7 — pass-through, defaults, lineup config, rawParamKeys, required-field + empty/unknown-event rejection, trigger ports) + NEW `call-events.test.ts` (15 — the PURE matcher, dispatch one-run-per-matching-flow, audio-persisted-once with mime/sampleRate, lifecycle items with currentTurn/queue, non-match/disabled/invalid skip, `emit` never-throws on router failure, and start()/stop() driven END-TO-END by a real `CallSessionService` + `LoopbackVoiceConnector` via connect/emitUtterance/grantTurn/leave; start() idempotent). **Verify:** all 6 packages typecheck GREEN; node-contract 7/7 + host 15/15 GREEN. Additive — no breaking change. Next: PE-T4 (`call.*` action nodes).
Task-prevE2: PE-T2 **Call Session Service + the `ctx.call` interface — the long-lived host runtime for realtime Telegram calls (the hard part of Phase E).** **Shared (`packages/shared/src/node-def.ts`, I5):** the live-voice capability surface — `CallTargetRef` (`kind` chat/channel/user + id), `CallMode` (`support`/`lineup`), `CallTurnOrder` (`sequential`/`random`), `CallSpeakRequest` (audio | fileId | pcm), `CallParticipant`, `CallStatus`, and `CallCapability` (`connect`/`speak`/`grantTurn`/`endTurn`/`mute`/`leave`/`status`); a NULLABLE `call: CallCapability | null` cap on `NodeCtx` (null on a host with no voice runtime → a `call.*` node fails loudly, I6). **Core (`packages/core/src/engine/executor.ts`):** `ExecutorServices.call?: (botId, flowId) => NonNullable<NodeCtx['call']>` + `buildCtx` resolves it to `ctx.call` or null (mirrors the `ai`/`files` factories; core never imports a driver, I3). **Host (`apps/server`):** NEW `triggers/call-session.ts` — `CallSessionService`, a long-lived service (a SIBLING to the Scheduler) that owns every live call; `capabilityFor(botId, flowId)` returns the per-run `ctx.call`; it resolves the `voiceConnection` credential **fail-closed** (via `makeVoiceCredentialResolver` over the host decrypt — the session stays host-side, I6/I7), drives the pluggable `VoiceConnector`, and holds per-call state: participants, the lineup **turn queue** (sequential/random `grantTurn`, `endTurn`, explicit-user line-jump, optional `maxTurnSeconds` auto-advance), and `status`. **Hard caps = config + safe defaults (`DEFAULT_CALL_CAPS`, I4):** `maxConcurrentCalls` (host-wide) + `maxCallsPerBot` + `maxCallSeconds` (auto-leave) — `connect` fails *loudly* past any cap (checked BEFORE resolving the credential or dialling). Inbound utterances are tracked, enqueue a fresh speaker in lineup, and fan out via `onUtterance` (the seam PE-T3's `trigger.callEvent` subscribes to). NEW `engine/loopback-connector.ts` — the DEPENDENCY-FREE default `LoopbackVoiceConnector` (no MTProto): `connect`/`leave` idempotent, `speak` optionally echoes back as an utterance, `emitUtterance` lets tests drive inbound audio — so the whole service is testable without a native engine and the userbot adapter swaps in with a one-line `wire.ts` change (I3). **Wiring (`wire.ts`):** builds the service (default loopback unless `WireOptions.voiceConnector`; caps via `WireOptions.callCaps`), exposes it on `Engine.callSessionService`, and adds the `call` factory to `ExecutorServices`; `main.ts` shutdown calls `callSessionService.stop()` (leaves open calls). **Both user scenarios are settings, not forks:** channel/group live broadcast = `mode:'lineup'` + a Q&A queue; 1:1 Direct→AI call = `mode:'support'` + `target.kind:'user'`. **Tests:** NEW `apps/server/test/call-session.test.ts` (24 — connect/leave idempotency + fail-closed, host/per-bot/duration caps, support vs lineup turn queue (sequential/random/explicit-jump/auto-advance), speak pcm/audio/fileId + missing-store/empty errors, mute, the utterance fan-out tagged with bot/flow/mode, and the loopback echo). **Verify:** all 6 packages typecheck GREEN; tests shared 73 / core 50 / nodes 509 / sandbox 16 / server 429 GREEN. Additive — no node/registry change. Next: PE-T3 (`trigger.callEvent`).
Task-prevD4: PD-T4 **Node library docs site — a browsable, bilingual (fa/en) `/docs` page auto-generated from the node CATALOG.** No node is hardcoded: a pure, DOM-free transform (`apps/editor/src/pages/node-docs/model.ts`) turns the `NodeTypeInfo[]` from `GET /api/node-types` into a grouped docs structure. `buildDocs` groups by category in palette order (`trigger`/`telegram`/`flow`/`data`/`ai`; an unknown future category is appended alphabetically so it can never silently vanish), sorts nodes by `type`, and `toDocNode` projects each node: **params** via the form engine's own `objectFields` (so the documented param shape can't drift from what the form collects/the server re-validates, I5) → per-param `{key, required, widget, typeSummary, defaultText, description}` (`summarizeType` renders enums as "one of: …", unions de-duplicated, arrays as "list of …", `z.unknown()` as "any"); **ports** (in/out, dynamic-out flagged); **typed-connection facts** (role / input-slot `kind`s / provides, PB-T1); and an `isTrigger` flag (no inputs). **Page (`apps/editor/src/pages/NodeDocsPage.tsx`, route `/docs`, nav "Node Library"):** fetches the catalog once via `api.listNodeTypes()`, builds + renders category sections of node cards (label/description resolved from the `meta` i18n keys so it's bilingual for free), with a free-text search (`filterDocs` over type/label/description/param keys, locale-aware via the passed `t`). **Wiring:** `/docs` route + `nav.docs` link added to `App.tsx`. **i18n:** `nav.docs` + the `docs.*` family added to BOTH `en.ts`/`fa.ts` (parity). **CSS:** a `.docs-page` block appended to `styles.css` (category-coloured headers reuse the `--node-*` palette vars; responsive node-card grid; param table). **Tests:** NEW `apps/editor/test/node-docs-model.test.ts` (21 — `summarizeType`/`defaultText`/`paramsOf`/`toDocNode`/`buildDocs` ordering+unknown-category/`filterDocs` by type+label+param/empty, plus a build against the live fake `GET /api/node-types`). **Verify:** editor typecheck GREEN; node-docs-model 21/21 + i18n-namespace 8/8 + api-client 10/10 + form-engine 24/24 + param-panel 3/3 + canvas-graph 19/19 + stores 11/11 GREEN; `npm run build --workspace @ctb/editor` GREEN (359 modules). Docs-/editor-only, additive. **Phase D is now complete; next is Phase E (live voice AI), pending the user's E.3 sign-off.**
Task-prevC5: PC-T5 **Builder docs + recipes — the open builder surface, documented + demoed end-to-end.** Extended PROTOCOL.md with an **"Authoring & MCP"** chapter (inserted after §MCP server): a *discover → build → validate → activate → trigger* lifecycle diagram, the two safety invariants the automation relies on (I5 — the catalog is the single source of truth + a v1-authored flow is byte-identical to an editor-built one; I2 — no domain nodes exist to assemble), **Recipe A** (a copy-paste `curl` script that reads `GET /api/v1/node-types`, `POST`s a 3-node graph, `/validate`s, `/activate`s, `/trigger`s, and polls `GET /api/v1/executions`), **Recipe B** (an AI agent walking the same lifecycle over MCP — `list_nodes`/`validate_flow`/`create_flow`/`trigger_flow`, self-correcting on validate before committing), and a "which surface for which job" quick-reference; the intro surface-table now lists discover/author/validate/activate as REST powers + points at the new chapter. **🎬 Demo:** `apps/server/test/e2e-phaseC-authoring-demo.test.ts` (2 tests, on the REAL wired engine + in-memory SQLite + fake Telegram transport, harness mirrored from `api-v1.test.ts`/`e2e-phase4-n8n-demo.test.ts`): **Test 1** — an external (bot-scoped) agent reads the catalog and FINDS `flow.manualTrigger`/`data.setFields`/`tg.sendMessage` (asserts `category`/`ports`/`paramsJsonSchema`), assembles the 3-node greeting flow, `validate`s (ok:true, still draft), `activate`s (→active), `trigger`s (`chat_id:555`), polls `GET /api/v1/executions?flow_id=` until `done`, and asserts the bot SENT `Hello from an external agent 👋` to chat 555 (the `{{ $json.greeting }}` expression resolved against the setFields output, through the live sender); **Test 2 (safety rail)** — a graph using a non-catalog type `shop.checkout` fails `validate` (ok:false + problems) and `activate` (`422 not_activatable`), staying a draft. **Walkthrough:** `docs/demos/phase-C-authoring.md` (mirrors `phase-B-voice.md`). **Additive — no node/registry/schema change** (a docs + test task). **Verify GREEN:** typecheck 6 pkgs; full suite shared 73 / core 50 / nodes 509 / sandbox 16 / server **342** (was 340, +2 demo) / editor 143. **Phase C COMPLETE.**
Task-prevC4: PC-T3 **CTB as an MCP _server_ — `POST /api/v1/mcp` (JSON-RPC 2.0, streamable-HTTP)** — an external AI agent (Claude Desktop, an IDE, another orchestrator) can now DISCOVER the node library and BUILD/RUN flows over MCP, the inverse of `ai.mcpClient` (P5-T3). **Design choice:** the protocol is implemented NATIVELY (no MCP SDK dependency — MCP is JSON-RPC 2.0 on the wire) so it lives INSIDE the existing bearer-auth `/api/v1` scope and REUSES the exact same engine handles + shared schemas the REST routes use (I5 — a flow built over MCP is byte-identical to one built over REST/editor; I3 — `mcp.ts` imports no driver). **New files:** `apps/server/src/api/mcp.ts` (the JSON-RPC handler + the six tools) + `apps/server/src/api/mcp-deps.ts` (the deps interface, split out to avoid a v1↔mcp import cycle). **Shared (`packages/shared/src/api.ts`):** MCP envelope types/schemas — `MCP_PROTOCOL_VERSION` (`2025-06-18`), `MCP_SERVER_INFO`, `JsonRpcRequestSchema`/`JsonRpcResponse`/`JsonRpcId`, `JSON_RPC_ERRORS`, `McpToolCallParamsSchema`, `MCP_TOOL_NAMES`. **Methods:** `initialize` (→ protocolVersion + `serverInfo {name:"ctb"}` + tools capability + instructions), `notifications/*` (any id-less message → `202` ack, no body), `ping` (→ `{}`), `tools/list` (→ 6 tools, each with a JSON-Schema `inputSchema` built via `z.toJSONSchema` from the SHARED Zod schemas), `tools/call` (→ an MCP result with a single JSON `content` text block). **Six tools, each bot-scoped via the per-request token:** `list_nodes` (= the PC-T1 catalog projection, bot-agnostic), `validate_flow {graph}` (dry-run `validateFlowForActivation` → `{ok,problems,nodeProblems}`), `create_flow {bot_id,name,graph?}` (DRAFT only; `bot_id`→`botId` alias; `unknown_bot`), `trigger_flow {flow_id,chat_id?,payload?}` (async run, item `source:"mcp"`; `flow_not_found`/`no_trigger_node`), `query_collection {bot_id,collection,filter?}` (reads through the engine `collectionStore.getBySlug`+`find`; `collections_not_available`/`collection_not_found`), `send_message {bot_id,chat_id,text,parse_mode?}` (centralized sender; `bot_not_running`). **Error model:** tool-level problems (unknown bot/not-found/bad-args/bot-scope) set `isError:true` on the result (the call SUCCEEDED, the tool reports a failure); protocol problems (malformed JSON-RPC / unknown method / unknown tool) return a JSON-RPC `error` (`-32600`/`-32601`/`-32602`/`-32603`). **Wiring:** `v1.ts` mounts `registerMcpApi` inside its bearer-auth scope (passing a per-request `tokenBotId(req)` reader + `deps.collectionStore`); `app.ts` passes `opts.engine.collectionStore` to v1. No scheduler reconcile is needed (no MCP tool toggles the ACTIVE set — create makes a draft, trigger runs an existing flow). **Docs:** PROTOCOL.md gained a "§MCP server — CTB as a tool surface" chapter (methods + tools + a Claude-Desktop quick-start) + the route-list line + the top capability row; PLAN2.md PC-T3 marked ✅. **Tests (`apps/server/test/mcp.test.ts`, +23):** a sqlite-wired World (so `query_collection` is live); auth (missing/garbage → 401), `initialize`/`ping`/notification-202/bad-JSON-RPC/unknown-method, `tools/list` (exactly the 6, each object schema), and `tools/call` for every tool incl. happy + error paths (list_nodes byte-equal to the REST catalog, validate good/no-trigger/malformed, create persists a draft the REST `/validate` then reads, trigger spawns a real execution row, send invokes the fake sender, query reads + where-filters records), an unknown-tool `-32602`, and a bot-scoped-token sweep (create/trigger/query/send all `token_not_authorized_for_bot` on another bot; list_nodes still works). **Verify:** all 6 packages typecheck GREEN (run package-by-package — the slow post-reset sandbox times out on a single whole-repo `npm run verify`); `@ctb/server` suite GREEN **340/340** (was 317; +23). Additive (no node/registry/i18n touch → no guard bumps); other packages unchanged.
Task-prevC2: PC-T2 **Flow authoring API — `POST/PATCH /api/v1/flows` + dry-run validate** — an external agent can now BUILD and ACTIVATE a flow, not just trigger one. **Server (`apps/server/src/api/v1.ts`):** five new routes inside the existing bearer-auth `scope`, all reusing the SAME shared contracts as the panel's flows API (I5 — a v1-authored flow is byte-identical to an editor-built one): `POST /api/v1/flows` (create draft; accepts snake_case `bot_id` as a `botId` alias; `CreateFlowBodySchema` → `graph` defaults empty; `400 unknown_bot`), `PATCH /api/v1/flows/:id` (`UpdateFlowBodySchema`; a graph change snapshots the outgoing version into `flow_versions` + bumps `version`; same-bot error-handler rule `error_handler_self`/`error_handler_not_same_bot`; editing an ACTIVE flow's graph fires `onFlowsChanged`), `POST /api/v1/flows/:id/validate` (DRY-RUN — `validateFlowForActivation(graph, paramSchemas, nodeMeta)` → `{ ok, problems, nodeProblems }`, NOTHING saved), `POST /api/v1/flows/:id/activate` (same validation → `422 not_activatable` with problems else `status:'active'` + reconcile), `POST /api/v1/flows/:id/deactivate`. `paramSchemas`+`nodeMeta` are built ONCE from the registry (mirrors flows.ts). Bot-scoped tokens 403 on another bot's flow (via the existing `tokenAllowsBot`). **App wiring (`app.ts`):** `V1ApiDeps.onFlowsChanged` added → `scheduler.reconcile()` (I3 — v1 never imports the Scheduler). **Docs:** PROTOCOL.md §Inbound REST API gained a "Flow authoring (PC-T2)" subsection + the route list; PLAN2.md PC-T2 marked ✅. **Tests (`apps/server/test/api-v1.test.ts`, +13):** create (+snake_case alias, +empty-graph default, +unknown_bot), PATCH (version bump + snapshot, 404), validate (good→ok:true & still draft; no-trigger→ok:false+problem), activate (good→active; no-trigger→422 stays draft), deactivate, and a bot-scoped 403 sweep over create/patch/validate/activate/deactivate. **Verify:** `@ctb/server` suite GREEN (317/317, was 304); all 6 packages typecheck GREEN (run package-by-package — the sandbox is slow post-reset so a single `npm run verify` times out, but every workspace passed individually). Additive (no node/registry change → no guard bumps).
Task-prevC1: PC-T1 **Node catalog API — `GET /api/v1/node-types`** — promoted the panel's internal `/api/node-types` projection to the public bearer-auth v1 surface so an external builder/AI agent can DISCOVER the node library without the panel cookie. `apps/server/src/api/v1.ts` imports `nodeTypeInfos` and serves a once-computed `{ nodeTypes }` const inside the bearer-auth scope (SAME projection the editor gets → can never advertise an unrunnable node); each entry carries `type`/`category`/`meta`/`ports`/`paramsJsonSchema` + the PB-T1 typed sub-connection surface (`role`/`inputSlots`/`provides`). Any valid token reads it (bot-agnostic library — no bot gate). Tests +6 in `api-v1.test.ts` (auth 401×2, registry deep-equal, AI typed-slot surface, bot-scoped read, byte-identical to internal). PROTOCOL.md + PLAN2.md updated. (PR #54, MERGED → `origin/main` `d816b33`.) **Also at that session start:** restored the missing `better-sqlite3` native binding via `npm rebuild better-sqlite3` (env-only) so the `@ctb/server` suite ran again.
Task-prevB8: PB-T8 **🎬 Phase-B voice demo — the screenshot rebuilt end-to-end** — a 9-node flow proving the whole Phase-B AI tier in one run from ONLY shipped nodes wired by the typed-slot contract: `tg.trigger` → `ai.speechToText` (owns the `ctx.tg.getFile` voice download → `ctx.ai.transcribe`) → `ai.agent` consuming four PROVIDERS via typed slots (`ai.modelOpenai`/`ai:model`, `ai.memoryPostgres`/`ai:memory`, `tool.think`+`tool.httpRequest`/`ai:tool`×2) → `ai.textToSpeech` (`ctx.ai.speech` → `ctx.files.write`) → `tg.sendMedia` (`source:'file'`, `kind:'audio'`). Fixture `packages/shared/test/fixtures/phase-b-voice-flow.json`; scripted e2e `apps/server/test/e2e-phaseB-voice-demo.test.ts` (2 tests on the REAL Executor + slot resolver + agent loop with fake transports: the full hear→reason→tool-call→speak loop incl. the real `ctx.http.request`, and cross-call Postgres memory replay); walkthrough `docs/demos/phase-B-voice.md`. Additive — no node/registry change. **Phase B COMPLETE.** (PR #53, MERGED → `origin/main` `1b10073`.)

Task-prevB7: PB-T7 **Speech nodes — `ai.speechToText` + `ai.textToSpeech`** — the transcribe + TTS legs of the voice flow (the PLAN2 screenshot), as plain DATA nodes (not providers). **Shared (I5):** NEW `AiSpeechToTextParamsSchema` (credentialId `openAiApi` / model default `whisper-1` / source `telegram|file` / audio_source / optional language+prompt / save_as default `transcript`) and `AiTextToSpeechParamsSchema` (credentialId / model default `tts-1` / text / voice default `alloy` / format `mp3|opus|aac|flac|wav|pcm` / optional speed / save_as default `speech`) in `node-params.ts`; NEW host-capability types `AiTranscribeRequest`/`AiTranscribeResult` + `AiSpeechRequest`/`AiSpeechResult` and two NEW optional `ctx.ai` methods `transcribe?()` / `speech?()` in `node-def.ts` (nullable — fail-loud when an older host doesn't wire them, I6). **Nodes:** `ai/speech-to-text.ts` (locates audio via `ctx.tg.getFile` for `source:telegram` or `ctx.files.read` for `source:file`, derives a filename, calls `ctx.ai.transcribe`, merges `{text,language?,duration?}` under save_as) + `ai/text-to-speech.ts` (calls `ctx.ai.speech`, stores bytes via `ctx.files.write`, merges `{fileId,mime,size,url}` under save_as so `tg.sendMedia` `source:file` can send it); both run ONCE per node run, fail loud on absent capability / empty audio / download / provider errors. Registered in `index.ts` (registry 51→53). **Server (I3):** `engine/wire.ts` `makeAi` refactored to share a `resolveOpenAi(credentialId)` helper and gained `transcribe` (multipart `POST /audio/transcriptions`, `response_format:verbose_json` → `parseTranscription`) + `speech` (`POST /audio/speech` → raw audio bytes + `speechMime(format)`); the decrypted key stays host-side (I7). **Editor:** fa/en i18n (parity compile-enforced) — `nodes.ai.{speechToText,textToSpeech}.label` + `nodeDesc.ai.{…}` + the full `param/paramDesc.ai.{speechToText,textToSpeech}.*` families. **Docs:** NODES.md §AI gained a "Speech nodes" subsection. **Tests:** NEW `ai-speech-nodes.test.ts` (20 — registration/contract, telegram-download + file-store transcribe, language/prompt forwarding, save_as + multi-item fan-out, and fail-loud for absent ai / absent transcribe / absent speech / no file store / download error / provider error / empty audio); node-harness gained `transcribe`/`speech` mocks + `transcribeResult`/`speechResult`/`dropTranscribe`/`dropSpeech` overrides + `transcribeCalls`/`speechCalls` recorders; all 15 registry-count guards bumped 51→53. **Verify:** per-workspace GREEN — shared 73, core 50, nodes 504 (was 484: +20), editor 143 (i18n parity), server 296.
Task-prevB6: PB-T6 **AI Agent tool nodes — `tool.httpRequest` / `tool.code` / `tool.think` / `tool.subflow`** — the dedicated *provider* sub-nodes (`role:'provider'`, `provides:'ai:tool'`, no data ports, single dashed `provider` wire) that fill the PB-T5 agent's repeatable `ai:tool` slot. Each node's `tool_name`+`description` drive the model's tool-selection; its `params` rows become the tool's JSON-Schema arguments. **Shared (I5):** NEW `ToolNameSchema`, `ToolParamRowSchema`/`ToolParamRow` (name/type/description/required), and the four param schemas `ToolHttpRequestParamsSchema` (method/url/credentialId/headers/body_type/body/timeout/params), `ToolCodeParamsSchema` (code raw + timeout + params), `ToolThinkParamsSchema` (just tool_name/description), `ToolSubflowParamsSchema` (flow_id/tool_name/description/params) in `node-params.ts`. **Nodes:** four new files `ai/tool-http-request.ts` / `ai/tool-code.ts` (rawParamKeys `code`) / `ai/tool-think.ts` / `ai/tool-subflow.ts` (all fail-loud `execute()`); `ai/agent.ts` gained `resolveSlotTools` + `buildSlotTool` + per-type builders (`buildHttpTool` merges model args into query/JSON body via `ctx.http.request`; `buildCodeTool` runs `ctx.code.run` with args as `$json`, 10s cap; `buildThinkTool` echoes the `thought`; `buildSubflowTool` runs `ctx.subflow.run`) + `buildToolParameters` (rows→JSON Schema, no rows→open object) + dedupe (first wins); `toolSourcesFromSlots` now SKIPS `tool.*` types (handled directly) and keeps the forward-compat mcp/subflow-shape path. Registered all four in `index.ts` (registry 47→51). **Editor:** fa/en i18n (parity compile-enforced) — `nodes.tool.*.label` + `nodeDesc.tool.*` + the full `param/paramDesc.tool.{httpRequest,code,think,subflow}.*` families. **Docs:** NODES.md §AI gained an "AI Agent tool nodes" subsection + the `ai:tool` slot note updated. **Tests:** NEW `ai-tool-nodes.test.ts` (21 — registration/contract for all four; fail-loud execute; param-schema defaults+validation; END-TO-END runners through the real `ai.agent`: http GET-as-query + POST-as-body+credential headers, code sandbox add, think echo, subflow run+name-derivation, multi-tool advertise + duplicate-name dedupe); server `node-types.test.ts` back-compat guard extended for the four providers + a new positive assertion; all 15 registry-count guards bumped 47→51. **Verify:** per-workspace GREEN (shared/core/nodes/editor/server typecheck; nodes 484 / editor 143 / server 295). Earlier this Phase: PB-T5 `ai.agent` slots + `ai.modelOpenai` (PR #48); PB-T4 chat-memory providers (PR #47, merged); see Phase-B task history below.
Branch    : genspark_ai_developer
Blockers  : none
```

## Phase-B task history (most recent first)

Task-prevB5: PB-T5 **`ai.agent` orchestrator with slots + `ai.modelOpenai` provider** — evolved the P5-T4 tool loop into a Phase-B **consumer** declaring `inputSlots` (`ai:model` required-shape / `ai:memory` optional / `ai:tool` repeatable) and consuming them via the NEW `ctx.slots: AttachedProviders` resolved by the executor's `resolveSlots` (walks `graph.edges` whose `to.port` is a slot kind, validates the `role:'provider'` source's params, groups in graph order). NEW `ai.modelOpenai` provider fills the required model slot. Shared: `NodeCtx.slots`/`AttachedProviders`/`AttachedProvider` + `AiModelOpenaiParamsSchema`; `AiAgentParamsSchema.credentialId` relaxed to `.default('')`. Core: `resolveSlots` + thread `graph` + `buildCtx` slots param (default `{}`). Nodes: `ai/model-openai.ts` (fail-loud); `ai/agent.ts` resolves model (inline fallback, `no model:` fail), wires the PB-T4 memory runtime via the `ai:memory` slot, merges `ai:tool` providers with inline tools. Registry 46→47. Editor fa/en i18n. Tests: `ai-model-openai.test.ts` + agent slot tests + server `node-types.test.ts` + guards 46→47. (PR #48, OPEN — superseded by PB-T6 which stacks on top.)

Task-prevB4: PB-T4 **chat-memory providers — `ai.memoryKv` (default) + `ai.memoryPostgres`** — Phase-B's first concrete *provider* sub-nodes (`role:'provider'`, `provides:'ai:memory'`), the n8n "Chat Memory" nodes kept generic (I2). They attach to an AI Agent's `ai:memory` slot and give it a rolling conversation memory; the Agent consumes them in PB-T5 (now done). A provider is **never executed as a data step** — its params ARE its contract: the consumer resolves them into a `ChatMemoryConfig` and drives the shared runtime. NEW `chat-memory.ts` (I5): `ChatMemoryConfig` (discriminated `kv`|`postgres`), `loadChatHistory()`/`appendChatTurn()` over injected `ctx.kv`/`ctx.db` only (I3/I6 — no driver import, no decrypted secret); `kvMemoryKey()`, `quotePgIdent()`, `toAiMessages()`. SQL values ALWAYS bound; KV stores the trimmed window as one JSON value under `__ai_mem__:<session>` (scope `user`). Plus `AiMemoryKvParamsSchema` + `AiMemoryPostgresParamsSchema`; both nodes (`ai/memory-kv.ts` + `ai/memory-postgres.ts`) registered (registry 44→46). Editor fa/en i18n. Tests cover the provider contract + the runtime over the harness's `ctx.kv`/recording `ctx.db`. (PR #47, MERGED.)

Task-prevB3: PB-T3 **`db.mysql` — generic MySQL / MariaDB database node** — Phase-B's second DB connector, the exact mirror of `db.postgres` (n8n "MySQL" node, generic infrastructure not domain — I2). Same `ctx.db` capability contract; the `mysql2` driver + POOL live ONLY in `apps/server` (I3 — `core`/`nodes` never import a DB driver). Connection comes from a NEW `mysql` credential (host/port[default 3306]/database/user/password/ssl, AES-256-GCM at rest — I7; node sees only a `credentialId`). **Same operations** as db.postgres (`query`/`select`/`insert`/`update`/`delete`, `return_mode`, `confirm_many`, `save_as`) with the **MySQL dialect:** placeholders are positional `?` (not `$1,$2,…`); identifiers are backtick-quoted (`` `col` ``, not `"col"`); there is **NO `RETURNING *`** — a write returns the driver's OK packet which the host normalizes to a synthetic row `{affectedRows, insertId?}` so `rows`/`single` still behave. Values ALWAYS bound, identifiers validated against `/^[A-Za-z_][A-Za-z0-9_]*$/`. Runs ONCE per node run; fails LOUDLY when `ctx.db` is null. **Shared (I5):** `MysqlSchema` added to `CredentialDataSchema` discriminatedUnion (+`'mysql'` enum/labels/hint/authHeaders kept exhaustive); `DbQueryRequest` gained `dialect?: 'postgres'|'mysql'` (defaults `postgres` → PB-T2 node unchanged); `DbMysqlParamsSchema`/`DbMysqlParams` (mirror of DbPostgres, `credentialType:'mysql'`) reusing `DbOperationSchema`/`DbWhereRowSchema`/`DbValueRowSchema`. **Nodes:** `packages/nodes/src/db/mysql.ts` (backtick `quoteIdent`, `?` placeholders, no RETURNING, sends `dialect:'mysql'`) + registered in `index.ts`. **Server (`wire.ts`):** `makeDb()` made dialect-aware (4-arg: db, key, pgFactory, mysqlFactory) — `resolvePool(credentialId, dialect)` validates `data.type !== dialect` (throws "is not a {MySQL|Postgres} credential") and routes to the right factory, cached per credentialId; `mysqlPoolFactory` lazily `createRequire(import.meta.url)`-loads `mysql2` (ESM — bare require absent), wraps the callback pool via `.promise()`, and normalizes a write OK packet → `{affectedRows, insertId?}` row; INJECTABLE via `WireOptions.mysqlPoolFactory` so tests supply a fake. New runtime dep `mysql2 ^3.15.4` (3.22.5 installed; ships its own types — no @types). **Editor:** `CredentialsPage` gained a `mysql` credential form (host/port/database/user/password/ssl); the `credentialRef` widget already filters by `credentialType` so no widget change; fa/en i18n (parity compile-enforced; `credentials.type.mysql` + `credentials.field.my*` + `nodes.db.mysql.label` + `nodeDesc.db.mysql` + all `param.db.mysql.*`/`paramDesc.db.mysql.*`/`option.db.mysql.*`). NODES.md §Database connectors §MySQL filled in. Registry **43→44** types (all 12 registry-count guards incl. db-postgres bumped 43→44). **Tests (+36):** nodes `db-mysql.test.ts` (31 — registry-44; parameterized query/select/insert/update/delete with `?` + backtick idents + NO RETURNING; `dialect:'mysql'` asserted; identifier-safety rejects hostile/backtick-bearing table+column while keeping the value bound; confirm_many guard; return_mode single merge; runs-once; ctx.db-null fail; transport-error surfaces) + server `db-mysql.test.ts` (5 e2e through the wired engine w/ an injected fake `mysqlPoolFactory` — routes to the mysql factory not pg, builds pool from decrypted credential, backtick SELECT cached per credentialId, write OK-packet row surfaces, `closeDbPools` drains all, non-mysql credential rejected with "not a MySQL credential"; password never leaks into the response). **`npm run verify` GREEN** (typecheck all 6 workspaces; shared 73 / core 50 / sandbox 16 / nodes 430 / server 292 / editor 143 incl. i18n parity). NOTE: this task survived a mid-task sandbox reset — recovered from a tar backup; the untracked `mysql.ts` + both test files were re-created from session memory and re-verified green.

Task-prevB2: PB-T2 **`db.postgres` — generic Postgres database node** — Phase-B's first DB connector (n8n "Postgres" node, kept generic infrastructure not domain — I2). The node speaks SQL through an injected `ctx.db` capability; the `pg` driver + connection POOL live ONLY in `apps/server` (I3 — `core`/`nodes` never import a DB driver). Connection comes from a NEW `postgres` credential (host/port/database/user/password/ssl, AES-256-GCM at rest — I7; node sees only a `credentialId`, host resolves id→secret). **Operations:** `query` (arbitrary parameterized SQL — values ALWAYS bound via `$1,$2,…`, NEVER string-concatenated), plus `select`/`insert`/`update`/`delete` helpers that build safe statements (identifiers validated against `/^[A-Za-z_][A-Za-z0-9_]*$/` then double-quoted by `quoteIdent`; every value bound through `buildWhere`/`buildStatement` placeholder threading). `return_mode` rows→one output item per row, single→`{rows,rowCount}` merged under `save_as`. `confirm_many` guards a multi-row UPDATE/DELETE (a single-equality WHERE is allowed without it). Runs ONCE per node run; fails LOUDLY when `ctx.db` is null. **Shared (I5):** `PostgresSchema` added to `CredentialDataSchema` discriminatedUnion (+enum/labels/hint/authHeaders kept exhaustive); `NodeCtx.db` capability + `DbQueryRequest`/`DbQueryResult`; `DbWhereOpSchema`/`DbWhereRowSchema`/`DbValueRowSchema`/`DbOperationSchema`/`DbPostgresParamsSchema` in `node-params.ts`. **Core:** `ExecutorServices.db` + `buildCtx` wiring (nullable → node fails loudly when absent). **Server (`wire.ts`):** `makeDb()` owns a per-`credentialId` pool cache (decrypts the `postgres` credential, throws if the credential isn't `postgres`-typed, builds + caches a pool) + `closeAll()`; `pgPoolFactory` lazily `createRequire(import.meta.url)`-loads `pg` (apps/server is pure ESM — a bare `require` is absent at runtime) and is INJECTABLE (`WireOptions.dbPoolFactory`) so tests supply a fake pool; `Engine.closeDbPools()` drains every pool, called from `main.ts` shutdown. New runtime dep `pg ^8.21.0` (+ `@types/pg` devDep). **Editor:** `CredentialsPage` gained a `postgres` credential form (host/port/database/user/password/ssl); the `credentialRef` widget already filters by `credentialType` so no widget change was needed; fa/en i18n (parity compile-enforced; `credentials.type.postgres` + `credentials.field.pg*` + `nodes.db.postgres.label` + `nodeDesc.db.postgres` + all `param.db.postgres.*`/`paramDesc.db.postgres.*`/`option.db.postgres.*`). NODES.md gained a "Database connectors" section. Registry **42→43** types (all 11 registry-count guards bumped 42→43). **Tests (+34):** nodes `db-postgres.test.ts` (30 — registry-43; parameterized query/select/insert/update/delete; placeholder threading + in/is_null; identifier-safety rejects hostile table/column while keeping the value bound; confirm_many guard; return_mode single merge; runs-once; ctx.db-null fail; transport-error surfaces) + server `db-postgres.test.ts` (4 e2e through the wired engine w/ an injected fake `DbPoolFactory` — builds pool from decrypted credential, pool cached per credentialId, `closeDbPools` drains all, non-postgres credential rejected before any pool is built; password never leaks into the response). **`npm run verify` GREEN** (typecheck all 6 workspaces; nodes 399 / server 287 / editor 143 incl. i18n parity; core/shared/sandbox clean).

Task-prevB1: PB-T1 **typed sub-connection contract** — the Phase-B foundation that lets "provider" sub-nodes (Chat Model / Memory / Tool) ATTACH to a consumer's typed input SLOT, distinct from a data edge (the n8n "AI" canvas pattern), staying generic (I2). A sub-connection is an ordinary `FlowEdge` whose `to.port` IS the slot kind (`ai:model`|`ai:memory`|`ai:tool` — `PortName` allows `:`). **Shared (I5):** `SlotKindSchema`/`InputSlotSchema`(`{kind,required,repeatable}`)/`NodeRoleSchema`(`data`|`provider`) added to `node-def.ts`; `NodeDef` gains optional `role?`/`inputSlots?`/`provides?`; `NodeTypeInfo` (api.ts) mirrors them. **Validation (`flow-validate.ts`):** new `NodeSlotMeta` + `validateSubConnections` enforce — a slot edge must come from a provider of the SAME kind (`slot "<k>" must be fed by a <k> provider`), a provider attaches ONLY to a matching slot (`provider can only attach to a matching <k> slot`), a `required` slot must be connected, a non-`repeatable` slot accepts ≤1 provider, disabled nodes ignored, a provider never counts as a trigger; rules fire ONLY when an OPTIONAL 3rd `nodeMeta` arg is passed → Phase-A flows unaffected. **Server:** `node-types.ts` conditionally emits role/inputSlots/provides (OMITTED for plain data nodes → byte-identical payload); `flows.ts` builds a registry-derived `nodeMeta` map (omitted keys for `exactOptionalPropertyTypes`) and passes it to `validateFlowForActivation`. **Executor:** `isProviderNode` guard — a provider parked on the cursor is SKIPPED (emits nothing, ends the branch), never run as a data step. **Editor:** `canConnect` gained slot/role rules (`slotKindMismatch`/`providerNotAttachedToSlot`/`slotNotRepeatable`; duplicate-edge check runs FIRST so re-wiring the same edge is `duplicate`, not `slotNotRepeatable`); `flowToRfEdges(graph,selected,byType?)` renders slot edges DASHED (`ctb-slot-edge`, AI accent, kind label); `FlowCanvas` delegates `isValidConnection` to shared `canConnect`; `CtbNode` renders bottom-edge slot handles (`*`=required) + a provider output handle; `styles.css` adds slot handle/label/dashed-edge CSS; `fake-fetch.ts` mirrors the server `NODE_META` (I5 parity). **No behavior change to existing flows** — every new field optional; Phase-A nodes declare none. NODES.md gained a "Sub-connections — typed provider slots (PB-T1)" section. **Tests (+19):** shared `flow-validate.test.ts` +9 (well-wired agent / required-empty / wrong-kind / data-into-slot / provider-into-data / non-repeatable arity / repeatable tool / provider-not-a-trigger / disabled-provider-unfilled + a "without nodeMeta the rules are inert" back-compat test); editor `canvas-graph.test.ts` +8 (matching provider plugs in / wrong-kind / data-node / provider-into-data / non-repeatable rejects 2nd / repeatable accepts many / flowToRfEdges dashed slot edge / without-byType stays plain); server `node-types.test.ts` +2 (omits keys for data nodes / surfaces role+slots+provides when a node opts in); core `executor.test.ts` +1 (provider parked on cursor skipped, never executed). **`npm run verify` GREEN** (server 36 files/283 tests; editor 14 files/143 tests; full typecheck+lint clean). Test node-type gotcha: `NodeTypeSchema` is EXACTLY two dot-parts, so synthetic providers use `ai.modelOpenai`/`ai.memoryKv` (NOT `ai.model.openai`).

## What exists / what doesn't (Phase B)

| Area | Status |
|---|---|
| 🎬 Phase-A end-to-end demo (PA-T8) | ✅ DONE. PLAN2 PA-T8 — Phase A complete. A flow built from ONLY Phase-A nodes proves the conversational-domain data tier (full detail in docs/STATE.md Phase-A history). |
| Typed sub-connection contract (PB-T1) | ✅ DONE. PLAN2 PB-T1 + NODES.md §"Sub-connections — typed provider slots". **Phase B begins.** The foundation that lets provider sub-nodes attach to a consumer's typed input slot (slot edge = `FlowEdge` whose `to.port` is the slot kind). Every new field optional → Phase-A flows unaffected. |
| `db.postgres` node + `postgres` credential (PB-T2) | ✅ DONE. PLAN2 PB-T2 + NODES.md §Database connectors. **Phase B's first DB connector — a generic Postgres node** speaking SQL via injected `ctx.db`; `pg` driver/pool live only in apps/server (I3); values always bound (I-safety). |
| `db.mysql` node + `mysql` credential (PB-T3) | ✅ DONE. PLAN2 PB-T3 + NODES.md §Database connectors §MySQL. **Phase B's second DB connector — the MySQL/MariaDB mirror of db.postgres** (positional `?`, backtick idents, no RETURNING → host normalizes the OK packet); `mysql2` driver/pool only in apps/server. |
| chat-memory providers `ai.memoryKv` + `ai.memoryPostgres` (PB-T4) | ✅ DONE. PLAN2 PB-T4 + NODES.md §AI §"Chat memory providers". **Phase B's first concrete provider sub-nodes** (`role:'provider'`/`provides:'ai:memory'`); shared `chat-memory.ts` runtime over injected `ctx.kv`/`ctx.db`; registry 44→46. Consumed by the AI Agent via the `ai:memory` slot (PB-T5). |
| `ai.agent` orchestrator with slots + `ai.modelOpenai` (PB-T5) | ✅ DONE. PLAN2 PB-T5 + NODES.md §AI §"AI Agent (tools)" §"AI model providers". **Phase B's orchestrator wiring** — `ai.agent` now declares `inputSlots` and consumes the `ai:model` (required-shape, inline fallback) / `ai:memory` (optional, drives the PB-T4 runtime) / `ai:tool` (repeatable, merged with inline tools) slots via the NEW `ctx.slots: AttachedProviders` resolved by the executor's `resolveSlots`; NEW `ai.modelOpenai` provider (`role:'provider'`/`provides:'ai:model'`) fills the required model slot; registry 46→47. Back-compat: a slot-less agent still runs off its inline credential/model. |
| AI Agent tool nodes — `ai:tool` slot (PB-T6) | ✅ DONE. PLAN2 PB-T6 + NODES.md §AI §"AI Agent tool nodes". Four *provider* sub-nodes (`role:'provider'`/`provides:'ai:tool'`): `tool.httpRequest` (calls an API via `ctx.http`), `tool.code` (sandboxed JS via `ctx.code`), `tool.think` (no-op scratchpad), `tool.subflow` (a flow as a tool via `ctx.subflow` — the n8n "Workflow Tool"). The agent's `resolveSlotTools` builds runnable tools directly; `params` rows → JSON-Schema args; duplicate names dropped. Registry 47→51. |
| AI speech nodes — `ai.speechToText` + `ai.textToSpeech` (PB-T7) | ✅ DONE. PLAN2 PB-T7 + NODES.md §AI §"Speech nodes". Two plain DATA nodes: `ai.speechToText` (audio via `ctx.tg.getFile` or `ctx.files.read` → `ctx.ai.transcribe` → `{text,language?,duration?}`) and `ai.textToSpeech` (`ctx.ai.speech` → `ctx.files.write` → `{fileId,…}` for `tg.sendMedia`). NEW optional host caps `ctx.ai.transcribe`/`ctx.ai.speech` (`POST /audio/transcriptions` multipart + `/audio/speech`) wired in `engine/wire.ts`; the `openAiApi` key stays host-side (I7). Registry 51→53. |
| Phase-B voice demo (PB-T8) | ✅ DONE. PLAN2 PB-T8 + `docs/demos/phase-B-voice.md`. **Phase B COMPLETE.** The screenshot rebuilt end-to-end on the REAL engine: `tg.trigger` → `ai.speechToText` (owns the `ctx.tg.getFile` download) → `ai.agent` consuming four PROVIDERS via typed slots (`ai.modelOpenai` + `ai.memoryPostgres` + `tool.think` + `tool.httpRequest`) → `ai.textToSpeech` → `tg.sendMedia` (`source:'file'`, audio). Fixture `packages/shared/test/fixtures/phase-b-voice-flow.json`; scripted e2e `apps/server/test/e2e-phaseB-voice-demo.test.ts` (2 tests: the full hear→reason→tool-call→speak loop, and cross-call Postgres **memory replay**). Additive — no node/registry change. |
| Node catalog API — `GET /api/v1/node-types` (PC-T1) | ✅ DONE. PLAN2 PC-T1 + PROTOCOL.md §Inbound REST API. **Phase C begins — the open builder surface.** The panel's internal `/api/node-types` projection promoted to the public bearer-auth v1 surface (`apps/server/src/api/v1.ts` reuses `nodeTypeInfos(registry)`): an external builder/agent DISCOVERS every node's `type`/`category`/`meta`/`ports`/`paramsJsonSchema` + the typed sub-connection surface (`role`/`inputSlots`/`provides`). SAME projection as the engine registry (can never advertise an unrunnable node); any valid token reads it (bot-agnostic library). Tests +6 in `api-v1.test.ts`. (PR #54, MERGED.) |
| Flow authoring API — `POST/PATCH /api/v1/flows` + validate/activate (PC-T2) | ✅ DONE. PLAN2 PC-T2 + PROTOCOL.md §"Flow authoring". An external agent BUILDS + ACTIVATES a flow: `POST /api/v1/flows` (create draft, `bot_id` alias, empty-graph default), `PATCH /api/v1/flows/:id` (edit name/graph/settings; graph change snapshots version), `POST /…/validate` (DRY-RUN problems, nothing saved), `/activate` (422 with problems else active + reconcile), `/deactivate`. All reuse the SAME shared schemas + `validateFlowForActivation` as the panel (I5 — no surface drift); bot-scoped tokens 403 on another bot. `app.ts` wires `onFlowsChanged`→`scheduler.reconcile`. Tests +13 in `api-v1.test.ts`. |
| Builder docs + recipes (PC-T5) | ✅ DONE. PLAN2 PC-T5 + PROTOCOL.md §"Authoring & MCP" + `docs/demos/phase-C-authoring.md`. **Phase C COMPLETE.** The open builder surface documented + demoed: the *discover→build→validate→activate→trigger* lifecycle (diagram + I5/I2 safety invariants), a `curl` recipe + an MCP-agent recipe, and a "which surface for which job" quick-reference. 🎬 demo `apps/server/test/e2e-phaseC-authoring-demo.test.ts` (2 tests on the REAL engine + fake Telegram transport): an external agent reads `GET /api/v1/node-types`, assembles `flow.manualTrigger→data.setFields→tg.sendMessage`, validates/activates/triggers, polls `GET /api/v1/executions` to `done`, asserts the `{{ $json.greeting }}` message was sent; + a non-catalog-type (`shop.checkout`) safety-rail test. Additive — no node/registry/schema change. |
| MCP server — `POST /api/v1/mcp` (PC-T3) | ✅ DONE. PLAN2 PC-T3 + PROTOCOL.md §"MCP server". CTB speaks **MCP** (JSON-RPC 2.0 over streamable-HTTP) so an external AI agent discovers the node library and builds/runs flows — the inverse of `ai.mcpClient` (P5-T3). Implemented NATIVELY (no MCP SDK dep) inside the bearer-auth `/api/v1` scope, reusing the SAME engine handles + shared schemas as REST (I5; I3 — no driver import). Methods `initialize`/`ping`/`tools/list`/`tools/call` + notification-202. Six bot-scoped tools: `list_nodes` (= PC-T1 catalog), `validate_flow` (dry-run), `create_flow` (draft, `bot_id` alias), `trigger_flow` (async, `source:"mcp"`), `query_collection` (collection read+filter), `send_message` (centralized sender). Tool-level errors → `isError`; protocol errors → JSON-RPC `-326xx`. New `api/mcp.ts` + `api/mcp-deps.ts`; shared MCP envelope in `api.ts`; `v1.ts`+`app.ts` wire it + the `collectionStore`. Tests +23 in `mcp.test.ts`. |
| DB connection pooling + safety (PD-T1) | ✅ DONE. PLAN2 PD-T1 + NODES.md §Database connectors §"Connection hardening". **Phase D begins — hardening the new surface.** The `postgres`+`mysql` credentials gained `poolMax` (1–100, default 5), `statementTimeoutMs` (0–600000, default 30000) + `readOnly` (default false), all enforced HOST-SIDE in `wire.ts` (I3 — the node never sees them): pg gets `max` + a session `options` string (`statement_timeout`, and read-only → `default_transaction_read_only=on` so the SERVER refuses writes); mysql gets `connectionLimit` + a per-query `timeout`. Read-only is defence-in-depth — `DbQueryRequest.write?` (nodes send `operation !== 'select'`, fail-closed) lets `makeDb.query` refuse a write on a read-only pool before the driver. Editor Credentials form + fa/en i18n added the 3 fields. Injection guarantees unchanged (bound values, validated+quoted idents). Tests: NEW `db-hardening.test.ts` (17 — injection battery + pool/timeout pass-through + read-only enforcement, both dialects). |
| Agent cost governance (PD-T2) | ✅ DONE. PLAN2 PD-T2 + PROTOCOL/NODES. Per-bot **daily AI budget** (`maxCallsPerDay`/`maxTokensPerDay`/`maxTokensPerRun` in `bots.settings.aiBudget`) enforced **fail-closed BEFORE** each provider call + **metered AFTER** in an `ai_usage` ledger. `ctx.ai` became a per-run factory `(botId,flowId,executionId)` (`makeAiFactory`, `wire.ts`): reads the bot's budget + today's totals, refuses a call that would exceed the cap (`0`=unlimited), records one `ai_usage` row per successful chat (bot/flow/execution + credential + model). Panel: per-bot AI **budget** panel + **usage** view (`GET /api/bots/:id/ai-usage`, `PUT /api/bots/:id/ai-budget`); fa/en i18n. NEW `engine/ai-usage-store.ts` (`SqliteAiUsageStore`). Migration `0004`. Tests `ai-cost-governance.test.ts` (6). (PR #60, MERGED → `3750931`.) |
| MCP/API rate limiting + audit (PD-T3) | ✅ DONE. PLAN2 PD-T3 + PROTOCOL.md §"Rate limiting & audit". **Per-token rate limiting:** `api_tokens.rate_limit_per_min` (default 120, `0`=unlimited); the v1 bearer preHandler checks an in-memory sliding-window `RateLimiter` (process-local — single-process scope) and returns **429 + `Retry-After`** on breach, keyed per-token (before `last_used_at` is stamped; a rejected check records no hit). **Append-only audit:** NEW `api_audit` table + `SqliteApiAuditStore`; a v1 `onResponse` hook records one row per AUDITED authoring/trigger/send call (tokenId, **target** botId via `markAuditBot`, action, method, route, targetId, status, ts) — plain reads aren't audited, a 403/422 is logged like a 201/202, a 429 leaves no row. NEW `GET /api/v1/audit` (instance-wide sees all/filterable by bot+token+limit; bot-scoped locked to its own bot). `api-tokens.ts` carries `rateLimitPerMin`. Migration `0005`. Tests: NEW `api-rate-audit.test.ts` (25 — limiter+store unit + e2e 429/audit/scope). (PR #61, MERGED → `e4f0068`.) |
| `voiceConnection` credential (PE-T1) | ✅ DONE. PLAN2 PE-T1 + NODES.md §"Live-voice connection". **Phase E begins — live voice AI.** A NEW encrypted credential (I7) in the shared discriminated union with a `kind` setting (`userbot` shipped; `companion`/`external` forward-shaped) — the Bot API has NO call methods, so the audio always rides **MTProto via a USER session**; this credential carries it. Host `engine/voice-connector.ts` (I3): the `VoiceConnector` adapter interface (PE-T2 implements it behind ONE `ctx.call`) + `resolveVoiceConnection` (**fail-closed** — wrong type / a `kind` missing required fields throws a secret-free `VoiceConnectionError`) + `validateVoiceConnection` (leak-free health: no-adapter → "structurally valid, login not attempted"; wired-adapter surfaced verbatim; any throw → `{ok:false}`). `POST /api/credentials/:id/voice-health` (decrypts host-side, 404/409). `credentialHint`/`credentialAuthHeaders` extended exhaustively (hint = `kind · ••••tail`, never the secret; no HTTP headers — it resolves into a media engine). Editor Credentials form (`kind` picker → userbot/companion: api_id/api_hash/session; external: bridge URL/token) + fa/en i18n parity. Tests: NEW `voice-connector.test.ts` (15) + `api-credentials.test.ts` (+6). No audio yet — a verified, swappable connector. **Both user scenarios (group/channel live chat + 1:1 Direct call) are the SAME nodes via the `target` setting (PE-T3/T4).** |
| Node library docs site (PD-T4) | ✅ DONE. PLAN2 PD-T4. A browsable, bilingual (fa/en) **`/docs`** page (nav "Node Library") **auto-generated from the node catalog** (`GET /api/node-types`) — no node hardcoded. Pure transform `pages/node-docs/model.ts`: `buildDocs` groups the `NodeTypeInfo[]` by category (palette order; unknown category appended), sorts by `type`; `toDocNode` projects each node's **params** (via the form engine's `objectFields` so the shape can't drift, I5 — key/required/widget/`summarizeType`/`defaultText`/description), **ports** (in/out, dynamic flagged), **typed-connection facts** (role/slot kinds/provides), `isTrigger`. Page `pages/NodeDocsPage.tsx` fetches once, renders category sections + node cards (label/desc from `meta` i18n keys) + free-text search (`filterDocs`). `App.tsx` route+`nav.docs`; `docs.*` i18n in en/fa; `.docs-page` CSS. Tests: NEW `node-docs-model.test.ts` (21). Editor-only, additive. |
| Phase E — live voice AI (real-time Telegram calls) | 📐 DESIGNED (user request 2026-06-18; refined 2026-06-18 per the user's "choice belongs in the nodes, not the roadmap" principle). Full **Phase E** in PLAN2.md, now **settings-driven**: PE-T1 **`voiceConnection` credential** with a `kind` setting (`userbot` first; `companion`/`external` forward-shaped — covers the `@myidbot`-style helper-account idea) → PE-T2 host **Call Session Service** exposing ONE `ctx.call` interface with **pluggable connector adapters** (the JS-native-vs-Python-sidecar media engine is an INTERNAL, swappable choice, never exposed to flows; `target` setting handles group AND 1:1) → PE-T3 `trigger.callEvent` (events + `mode` `support`/`lineup` + `order` `sequential`/`random` are NODE SETTINGS) → PE-T4 `call.*` actions (`connect`/`speak`/`grantTurn`/`endTurn`/`mute`/`leave`) → PE-T5 demos+docs (both scenarios from the SAME nodes, differing only in settings). **Feasibility (researched):** Bot API has NO call methods — audio rides **MTProto via a user session (not a bot token)** + WebRTC (ntgcalls). **Key principle:** every transport/mode fork is a node/credential setting (I2 generic-only applied to voice), so building it narrows nothing; CTB picks the internal media adapter (§E.3) without constraining any flow. **Reuses PB-T5…PB-T7** directly. Sequenced last by default but can be pulled forward on request (the only genuine scheduling question). No PE-T* code until we actively start it. |

| Open PR | **A new PR will open on `genspark_ai_developer` → main for H-T4 (add-node-on-edge + wire-drop-to-palette — PLAN3 Phase H, COMPLETES Phase H)** (NEW pure `buildNode`/`insertNodeOnEdge`/`nodeFromDangling`/`PendingConnect` in `apps/editor/src/canvas/graph.ts` + `stores/canvas.ts` `insertNodeOnEdge`/`addNodeFromDangling` actions + NEW `canvas/CtbEdge.tsx` inline "+" + NEW `canvas/NodePicker.tsx` popup + `FlowCanvas.tsx` edgeTypes/onConnectStart/onConnectEnd/onPick wiring + `i18n/en.ts`+`i18n/fa.ts` `editor.edge.insert`/`editor.picker.title` + `styles.css` `.ctb-edge-add`/`.node-picker*` (+ latent `--panel`→`--bg-raised` fix), tests `apps/editor/test/canvas-graph.test.ts` +5 & `canvas-store.test.ts` +4, docs PLAN3 / STATE2). **NOTE:** this branch is currently STACKED on the still-open H-T3 PR #79 (sequential Phase-H work); once #79 merges, rebasing this on `origin/main` is clean (editor-only, no overlap). MERGED recently: PR #78 (H-T2 node rename, → `fef2508`), PR #77 (H-T1 sticky notes, → `d7ef9b9`), PR #76 (G-T3), PR #75 (G-T2), PR #74 (G-T1). Open: PR #79 (H-T3 canvas error surfacing). _Earlier line (now superseded):_ A new PR will open on `genspark_ai_developer` → main for PE-T1 (voiceConnection credential — Phase E begins) (`packages/shared/src/credentials.ts` + NEW `apps/server/src/engine/voice-connector.ts` + `apps/server/src/api/credentials.ts` voice-health route + `apps/editor/src/pages/CredentialsPage.tsx` + `i18n/en.ts`+`i18n/fa.ts`, NEW `apps/server/test/voice-connector.test.ts` + `api-credentials.test.ts` additions, NODES.md/PLAN2/STATE2). _Earlier line (now superseded):_ A new PR will open on `genspark_ai_developer` → main for PD-T4 (node-library docs site) (NEW `apps/editor/src/pages/node-docs/model.ts` + `apps/editor/src/pages/NodeDocsPage.tsx`, `App.tsx` route+nav, `i18n/en.ts`+`i18n/fa.ts`, `styles.css`, NEW `apps/editor/test/node-docs-model.test.ts`, PLAN2/STATE2). MERGED history: PR #47 (PB-T4…PB-T1), PR #48 (PB-T5, → `ae57418`), PR #49 (PB-T6, → `fa7c734`), PR #50 (PB-T7, → `1bfb030`), PR #51 (Phase-E design, → `5ad777c`), PR #52 (Phase-E settings-driven refinement, → `1018e72`), PR #53 (PB-T8 Phase-B voice demo, → `1b10073`), PR #54 (PC-T1 node catalog API, → `d816b33`), PR #55 (PC-T2 flow authoring API, MERGED), PR #56 (PC-T3 CTB-as-MCP-server, → `a1e4fc4`), PR #57 (PC-T4 tool.mcp, → `55bfaa8`), PR #58 (PC-T5 builder docs+recipes, → `ea36756`), PR #60 (PD-T2 agent cost governance, → `3750931`), PR #61 (PD-T3 MCP/API rate-limit+audit, → `e4f0068`). Full pre-Phase-B PR history (#46…#23) is in docs/STATE.md. |

## Session log (Phase B + Phase A, append-only, newest first)

> Pre-Phase-B session log (P6-T1/PA-T1 down to bootstrap) lives in docs/STATE.md.

| Date | Task(s) | Result / notes |
|---|---|---|
| 2026-06-27 | PLAN4 **J-T2** — editor "Test run" listens on the real trigger; the "use Manual trigger instead" alert is GONE (user Report B item 9) | **The user-facing half of J-T1.** On the user's instruction to finish J-T1, PR it, then immediately start the next step, I shipped J-T2 — the editor change that makes the J-T1 engine seam reachable. **Studied n8n's "listen for test event" UX once more:** the editor must NOT ask the user to swap triggers; the SAME `tg.trigger` should just listen for one live message in test mode. **DESIGN (the F-T3 pure-module pattern — EDITOR-ONLY, no schema/server/engine touch):** a NEW PURE, DOM-free `decideTestRunMode(graph)` in `apps/editor/src/lib/test-run.ts` returns `'manual' | 'listen' | 'none'` — `manual` when an enabled `flow.manualTrigger` exists (PRECEDENCE so the historical synchronous `/run` path is byte-identical; both-triggers → still manual), `listen` when there's NO manual trigger but an enabled `tg.trigger` (the flows that used to dead-end), `none` otherwise. **`TestRunButton` (`FlowEditorPage.tsx`)** decides over the LIVE canvas graph: `manual` → `api.runFlow` + load run-data (unchanged); `listen` → `api.testListen` (J-T1 `POST /test-listen`) then a `data-testid="test-listen-banner"` "Waiting for a message to your bot…" alert with a Cancel button, and a `useEffect` polls `api.testListenStatus` every 1.2s — on `captured` loads run-data so the NDV shows the REAL captured update, on `expired`/`gone` notifies; Cancel/unmount calls `api.testListenCancel` (`DELETE`) and aborts the poll; `none` → the broadened "add a trigger" hint (now naming BOTH trigger kinds — the old "use Manual instead" string is DELETED). Button disabled while a listen is armed. **Client:** `testListen`/`testListenStatus`/`testListenCancel` (POST/GET with url-encoded `executionId`/DELETE). **i18n:** NEW `editor.testRun.listen.waiting`/`.cancel`/`.expired` + broadened `editor.testRun.noTrigger`, en+fa parity. **Verify:** editor **271 GREEN** (+11: `test-run-mode.test.ts` 7 — manual/listen/none + manual-precedence + disabled-trigger fall-through; `test-listen-client.test.ts` 4 — verb/path/executionId-encoding via a fetch stub) + editor `tsc --noEmit` GREEN. Server/core/shared UNCHANGED (invariant I3) so they hold the J-T1 baseline (shared 100 / core 64 / server 466). Memory-aware run: stayed at `--max-old-space-size≤820` throughout (the ~1 GB sandbox OOMs above that), so the editor `tsc` + full editor vitest both completed cleanly. Next: J-T3 (trigger-node parity audit + NODES.md/demos + an arm→inject-one-update→assert-node-2 e2e). |
| 2026-06-27 | PLAN4 **J-T1** — listen for one live update (a real `tg.trigger` test run; **OPENS Phase J**) | **The "I can't even test a flow" blocker (user Report B): the editor's "Test run" was hard-wired to `flow.manualTrigger`, so a flow whose entry is a real Telegram Trigger could not be trialled — it forced the user to swap in a Manual trigger.** On the user's "ادامه" (and the standing instruction to study the n8n docs because the project's core pattern is inspired by them), I ran the session-start protocol (CLAUDE.md → STATE2 → PLAN4 J-T1 → ARCHITECTURE I3/I4/I5 + the executor `start()`/`runLoop`, the execution-state schema, the `triggerMatches` matcher, and the I-T1 `testRun` / I-T2 `stopAfterNode` precedents) and studied n8n's **"listen for test event"** model: a trigger listens for ONE live event in test mode, captures it, and the SAME node serves production — only the MODE differs. **DESIGN DECISION (ROADMAP Decision Log #25):** rather than add a new top-level `status:'listening'` (which would ripple through ~58 status-usage sites — every switch/guard/migration), I reused the durable pause/resume seam: a NEW optional `StartInput.listenMode` (`{ triggerParams, timeoutAt? }`) parks the run in a NEW `WaitSpec{kind:'trigger'}` + a persisted `ExecutionState.listening` marker, so the arming reuses the EXISTING `findWaiting`/`resume`/`triggerMatches` machinery verbatim — the same "reuse a seam, don't fork the engine" rationale as #21/#22. **Engine (`packages/core/src/engine/executor.ts`):** `start()` with `listenMode` does NOT execute the entry node — it stamps `testRun:true` (so I-T1 pins are honoured on capture) + `listening:true`, saves `status:'waiting'` with a `'trigger'` wait carrying the entry node's `triggerParams` snapshot + optional `timeoutAt`, and returns `{status:'waiting'}` (survives a process restart, I4). **Shared:** `'trigger'` `WaitSpec` kind + `ExecutionState.listening?` + `TestListenArmed`/`TestListenStatus`. **Store:** a bot-keyed `findListening(botId)` (a test-listen has no chat yet — it waits for the FIRST message — so the chat-keyed `findWaiting` can't see it) on the interface + MemoryExecutionStore + SqliteExecutionStore. **Router:** `handleSerialized` step (1.5) `tryResumeListen(event)` BEFORE `tryTriggers` resumes the FIRST armed match exactly-once on `main` with the real `triggerItem(event)`; a non-matching update doesn't consume the arming; `resumeTimedOut` DISARMS (cancels) an expired `'trigger'` wait. **Server:** `POST/GET-status/DELETE /api/flows/:id/test-listen` gated on a `tg.trigger` entry (422 `no_telegram_trigger`). Every new field is `.optional()` → a production run (no `listenMode`) persists byte-identically to pre-J-T1. **Tests:** core +4 (arming parks without executing the entry; next matching update resumes downstream; survives a fresh-Executor restart on the same store; production run byte-identical) / shared +4 (schema round-trips) / NEW `router-test-listen.test.ts` +5 (resume + sender data downstream; exactly-once; non-matching doesn't consume; survives restart; production unaffected) / NEW `api-flows-test-listen.test.ts` +5 (arm→listening→matching update→captured w/ sender id 555 reaching setFields; cancel→expired; 422 no-trigger; 404/400 guards; unknown id→gone). **Also fixed a pre-existing FLAKY test surfaced under the heavier suite: `e2e-phaseB-voice-demo` runs the REAL engine + sandbox but, unlike its sibling `e2e-phaseE-voice-demo`, never set the `ExecutorServices.evalOptions.budgetMs` host tuning seam — so under heavy parallel load the strict 50ms expression cold-start budget timed out (`expression exceeded 50ms budget: worker killed`), failing `transcribe`; I raised it to `5_000` to match Phase-E (behaviour test, not a latency test).** **Verify:** shared **100** / core **64** GREEN; FULL server suite **466 GREEN** (was 456 + my 10). Server `tsc` typecheck OOM/thrashes in this ~1 GB sandbox (the `--max-old-space-size=3072` from earlier sessions now exceeds total RAM and swaps — killed it, reset the sandbox twice to recover), so server type-safety is validated via the green vitest run (esbuild type-strips every modified file) + clean shared/core/nodes `tsc`. Next: J-T2 (editor "Test run" arms the live-listen, shows the captured update, removes the "use Manual trigger instead" alert). |
| 2026-06-23 | PLAN3 **I-T2** — execute a single node | **The “test fast” cure, gap G16 — sibling to I-T1's pin data: from the editor, run ONE node with its last-captured (or empty) input and see its output, WITHOUT running — or side-effecting — the whole flow.** Continuing on the user's “ادامه بده و بعد اتمام این استپ بعدی رو شروع کن” right after I-T1 (PR #80). Ran the session-start protocol (STATE2 → PLAN3 I-T2 → ARCHITECTURE I4/I5 + the executor `runLoop`/`start()`, the execution-state schema, and the I-T1 `testRun` precedent). **DESIGN (ROADMAP Decision Log #22):** a NEW optional `StartInput.stopAfterNode?: NodeId` mirrored onto a NEW PERSISTED `ExecutionState.stopAfterNode?`. **Engine (`packages/core/src/engine/executor.ts`):** `start()` stamps `stopAfterNode` on the state ONLY when set (omitted on every normal run → the persisted state stays byte-identical to pre-I-T2); in `runLoop`, AFTER the per-step I/O snapshot log (so the node's output IS captured for the editor) and BEFORE `switch(result.kind)`, if `state.stopAfterNode === node.id` AND the result is `items`/`goto`, the run is finalized `done` and ENDS — it NEVER routes downstream; a `wait`/`end`/`error` result terminates the step naturally, so a single-node run of a WAIT node correctly reports `waiting`. **Why the boundary is on the STATE not a transient arg (I4):** identical reasoning to I-T1's `testRun` — a single-node run that hits a WAIT must resume-and-stop correctly, which a runtime-only flag would lose on the first resume. **Shared:** `ExecutionStateSchema.stopAfterNode` (NodeId, optional) + NEW `RunNodeBodySchema` (`{ nodeId, input?: FlowItem[]≤200 }`) + `RunNodeBody` in `api.ts` (imports `NodeIdSchema` + `FlowItemSchema` as values). **Server (`apps/server/src/api/flows.ts`):** NEW `POST /api/flows/:id/run-node` — ALWAYS a TEST run (`testRun:true`, so pinned data from I-T1 is honoured) entering at the target node with `stopAfterNode` = that node; rejects a missing flow (404), unparsable graph (422), missing node (404 `node_not_found`), disabled node (422 `node_disabled`), bad body (400 `invalid_body`), no engine (503); the node's output is read back from `exec_logs`, not returned inline. **Editor:** `api.runNode(id, nodeId, input?)`; `NodeDetail.tsx` gains a “▶ Run this node” button that `saveNow()`s, runs ONLY this node with its last input (or one empty item) via the run-data store, then `refresh()`es the run-data overlay; i18n `editor.node.runNode.*` en/fa parity; CSS `.ndv-run-node`. **Tests:** core `executor.test.ts` +5 (runs only the target — steps=1, downstream var unset / the target executes via the FULL path — output captured in the log + “single-node run of X complete” / `stopAfterNode` persisted + chosen input reaches the node / single-node run of a WAIT node → `waiting` / a normal run with no `stopAfterNode` is unaffected); server NEW `api-flows-run-node.test.ts` (6 — output captured + no downstream `tg.sendMessage` call/`send` log row, empty item when no input, 404 node_not_found, 422 node_disabled, 400 invalid_body, 404 unknown flow); editor `api-client.test.ts` +1 (`runNode` sends nodeId+optional input to `/run-node`, unknown node → 404) via a NEW `/run-node` fake route + `runNodeCalls` in `fake-fetch.ts`. **Verify:** all 6 workspaces typecheck GREEN (`apps/server` with `--max-old-space-size=3072`; fixed two `noUncheckedIndexedAccess` `output!.main![0]!` accesses in the new tests); full suites GREEN — shared 96 / core 60 / nodes 535 / editor 240 / server 456. All optional/no-default → every existing flow + persisted execution state parses byte-identically. Additive — no node/registry change. Then immediately starting I-T3 (keyboard shortcut help overlay + coverage sweep, gap G11) per the user's standing “بعد اتمام این استپ بعدی رو شروع کن”. |
| 2026-06-23 | PLAN3 **I-T1** — pin data on a node (**OPENS Phase I**) | **The "test fast" cure (gap G4): a node carries a pinned sample so a TEST run reuses it instead of re-executing a slow/expensive/side-effecting node.** Continuing on the user's "ادامه بده و بعد اتمام این استپ بلافاصله استپ بعدی رو شروع کن" right after H-T4 (PR #80). Ran the session-start protocol (STATE2 → PLAN3 I-T1 → ARCHITECTURE I4/I5 + the executor `runLoop`/flow schema/execution state). **DESIGN (ROADMAP Decision Log #21):** a NEW optional `FlowNode.pinnedData` (`z.array(FlowItemSchema).max(50)` — a pin is a sample, not a dataset) + a NEW PERSISTED `ExecutionState.testRun?` flag. **Engine (`packages/core/src/engine/executor.ts`):** `StartInput.testRun?`; `start()` stamps `testRun:true` on the state when set; in `runLoop`, BEFORE the disabled-branch, a node carrying `pinnedData` IN A TEST RUN short-circuits — `result = { kind:'items', outputs:{ main: structuredClone(pinned) } }` (the clone means a downstream node can't mutate the stored pin) INSTEAD of executing, with an info log; never fires in a production run. **Why the flag is on the STATE not a transient arg (I4):** a flow that WAITs and resumes must keep honouring pins — a transient `start()` arg would be lost on the first resume, silently disabling pins mid-run; persisting it on the durable execution state is the only correct seam. **Server (`apps/server/src/api/flows.ts`):** ONLY the editor's "Test run" endpoint `/api/flows/:id/run` passes `testRun:true`; production runs (router/scheduler/webhook) omit it → pins invisible to a live bot. **Editor:** `stores/canvas.ts` `updateNode` type accepts `pinnedData` (impl spreads `{...n,...patch}` → no impl change); NEW pure DOM-free `flattenOutputForPin(output)` + `PIN_ITEMS_CAP=50` in `canvas/run-data.ts` (flattens a node's last run output, main-port FIRST, capped — F-T3 pattern, unit-tested directly); `NodeDetail.tsx` gains a `.ndv-pinbar` (📌 Pin output / Unpin / "Pinned (n) — used in test runs"); `CtbNode.tsx` shows a 📌 Pinned badge after the note line; i18n `editor.node.pin.*` (action/unpin/pinned/badge/hint) en+fa parity; CSS `.ndv-pinbar`/`.ndv-pinned`/`.ndv-pin-hint`/`.ctb-node-pinned`. All optional/no-default → every existing stored flow/fixture/export parses byte-identically. **Tests:** core `executor.test.ts` +5 ("executor — pinned data": TEST run uses pin not execution; PRODUCTION run ignores pin; pin on entry feeds downstream; empty pin → no items; testRun persists & survives WAIT/resume) via a `graph()` helper extended to accept `pinnedData` (`executor-fakes.ts`); shared `flow.test.ts` +7 ("node pinnedData": optional/absent; byte-identical sample; preserves items; empty pin ok; rejects non-FlowItem; rejects >50; accepts exactly 50); editor `run-data.test.ts` +4 ("flattenOutputForPin": null/empty→null; single-port flatten; main-first ordering; caps at `PIN_ITEMS_CAP`). **Verify:** typecheck all 6 workspaces GREEN (`apps/server` with `NODE_OPTIONS=--max-old-space-size=3072`); shared 96 / core 55 / editor 239 / nodes 535 / **server 450** all GREEN (the e2e quickstart "Test run sends the greeting" still passes — the additive `testRun:true` didn't break the manual run path). **Next: I-T2 (Execute a single node, gap G16) — started immediately per the user's instruction.** |
| 2026-06-23 | PLAN3 **H-T4** — add-node-on-edge + wire-drop-to-palette (**COMPLETES Phase H**) | **The cure for "growing a flow means drag-from-palette-then-rewire": two wire-first authoring affordances.** Continuing on the user's "ادامه" right after H-T3 (PR #79). **DESIGN (F-T3 pattern — risky structural edits are PURE + DOM-free in `canvas/graph.ts`; React/RF is thin glue):** **(G8) add-node-on-edge** — a NEW custom edge `CtbEdge.tsx` (registered `edgeTypes.default`) shows an inline "+" at the wire midpoint (`EdgeLabelRenderer`, hover/selection-revealed); clicking opens a small floating `NodePicker.tsx` popup pre-targeted at THAT edge, and the chosen type runs pure `insertNodeOnEdge(graph, edgeId, type, pos)` → `A.outPort→B.inPort` becomes `A.outPort→N.main` + `N.main→B.inPort`. **Bug the tests caught + fixed:** the new edge ids must be minted against the graph that STILL holds the original edge (drop it LAST), else `nextEdgeId` reuses the freed id and silently resurrects A→B. **(G9) wire-drop-to-palette** — `onConnectStart` records the dragged handle; `onConnectEnd` on the empty `.react-flow__pane` opens the SAME picker at the drop point, and pure `nodeFromDangling(graph, pending, type, pos)` creates the node already wired to the dangling end (RF's `handleType` says which end was fixed: pending SOURCE→`A→N.main`, pending TARGET→`N.main→B`). **Store:** NEW undoable `insertNodeOnEdge` (returns null + no-op-no-history on unknown edge) + `addNodeFromDangling`, each a single `commit` so the whole rewrite is one undo/autosave; every edge on the universal `'main'` port; result re-validates against `FlowGraphSchema`. **i18n:** `editor.edge.insert` + `editor.picker.title` (en/fa). **CSS:** `.ctb-edge-tools`/`.ctb-edge-add` + `.node-picker*`; also fixed a latent undefined `--panel` var (4 refs) → `--bg-raised`. **Tests:** editor `canvas-graph.test.ts` +5 (insert split + branch-port preservation + unknown→null; dangling source/target; `buildNode` uniqueness) + `canvas-store.test.ts` +4 (insert one-undoable A→N→B & undo restores original; unknown-edge no-op; dangling source/target single edge). **Verify:** all 6 workspaces typecheck GREEN (server with `--max-old-space-size=3072`); shared **89 GREEN**; editor **235 GREEN** (was 226, +9); i18n-parity GREEN; editor build GREEN. Editor-only — no schema/server/registry/engine change. **Phase H COMPLETE. Next: Phase I — I-T1 (pin data on a node).** |
| 2026-06-23 | PLAN3 **H-T3** — canvas error surfacing | **The cure for "a run failed but the canvas doesn't show me WHERE": a node that failed on the LATEST execution now glows red and reveals its error in one click.** Resuming on the user's "ادامه" after PR #78 (H-T2) MERGED (`origin/main` → `fef2508`); re-cut `genspark_ai_developer`, ran the session-start protocol (STATE2 → PLAN3 H-T3 → the run-data overlay infra + `exec_logs` schema). **DESIGN (F-T3 pattern — risky logic in a pure DOM-free module, React is thin glue):** NEW `mapRunErrors(logs)` in `apps/editor/src/canvas/run-data.ts`, sibling to `mapRunData`. Key insight: a FAILED step's `exec_logs` row is `level:'error'` but usually has NO I/O snapshot, so `mapRunData` deliberately SKIPS it — `mapRunErrors` keeps EXACTLY those rows, prefers the structured `error` column, falls back to `message`, defaults to a stable non-empty `'error'`, and keeps the LAST error per node (loop-revisit = most recent, matching mapRunData's "latest visit"). **Store (`stores/run-data.ts`):** NEW `errorsByNode: Map<string,string>` populated beside `byNode` on `load`, cleared on the no-execution path + `reset`. **Canvas (`CtbNode.tsx`):** reads `errorsByNode.get(id)`; when set, the outer div gets an `errored` class (pulsing red glow, motion-reduced) and the body renders a clickable `.ctb-node-error` `<button>` ("Failed on last run") that `stopPropagation`s and calls `useNodeDetail.open(id)`. **NDV (`NodeDetail.tsx`):** renders a `.ndv-error` banner (`role=alert`) under the head when the open node errored. **i18n:** `editor.node.runError` (en/fa parity). **CSS:** `.ctb-node.errored` + `ctb-node-error-pulse` keyframe (disabled under `prefers-reduced-motion`), `.ctb-node-error` (button reset), `.ndv-error`. **Tests:** `apps/editor/test/run-data.test.ts` +8 — `mapRunErrors` describe (keeps error-only rows mapRunData skips; error-column-over-message; empty→`'error'`; last-wins + ignores non-error/nodeless) + store tests (FIXTURE run where node `fetch` throws while `send` succeeds → `errorsByNode.get('fetch')` is the message, `send` absent from errors but keeps its I/O in `byNode`; cleared on never-ran + reset). **Verify:** all 6 workspaces typecheck GREEN (server with `--max-old-space-size=3072`); shared **89 GREEN**, editor **226 GREEN** (was 221, +5); i18n-parity GREEN; editor build GREEN. Read-only run-data overlay — no schema/server-route/registry/engine change; stored flows unchanged. **Next: H-T4 (add-node-on-edge + wire-drop-to-palette).** |
| 2026-06-23 | PLAN3 **H-T2** — node rename / human title | **The cure for "a big flow is a wall of identical type labels" — a node can carry a custom human name.** Resuming where the prior turn was cut off (the user: "ادامه"), I ran the session-start protocol (STATE2 → PLAN3 H-T2 → ARCHITECTURE §6 + the existing flow schema / canvas code), confirmed PR #77 (H-T1 sticky notes) MERGED (`origin/main` → `d7ef9b9`), re-cut `genspark_ai_developer`, removed a stray `apps/server/core` core-dump, and verified baseline GREEN (all 6 workspaces typecheck — `apps/server` needs `--max-old-space-size=3072` in this sandbox, the default OOM-aborts; shared 83 / editor 216). **DESIGN DECISION (ROADMAP Decision Log #20):** a NEW optional `FlowNode.title?` (`z.string().max(120).optional()`) — a PRESENTATIONAL human name, NOT a routing key. The executor NEVER reads it (it routes by `id`/`type`/edges, exactly as it ignores `position`/`note`), so the field is `.optional()` with no default → every existing stored flow/fixture/export parses byte-identically. **I deliberately did NOT ship the "expressions reference a node by title" half** that PLAN3's one-line H-T2 sketch mentioned: CTB's executor is a LINEAR CURSOR that doesn't retain every node's output keyed by name, so n8n's `$('Node Name')` would be a deep durability change far outside an atomic task — and a node id already uniquely addresses a node; I logged that as a separate future engine task in Decision Log #20 + PLAN3. **Shared:** `title` added to `FlowNodeSchema`. **Editor:** NEW pure DOM-free `nodeDisplayName(node, typeLabel)` in `canvas/graph.ts` (non-blank trimmed title wins, else the type label) — unit-tested directly, the React surfaces are thin glue (F-T3 pattern); `CtbNode.tsx` head shows `displayName` with the type label dropping to a new `.ctb-node-type` sub-line when a custom title is set (so the user still sees WHAT the node is); `ParamPanel.tsx` header shows `displayName` + a NEW "Name" text field (placeholder = type label) editing `title` via `updateNode`; `NodeDetail.tsx` NDV header shows `displayName`; `stores/canvas.ts` `updateNode` patch-type widened to include `title`. **i18n:** `panel.title` + `paramDesc.panel.title` (en/fa parity). **CSS:** `.ctb-node-type`. **Tests:** shared `flow.test.ts` +6 (optional/absent; sample byte-id; custom title preserved; RTL title; >120 rejected; =120 accepted) + editor `canvas-graph.test.ts` +5 (`nodeDisplayName` fallback/title-wins/blank-or-ws→fallback/trim/RTL). **Verify:** all 6 workspaces typecheck GREEN; shared **89 GREEN** (was 83); editor **221 GREEN** (was 216); i18n-parity test GREEN; shared `flow-export`+`flow-validate` 31 GREEN (export/import unaffected). Additive — no server/registry/node/engine change. **Next: H-T3 (canvas error surfacing).** |
| 2026-06-23 | PLAN3 **H-T1** — sticky notes (**OPENS Phase H**) | **The cure for "a big flow is an unlabelled wall of nodes": free-floating annotation boxes on the canvas.** A NEW optional `FlowGraphSchema.notes: StickyNote[]` (`id`/`text`/`position`/`size`/`color` — ROADMAP Decision Log #19), a CANVAS-ONLY element that is NOT a node type and NOT a separate store: no params, no ports, and the executor never reads it (it touches only `graph.nodes`/`graph.edges`). `notes` is `.optional()` so every existing stored flow/fixture/export parses byte-identically; notes ride the one flow document so they inherit undo/redo, autosave and export/import for free. Shared `StickyNoteSchema` + note-id-uniqueness `superRefine`; editor pure mapping (`notesToRfNodes` behind flow nodes at `zIndex:0`, `rfIdForNote`/`noteIdFromRf` `note:` namespace, `nextNoteId`); store `addNote`/`updateNote`/`removeNotes` + gesture-coalesced `moveNote`/`resizeNote`; `StickyNote.tsx` React Flow custom node (NodeResizer + in-place edit + 5-colour picker); `FlowCanvas` registers `sticky`; toolbar AddNote button; i18n `editor.note.*` (en/fa); CSS `.sticky-*`. Tests: shared `flow.test.ts` +8 + `flow-export.test.ts` +1; editor `canvas-graph.test.ts` +5 + `canvas-store.test.ts` +5. (PR #77, MERGED → `d7ef9b9`.) |
| 2026-06-23 | PLAN3 **G-T3** — explicit simple/advanced field grouping (**Phase G COMPLETE**) | **The cure for "the node still looks busy": a node author can demote a power-user param under a collapsible "Advanced" section — structurally, so all 61 nodes + Collection forms inherit it (UX-T1/G-T1 precedent).** Ran the session-start protocol (STATE2 → PLAN3 G-T3 → ARCHITECTURE I2/I5), removed a stray 622 MB `apps/server/core` core-dump from the working tree, and confirmed baseline GREEN (editor typecheck + **201 tests**). **Empirically verified** `z.toJSONSchema(schema, {io:'input'})` carries a custom `z.meta({ advanced:true })` through as a plain field property — exactly like the proven `ctbWidget` pattern — so the flag needs NO schema/engine change (ROADMAP **Decision Log #18**). **Pure layer (`apps/editor/src/form/schema.ts`):** `JsonSchema.advanced?:boolean`; NEW `isAdvanced(spec)`; `PartitionedFields` gained a third `advanced` bucket; `partitionFields` routes advanced fields to it FIRST (precedence over required/isSet/added); NEW `anyAdvancedSet(advanced,value)` for auto-open. **UI (`SchemaForm.tsx`):** extracted a shared `renderField(spec,removable)`; advanced fields render under a native `<details class="form-advanced">` "Advanced" `<summary>` that `open`s (via `key`-remount) when `anyAdvancedSet` is true so editing an existing node never hides the user's data. **Author change (`packages/shared/src/node-params.ts`):** `tg.sendMessage.parse_mode` → `.meta({ advanced:true })` (first demoted param — a beginner-irrelevant HTML/MarkdownV2 toggle). **i18n:** `form.advanced.title` (en "Advanced" / fa "پیشرفته", parity compile-enforced). **CSS:** `.form-advanced*` (custom ▸ caret rotating on `[open]`). **Tests (`form-engine.test.ts`): +5** against the REAL shared schema — `isAdvanced` true/false; advanced field routed to `advanced` even when SET; unannotated `tg.trigger` split byte-identical (empty advanced bucket); `anyAdvancedSet` truth table; `parse_mode='HTML'` still validates + all three buckets cover every prop once; plus the existing `tg.sendMessage` progressive-disclosure test updated. **Verify:** editor typecheck + build GREEN, editor **206 GREEN** (was 201, +5); shared **74 GREEN**; server **450 GREEN** (the schema-meta touch is inert — engine/exports unchanged). Additive — no registry/route/engine change. **Phase G COMPLETE; next Phase H (canvas power) — H-T1 sticky notes (schema-touching → own Decision Log entry).** |
| 2026-06-22 | PLAN3 **F-T3** — Export button in the editor toolbar + shared flow-export helper | **First PLAN3 task shipped — the cure for the #1 discoverability complaint ("how do I extract a workflow?").** Audit finding: import/export/templates all EXISTED on the per-bot flow LIST page (`FlowsPage`), but a user INSIDE the editor (`FlowEditorPage`) had no way to export the open flow — they had to navigate back. **Fix:** (1) NEW `apps/editor/src/lib/flow-export.ts` — a PURE, DOM-free `flowExportFilename(name)` (collapses non-word runs → `_`, trims, falls back to `flow`) + `flowExportBlob(envelope)` (pretty JSON, `application/json`) + a thin DOM `downloadFlowExport()`. (2) `FlowEditorPage.tsx` — NEW `ExportButton` in the toolbar (next to Save/Test-run): flushes pending edits via `saveNow()` first so the download matches the canvas, then `GET /api/flows/:id/export` → downloads `<name>.json`; on error shows `flows.export.failed`. (3) `FlowsPage.tsx` — its inline export logic de-duplicated onto `downloadFlowExport` (single source of truth). (4) i18n `editor.export.button`/`.busy`/`.hint` (en+fa, parity compile-enforced). Import + the template gallery stay on `FlowsPage` (the right home — both CREATE a new flow). **Tests:** NEW `flow-export.test.ts` (+6 — filename clean/collapse/trim/fallback/non-latin, blob pretty-JSON round-trip). **Verify:** editor typecheck + build GREEN; editor tests **177 GREEN** (was 171). Additive — no schema/server/registry/node change. |
| 2026-06-22 | PLAN3 authored — **n8n-parity & editor-polish roadmap (docs only, no code)** | **The user's ask after UX-T1: "we modelled our UI on n8n but never reviewed it carefully — many things slipped past us; review everything against n8n and build a PLAN3 to take the project to a higher level" (e.g. "how do I even extract/import a workflow?").** Did a systematic audit of CTB's editor vs n8n. **Key finding (the PLAN3 thesis): the gap is NOT missing capability — it's discoverability + ergonomics.** Confirmed import/export AND the template gallery ALREADY exist in BOTH the backend (`apps/server/src/api/flows.ts`: `GET /:id/export`, `POST /import`, `GET /flow-templates`, `POST /import-template`) and the editor UI — the user couldn't find them, which validated the thesis. Wrote **`docs/PLAN3.md`** (213 lines): §0 honest diagnosis, §1 what CTB already has, §2 a 16-row gap-analysis table (CTB vs n8n; ✅/🟡/❌/⭐), §3 guiding principles (discoverability over capability; structural-not-per-node like UX-T1; beginner floor/expert ceiling; no engine rewrite/pivot/n8n-fork), §4 phased atomic tasks — **Phase F** first-run & discoverability (guided empty state, hello-bot template, surface import/export/templates), **Phase G** field ergonomics (⭐ Fixed\|Expression toggle, live expression preview, simple/advanced grouping), **Phase H** canvas power (sticky notes, node rename, error surfacing, add-node-on-edge), **Phase I** run & iterate (pin data, single-node execute, shortcut help) — §5 sequencing, §6 explicit non-goals, §7 decision-log seeds. ROADMAP.md updated: a **"PLAN3 — n8n-parity & editor polish"** track added after Phase 6 + **Decision Log #17** (reframe to discoverability/ergonomics; sticky-notes/node-title/pin-data are the only schema-touching items, each gets its own future entry). **Docs-only — no code, no schema/registry/node change.** Awaiting user review/approval before any Phase F implementation. |
| 2026-06-22 | UX-T1 (progressive-disclosure node forms) — **the form-engine fix for "every node is overwhelming"** | **The user's core UX complaint: opening a node (e.g. `tg.trigger`) shows ALL fields at once, so a beginner can't tell what's essential. Fix = n8n's "+ Add option" pattern, applied generically at the form-engine level so EVERY node benefits with zero per-node work.** Audited the project first (it actually runs end-to-end: server boots, DB migrates, login + `/api/node-types` serve 61 nodes, 6 packages typecheck, 106 test files; the inline/reply-keyboard + voice gaps the user hit in n8n ARE solved here). Root cause found in `apps/editor/src/form/SchemaForm.tsx` + `schema.ts`: `objectFields` returned EVERY property and the form rendered them all. **Fix (purely structural — no node-type knowledge, so Collection forms benefit too):** (1) `schema.ts` — NEW `partitionFields(schema, value, added?)` splitting properties into `shown` (required, OR already-set via the new `isSet`, OR explicitly opted-in via the session `added` set) vs `optional` (everything else); NEW `isSet` (blanks/`''`/`[]`/`{}` unset; `0`/`false` count as set). (2) `widgets.tsx` — NEW `AddOptionControl` (a "+ افزودن گزینه" dropdown of the not-yet-added optional fields, click-away close) + `FieldRow` gained an optional remove (×) button for added/non-required fields. (3) `SchemaForm.tsx` — renders only `shown` fields, each non-required one with a × that unsets it and returns it to the menu; an `added` `useState<Set>` keeps a freshly-added blank field visible (so an empty-string seed doesn't instantly re-hide). (4) i18n `form.addOption`/`form.removeOption` (en+fa parity); (5) CSS for `.add-option*` + `.field-remove` (reuses theme vars, RTL-safe). **Tests:** +7 in `form-engine.test.ts` driving the PURE layer against the REAL shared schemas — `tg.trigger` empty shows ONLY `event` (the rest opt-in), a filled optional stays visible, add seeds+keeps via `added`, remove returns to menu, `tg.sendMessage` chat/keyboard/options are opt-in, and a no-field-is-dropped invariant. **Verify:** editor typecheck GREEN; editor tests **171 GREEN** (was 164); `npm run build -w apps/editor` GREEN. Additive — no schema/registry/node change, no server change. **This is interim; the user next wants a systematic n8n review → PLAN3.** |
| 2026-06-20 | PE-T2 (Call Session Service + `ctx.call`) — the live-voice host runtime | **The hard part of Phase E: one typed `ctx.call` capability backed by a long-lived host service that owns every realtime Telegram call — with NO native dependency in the default path.** Resuming where the prior turn left off (the user: "ادامه"), I confirmed PR #63 (PE-T1) MERGED (`origin/main` → `d3cd128`) and that the working tree already held the start of PE-T2 (the shared `ctx.call` types + the core `ExecutorServices.call` factory, both typecheck-GREEN; nodes was RED because the test harness's `FakeCtx` lacked the new required `call` prop). I finished PE-T2 to Definition-of-Done. **(1) Harness fix:** added a recording `call` fake to `packages/nodes/test/node-harness.ts` (`callCalls[]` + `CallInvocation` union; `call: null` opt-out; `status`/`grantTurn` overridable) → nodes typecheck + all 509 tests GREEN. **(2) Loopback connector** (`apps/server/src/engine/loopback-connector.ts`): the DEPENDENCY-FREE default `VoiceConnector` — `connect`/`leave` idempotent, `speak` optionally echoes back as an utterance, `emitUtterance` lets tests inject inbound audio, `onUtterance` fan-out is leak-safe; so the whole service runs without MTProto and the real userbot engine swaps in at the composition root (PLAN2 §E.1 "one interface, many adapters", I3). **(3) `CallSessionService`** (`apps/server/src/triggers/call-session.ts`): a long-lived service (a SIBLING to the Scheduler) that resolves the `voiceConnection` credential fail-closed (`makeVoiceCredentialResolver` over the host decrypt — the session stays host-side, I6/I7), drives the connector, and holds per-call state — participants, the lineup **turn queue** (`grantTurn` sequential/random + explicit line-jump, `endTurn`, optional `maxTurnSeconds` auto-advance), `status`, and the `onUtterance` sink PE-T3 will subscribe to. **Hard caps = config + safe defaults (`DEFAULT_CALL_CAPS`, I4):** `maxConcurrentCalls` (host) + `maxCallsPerBot` + `maxCallSeconds` (auto-leave); `connect` fails *loudly* past a cap, checked BEFORE touching secrets/the network. **Behaviour = config:** channel/group live broadcast = `mode:'lineup'`; 1:1 Direct→AI = `mode:'support'` + `target.kind:'user'` — the SAME nodes, no fork. **(4) Wiring** (`wire.ts`): builds the service (default loopback unless `WireOptions.voiceConnector`; caps via `WireOptions.callCaps`), adds the `call` factory to `ExecutorServices` (`capabilityFor(botId, flowId)`), exposes `Engine.callSessionService`; `main.ts` shutdown calls `callSessionService.stop()`. **(5) Tests:** NEW `apps/server/test/call-session.test.ts` (24). **(6) Docs:** NODES.md §"Call Session Service + ctx.call" + this STATE2 + PLAN2 PE-T2 ✅, same commit. **Verify:** 6 pkgs typecheck GREEN; tests shared 73 / core 50 / nodes 509 / sandbox 16 / server 429 GREEN. Additive — no node/registry change. Next: PE-T3 (`trigger.callEvent`). |
| 2026-06-20 | PE-T1 (`voiceConnection` credential) — **Phase E begins** | **Live voice AI starts: the credential that abstracts "who connects the audio" to a live Telegram call.** The user clarified the two target scenarios in their own words — (1) a **group/channel live voice chat** that members hear by joining the call, with a Q&A turn-taking mode; and (2) enabling a **channel's Direct** so that when a user messages it they can place a **voice call an AI answers in real time** — and I confirmed both are covered by the SAME generic nodes through the `target`/`mode` settings (PLAN2 §E.1, no per-scenario node). Ran the session-start protocol (STATE2 → PLAN2 §E → ARCHITECTURE I2/I3/I6/I7), confirmed PD-T4/PR #62 MERGED (`origin/main` → `8d970c6`), re-cut `genspark_ai_developer`, and verified baseline GREEN (6 pkgs typecheck; tests shared 73 / core 50 / nodes 509 / sandbox 16 / server 384). **Found a prior session had begun PE-T1 but never committed it** — the working tree held a complete shared schema + host connector + API route + editor form, all uncommitted. I reviewed every diff, found it sound, and **finished PE-T1 to Definition-of-Done:** (a) **shared** — `VoiceConnectionSchema` in the `CredentialDataSchema` discriminated union with the `kind` enum (`userbot`/`companion`/`external`, default userbot) + MTProto fields (`apiId`/`apiHash`/`session`) + external fields (`bridgeUrl`/`bridgeToken`); `credentialHint` (→ `kind · ••••<session-tail>`, external → `kind · <url>`, never the secret) + `credentialAuthHeaders` (→ `{}` — a voice connection resolves into a media engine, not HTTP headers) extended EXHAUSTIVELY; `CredentialTypeSchema`/`CREDENTIAL_TYPE_LABELS` gained `voiceConnection`. (b) **host (`apps/server/src/engine/voice-connector.ts`, I3)** — the `VoiceConnector` adapter interface (`checkHealth`/`connect`/`speak`/`onUtterance`/`leave`) + `CallTarget`/`PcmFrame`/`CallUtterance`/`VoiceConnectorHealth` types the PE-T2 media engine implements behind ONE `ctx.call`; `resolveVoiceConnection` validates **fail-closed** (non-voice type, or a `kind` missing its required fields, throws a clear secret-free `VoiceConnectionError`); `validateVoiceConnection` is the panel's leak-free probe (no adapter → "structurally valid, login not attempted"; wired adapter surfaced verbatim; any throw → `{ok:false,error}`). (c) **API** — `POST /api/credentials/:id/voice-health` decrypts host-side (the session never leaves the server, I7), 404 unknown / 409 non-voice, returns `{health}`; `CredentialsApiDeps.voiceConnector?` optional (wired in PE-T2). (d) **editor** — Credentials form gained the type (a `kind` picker → api_id/api_hash/session for userbot/companion, bridge URL/token for external; blank fields omitted so the server fails closed); fa/en i18n parity. **NEW tests I wrote:** `voice-connector.test.ts` (15 — resolve happy/fail-closed per kind, never-leak-the-session, health probe with/without a wired adapter, adapter-throws → leak-free) + `api-credentials.test.ts` (+6 — create/encrypt/mask, voice-health 200-valid / 200-false-incomplete / 404 / 409 / 401). **Verify:** server `voice-connector` + `api-credentials` suites GREEN (29/29 in those two files); typecheck GREEN (shared/server/editor). Additive — no node/registry change → no guard bumps. Next: PE-T2 (Call Session Service + `ctx.call`). |
| 2026-06-20 | PD-T4 (node-library docs site) — **Phase D COMPLETE** | **The node registry is now browsable: a bilingual `/docs` page auto-generated from the catalog, no node hardcoded.** After the user said "continue" / "عالی ادامه بده", I confirmed PR #61 (PD-T3) MERGED (`origin/main` → `e4f0068`), rebased `genspark_ai_developer` onto it, and built **PD-T4** — the last Phase-D task. **Model (`apps/editor/src/pages/node-docs/model.ts`, pure/DOM-free):** `buildDocs` turns the `NodeTypeInfo[]` from `GET /api/node-types` into category buckets in palette order (`trigger`/`telegram`/`flow`/`data`/`ai`; an unknown future category is appended alphabetically), nodes sorted by `type`; `toDocNode` projects **params** via the form engine's own `objectFields` (so the documented param shape can NEVER drift from what the form collects / the server re-validates, I5) → `{key, required, widget, typeSummary, defaultText, description}` with `summarizeType` (enum → "one of: …", union de-duplicated, array → "list of …", `z.unknown()` → "any") + `defaultText`; plus **ports** (in/out, dynamic-out flagged), **typed-connection facts** (role / input-slot `kind`s / provides, PB-T1), and an `isTrigger` flag (no inputs). `filterDocs` is a locale-aware free-text search over type/label/description/param keys; `totalNodes` for the header. **Page (`apps/editor/src/pages/NodeDocsPage.tsx`, route `/docs`, nav "Node Library"):** fetches the catalog once via `api.listNodeTypes()`, renders category sections of node cards — each with the fa/en label+description resolved from the `meta` i18n keys (bilingual for free), a params table, ports, and connection facts — over a search box. **Wiring:** `/docs` route + a `nav.docs` NavLink added to `App.tsx`. **i18n:** `nav.docs` + the `docs.*` family (title/subtitle/search/empty/tags/ports/conn/param.*) added to BOTH `i18n/en.ts` and `i18n/fa.ts` (parity — `MessageKey` is `keyof typeof fa`, so a missing key wouldn't compile). **CSS:** a `.docs-page` block appended to `styles.css` (category headers reuse the `--node-*` palette colours; a responsive `auto-fill minmax(320px)` node-card grid; param table; a local `.muted`/`.error`). **Snag fixed:** `InputSlot` has no `name` — its `kind` doubles as the port name (shared `node-def.ts`), so the model maps slots by `kind`; the "unknown category" test casts past the typed `category` union via `as unknown as NodeTypeInfo`. **Tests:** NEW `apps/editor/test/node-docs-model.test.ts` (21 — `summarizeType`/`defaultText`/`paramsOf` against the genuine `tg.sendMessage` schema + a synthetic union node, `toDocNode` trigger/ports/null-descKey, `buildDocs` palette ordering + node sort + unknown-category append, `filterDocs` by type/label/param + empty, and a build against the live fake `GET /api/node-types`). **Verify:** editor typecheck GREEN; node-docs-model 21/21 + i18n-namespace 8/8 + api-client 10/10 + form-engine 24/24 + param-panel 3/3 + canvas-graph 19/19 + stores 11/11 GREEN; `npm run build --workspace @ctb/editor` GREEN (359 modules transformed). Editor-/docs-only, additive — no server/shared change. **Phase D (hardening) is now COMPLETE (PD-T1…PD-T4); the next PLAN2 work is Phase E (live voice AI), pending the user's sign-off on the E.3 open questions.** |
| 2026-06-20 | PD-T3 (MCP/API rate limiting + audit) | **The public `/api/v1/*` surface is hardened with per-token rate limiting + an append-only audit log.** Session resumed mid-task after a sandbox reset (backup + context recovered); PD-T2 was already committed (`3750931`, PR #60 MERGED). **Shared (I5):** `CreateApiTokenBodySchema` gained `rateLimitPerMin` (`int().min(0).max(100_000).default(120)`); `DEFAULT_API_RATE_LIMIT=120`; `ApiTokenPublic.rateLimitPerMin`; new `ApiAuditEntry`/`ApiAuditList`. **DB (migration `0005`):** `api_tokens.rate_limit_per_min` (default 120) + NEW `api_audit` table (token_id->api_tokens ON DELETE SET NULL, bot_id, action, method, route, target_id, status, ts) + ts/token indexes. **Libs:** NEW `lib/rate-limiter.ts` (`RateLimiter` — in-memory 60s sliding window, `check(key,limit)`→`{allowed,remaining,retryAfterSec}`, `0`=unlimited, rejected check records no hit, `forget`/`reset`) + NEW `engine/audit-store.ts` (`SqliteApiAuditStore` — `record`/`list` newest-first, token/bot filters, cap 500). **Wiring:** both built once per process in `wire.ts` (limiter clocked off the engine clock) + exposed on `Engine`; `app.ts` passes them to `registerV1Api`. **v1 (`api/v1.ts`):** the bearer preHandler runs the limiter on `row.rateLimitPerMin` → `429 {error:'rate_limited',retryAfterSec}` + `Retry-After` header on breach (BEFORE last-used is stamped); a NEW `onResponse` hook writes one `api_audit` row per AUDITED authoring/trigger/send call (keyed by `auditKey(method,path)` collapsing `:id`, e.g. `POST /api/v1/flows/activate`), stamping the **target** bot each route stashes via `markAuditBot(req,botId)` (not the token scope) so a row's `botId` is the bot acted upon — a 403/422 is logged like a 201/202, a 429 leaves no row, plain reads aren't audited; NEW `GET /api/v1/audit` (instance-wide sees all/filterable by bot_id+token_id+limit; bot-scoped locked to its own bot, 403 on another bot). `api/api-tokens.ts` `toPublic`+POST carry `rateLimitPerMin`. `test/db.test.ts` EXPECTED_TABLES += `api_audit`. **Docs:** PROTOCOL.md §Inbound REST API gained a "Rate limiting & audit" subsection + the `GET /api/v1/audit` endpoint + the `rateLimitPerMin` token field; PLAN2 PD-T3 marked done. **Tests:** NEW `apps/server/test/api-rate-audit.test.ts` (25 — RateLimiter unit (window/unlimited/per-key/forget/reset), SqliteApiAuditStore unit (record/list/filter/cap), token config (default 120 + custom echo), e2e 429+retry-after & per-token keying & unlimited & no-audit-on-429, e2e audit of create/activate/trigger/send + no-audit-on-reads + 403-logged, `GET /api/v1/audit` scope). **Verify:** typecheck GREEN (shared/core/server/editor); api-rate-audit 25/25 + db 2/2 + existing api-v1 35/35 GREEN. Additive (token field defaults → existing tokens get 120). Next: PD-T4 (node-library docs site). (PR #61, MERGED → `e4f0068`.) |
| 2026-06-20 | PD-T2 (agent cost governance) | **Per-bot AI spend is now budgeted + metered.** A per-bot **daily AI budget** (`maxCallsPerDay`/`maxTokensPerDay`/`maxTokensPerRun`, stored in `bots.settings.aiBudget`; shared `BotAiBudgetSchema`+`readBotAiBudget`) is enforced **fail-closed BEFORE** every provider call and **metered AFTER** in a new `ai_usage` ledger. The `ctx.ai` capability became a **per-run factory** `(botId,flowId,executionId)` (`makeAiFactory` in `wire.ts`, executor passes the run context): before each `chat` it reads the bot's budget + today's totals and throws if the cap would be exceeded (`0`=unlimited, no provider request made); after a successful chat it records one `ai_usage` row (bot/flow/execution + credential + model + token counts). NEW `engine/ai-usage-store.ts` (`SqliteAiUsageStore` — record/todayTotals/allTimeTotals/byCredential/summary). Panel: `GET /api/bots/:id/ai-usage` + `PUT /api/bots/:id/ai-budget` (`api/bots.ts`); editor `BotsPage.tsx` AI-budget panel + usage view; `client.ts` `getBotAiUsage`/`setBotAiBudget`; fa/en i18n. Migration `0004`. The `exactOptionalPropertyTypes` token fields on `AiUsageEntry` were typed `number|undefined`; the voice-demo test's `makeAi` fake was adapted to the per-run factory shape. Tests `ai-cost-governance.test.ts` (6). (PR #60, MERGED → `3750931`.) |
| 2026-06-20 | PD-T1 (DB connection pooling + safety) — **Phase D begins** | **The `db.postgres`/`db.mysql` connectors gained connection-pool caps, per-statement timeouts, and a read-only credential option — plus an explicit SQL-injection test battery.** After the user said "continue development", I ran the session-start protocol (STATE2 → PLAN2 PD-T1 → ARCHITECTURE I2/I3/I5/I6/I7 + the existing PB-T2/PB-T3 DB code), confirmed PR #58 (PC-T5) MERGED (`origin/main` → `ea36756`), re-cut `genspark_ai_developer`, and verified baseline green before coding. **Shared (I5):** added three fields to BOTH `PostgresSchema` and `MysqlSchema` in `credentials.ts` — `poolMax` (`z.coerce.number().int().min(1).max(100).default(5)`), `statementTimeoutMs` (`.min(0).max(600_000).default(30_000)`, 0 = none), `readOnly` (`z.boolean().default(false)`); added `write?: boolean` to `DbQueryRequest` in `node-def.ts`. **Host (`apps/server/src/engine/wire.ts`, I3):** `DbPoolConfig` carries the three fields; `DbPool` gained `readOnly?`; `makeDb.resolvePool` builds the cfg from the decrypted credential and stamps `pool.readOnly` (fail-closed when a fake factory doesn't surface it); `pgPoolFactory` passes `max` + a session `options` string (`-c statement_timeout=…`, and read-only → `-c default_transaction_read_only=on` so the SERVER refuses writes); `mysqlPoolFactory` passes `connectionLimit` + a per-query `timeout`; `makeDb.query` refuses a write on a read-only pool (`if (pool.readOnly && req.write !== false) throw …`). **Nodes:** `db/postgres.ts`+`db/mysql.ts` send `write: params.operation !== 'select'` (SELECT = read; arbitrary `query` = conservatively a write → fail-closed). **Editor:** `CredentialsPage.tsx` Postgres+MySQL forms gained poolMax/statementTimeout number inputs + a readOnly checkbox; `buildData()` always emits them (this fixed the TS2322/TS2339 error the new required-with-default schema fields caused in the literal cfg objects); fa/en i18n parity (`credentials.field.{poolMax,statementTimeoutMs,readOnly}`). **Docs:** NODES.md §Database connectors gained a "Connection hardening +PD" subsection. **Tests:** NEW `apps/server/test/db-hardening.test.ts` (17, on the REAL wired engine + an injected recording fake pool — no socket): a SQL-injection battery (hostile VALUES stay bound; hostile TABLE/COLUMN/ORDER-BY identifiers REJECTED before any query is built; a raw `query` passed verbatim so interpolation must use bind params; both dialects), pool-limit/timeout pass-through to the factory (+ schema defaults 5/30000/false), and read-only enforcement (SELECT passes; insert/update/`query` refused with "read-only"; mysql host-side refusal; a writable credential still writes). **Verify:** typecheck GREEN (6 pkgs); db-hardening 17/17 GREEN. Next: PD-T2 (agent cost governance). |
| 2026-06-20 | PD-T1 (DB connection pooling + safety) — **Phase D begins** | **The `db.postgres`/`db.mysql` connectors are hardened with pool limits, statement timeouts + a read-only option, plus a SQL-injection test battery.** After the user said "continue development" / "I merged, continue" / "continue from where we got cut off", I ran the session-start protocol (STATE2 → PLAN2 PD-T1 → ARCHITECTURE I2/I3/I5/I6/I7), confirmed PR #58 (PC-T5) MERGED (`origin/main` → `ea36756`), and built **PD-T1** — Phase D's first hardening task. **Shared (I5):** added three fields to BOTH `PostgresSchema` and `MysqlSchema` in `credentials.ts` — `poolMax` (`z.coerce.number().int().min(1).max(100).default(5)`), `statementTimeoutMs` (`.min(0).max(600_000).default(30_000)`, 0 = none), `readOnly` (`z.boolean().default(false)`); and `write?: boolean` to `DbQueryRequest` in `node-def.ts`. **Host (`apps/server/src/engine/wire.ts`, I3):** `DbPoolConfig` now carries `poolMax`/`statementTimeoutMs`/`readOnly` and `DbPool` an optional `readOnly`; `makeDb.resolvePool` builds the cfg from the decrypted credential + stamps `pool.readOnly` (fail-closed when a fake factory omits it); `makeDb.query` refuses a write on a read-only pool BEFORE the driver (`if (pool.readOnly && req.write !== false) throw`); `pgPoolFactory` passes `max` + a session `options` string (`-c statement_timeout=…`, and read-only → `-c default_transaction_read_only=on` so the SERVER refuses writes); `mysqlPoolFactory` passes `connectionLimit` + a per-query `timeout`. **Nodes:** `db/postgres.ts`+`db/mysql.ts` send `write: params.operation !== 'select'` (a plain SELECT is a read; an arbitrary `query` is conservatively a write → fail-closed). **Editor:** `CredentialsPage.tsx` Postgres+MySQL forms gained the 3 fields (poolMax/statementTimeout number inputs + readOnly checkbox), `buildData()` always emits them (which fixed the TS error the new required-with-default schema fields caused on the literal `{type:'postgres',ssl:false}` objects), plus fa/en i18n parity (`credentials.field.poolMax`/`statementTimeoutMs`/`readOnly`). **Docs:** NODES.md §Database connectors gained a "Connection hardening +PD" subsection. **Tests:** NEW `apps/server/test/db-hardening.test.ts` (17, REAL wired engine + an injected recording fake pool — no socket): SQL-injection battery (hostile VALUES stay bound; hostile TABLE/COLUMN/ORDER-BY identifiers REJECTED before any query; a raw `query` passes verbatim → interpolation must use bind params; both dialects), pool-limit/timeout pass-through to the factory (+ schema defaults 5/30000/false), and read-only enforcement (SELECT passes; insert/update/`query` refused with "read-only"; mysql host-side refusal; writable credential still writes). **verify:** typecheck GREEN (6 pkgs); the new file's 17 tests GREEN; full `npm run verify` GREEN. Additive to the credential schema (defaults → existing credentials unchanged). Next: PD-T2 (agent cost governance). |
| 2026-06-19 | PC-T5 (builder docs + recipes) — **Phase C COMPLETE** | **The open builder surface is now documented + demoed end-to-end.** After the user said "continue development", I ran the session-start protocol (STATE2 → PLAN2 PC-T5 → ARCHITECTURE I5/I2 + PROTOCOL.md style), confirmed PR #57 (PC-T4) MERGED (`origin/main` → `7e1ae8d`), re-cut `genspark_ai_developer`, and ran STEP-4 verify: typecheck GREEN (6 pkgs) and the **full** suite GREEN (shared 73 / core 50 / nodes 509 / sandbox 16 / server 340 / editor 143) — note `api-executions.test.ts` PASSED this session (the `better-sqlite3` native binding was healthy; no rebuild needed), so the repo was green before I started. Then I built **PC-T5**, a docs + demo task (additive — no node/registry/schema change). **Docs:** PROTOCOL.md gained the **"Authoring & MCP"** chapter (inserted after §MCP server, before §Outbound): a *discover → build → validate → activate → trigger* lifecycle diagram; the two invariants the automation leans on (**I5** — `GET /api/v1/node-types` is the same projection the editor + engine use so the catalog can never advertise an unrunnable node, AND a v1-authored flow is byte-identical to an editor-built one; **I2** — no domain nodes exist to assemble); **Recipe A** (a copy-paste `curl` script walking all five steps incl. polling `GET /api/v1/executions`); **Recipe B** (an AI agent doing the same over MCP — `list_nodes`/`validate_flow`/`create_flow`/`trigger_flow`, self-correcting on `validate` before committing); and a "which surface for which job" quick-reference. The intro surface-table + the MCP quick-start now point at the new chapter. **🎬 Demo** `apps/server/test/e2e-phaseC-authoring-demo.test.ts` (harness mirrored from `api-v1.test.ts` + the `until()` poller from `e2e-phase4-n8n-demo.test.ts`; REAL wired engine + in-memory SQLite + fake Telegram transport): **Test 1** — an external **bot-scoped** agent reads the catalog and FINDS the three node types it needs (asserts `category`/`ports`/`paramsJsonSchema`), assembles the 3-node greeting flow (`flow.manualTrigger → data.setFields → tg.sendMessage`, using the snake_case `bot_id` alias a script would send), `validate`s (`ok:true`, asserts the flow is STILL a draft — validate never mutates), `activate`s (→ `active`), `trigger`s (`chat_id:555`, 202 + executionId), polls `GET /api/v1/executions?flow_id=` until `done`, and asserts the bot SENT `Hello from an external agent 👋` to chat 555 through the live sender (the `{{ $json.greeting }}` expression resolved against the `data.setFields` output); **Test 2 (safety rail)** — a graph with a non-catalog type (`shop.checkout`) fails `validate` (`ok:false` + problems) and `activate` (`422 not_activatable`), staying a draft, proving the catalog is the single source of truth. **Walkthrough** `docs/demos/phase-C-authoring.md` (mirrors `phase-B-voice.md`). **Verify after the change GREEN:** server 340→**342** (+2 demo), 41 test files; everything else unchanged. PLAN2.md PC-T5 marked ✅ ("Phase C COMPLETE"); STATE2.md updated in the same commit. Next: **PD-T1 (DB connection pooling + safety)**. |
| 2026-06-18 | PC-T4 (`tool.mcp` agent tool node) | **A CTB agent can now use any MCP server's tools — the inverse of PC-T3.** After the user said "continue development", I ran the session-start protocol and VERIFIED the PC-T4 note: the agent already supported an `mcp` tool *source* (P5-T3 `ai.mcpClient` is a standalone main-flow node; the inline `AgentToolSource` `mcp` variant already expanded to one tool per advertised server tool), **but there was NO first-class draggable provider node** — MCP-as-agent-tool was only reachable by hand-authoring the `tools[]` param, never via the `ai:tool` slot like `tool.code`/`tool.subflow`. PC-T4 fills exactly that gap. **New node** `tool.mcp` (`role:'provider'`, `provides:'ai:tool'`, no data ports) — its ONLY param is an `mcpServer` `credentialId` (`ToolMcpParamsSchema` in shared). **Design choice (impl detail, not a contract change):** the agent maps a `tool.mcp` provider to an `mcp` tool source BY NODE TYPE in `toolSourcesFromSlots` (so it reuses the existing `resolveTools` MCP listing/expansion path and the editor form stays a clean single credential picker — no confusing `type` field). NODES.md §"AI Agent tool nodes" header + bullet added (spec-before-code). Registry 53→54; editor fa/en i18n (label/desc/param/paramDesc, identical key set both locales). Tests: `ai-tool-nodes.test.ts` gains tool.mcp registration+contract+schema + 2 e2e-through-`ai.agent` cases (expands the server's tools and the model calls one; fails loud with no `ctx.mcp`); `node-types.test.ts` lists tool.mcp as an `ai:tool` provider; all 15 `builtinNodes.length` guards 53→54. **verify:** typecheck GREEN (6 pkgs); PC-T4-relevant suites GREEN (nodes ai-tool-nodes 26 / ai-agent 34 / ai-mcp-client 20; server node-types 9 / mcp 23). NOTE: `apps/server/test/api-executions.test.ts` fails in THIS sandbox (a data.code demo flow → status `error` instead of `done`) — confirmed PRE-EXISTING (fails identically on clean `a1e4fc4` with my changes stashed): the `@ctb/sandbox` worker pool can't spawn worker_threads in the E2B sandbox; it's an environment artifact, not a PC-T4 regression. Next: PC-T5 (builder docs + recipes). |
| 2026-06-18 | PC-T3 (CTB as an MCP server) | **CTB now speaks MCP — an external AI agent can drive the builder.** After the user said "continue", I ran the session-start protocol (STATE2 → PLAN2 PC-T3 → ARCHITECTURE I3/I5; checked existing MCP code and found `ai.mcpClient` from P5-T3 is the *client* side, so PC-T3 = the new *server* side), confirmed PR #55 (PC-T2) MERGED, and built **PC-T3**: `POST /api/v1/mcp`, a streamable-HTTP **JSON-RPC 2.0** endpoint exposing CTB's builder powers as MCP tools — the inverse of `ai.mcpClient`. **Key design decision:** implement the protocol NATIVELY (no `@modelcontextprotocol/sdk` dependency — MCP is JSON-RPC 2.0 on the wire) so the endpoint lives INSIDE the existing bearer-auth `/api/v1` scope and reuses the EXACT same token guard + engine handles + shared schemas as the REST routes (I5 — a flow built over MCP is byte-identical to one built over REST/editor; I3 — `mcp.ts` imports no driver; avoids the SDK's own transport+auth model fighting the existing scope). **New files:** `apps/server/src/api/mcp.ts` (the JSON-RPC handler + the six tool implementations) and `apps/server/src/api/mcp-deps.ts` (the deps interface, split out so `v1.ts` can mount `registerMcpApi` without an import cycle). **Shared (`packages/shared/src/api.ts`):** the MCP wire types — `MCP_PROTOCOL_VERSION` (`2025-06-18`), `MCP_SERVER_INFO`, `JsonRpcIdSchema`/`JsonRpcRequestSchema`/`JsonRpcResponse`, `JSON_RPC_ERRORS`, `McpToolCallParamsSchema`, `MCP_TOOL_NAMES`. **Methods:** `initialize` (→ protocolVersion + `serverInfo {name:"ctb",version:"1"}` + `capabilities.tools` + instructions), `notifications/*` (any id-less message → `202` ack, no JSON-RPC body), `ping` (→ `{}`), `tools/list` (→ the 6 tools, each `inputSchema` built via `z.toJSONSchema` from the SHARED Zod schemas where one exists — FlowGraph/RecordFilter — so the advertised arg shape matches REST byte-for-byte), `tools/call` (→ an MCP result with a single JSON `content` text block). **Six tools, each bounded by the per-request token's bot scope (a `tokenBotId(req)` reader passed from v1):** `list_nodes` (= the PC-T1 catalog projection; bot-agnostic, any valid token), `validate_flow {graph}` (dry-run `validateFlowForActivation` → `{ok,problems,nodeProblems}`), `create_flow {bot_id,name,graph?}` (DRAFT only; `bot_id`→`botId` alias; `unknown_bot`), `trigger_flow {flow_id,chat_id?,payload?}` (async fire-and-forget run via `executor.start`, item `source:"mcp"`; `flow_not_found`/`no_trigger_node`), `query_collection {bot_id,collection,filter?}` (reads through the engine `collectionStore.getBySlug`+`find` — reusing the SAME store the panel + `data.collection` node use; `collections_not_available` when unwired / `collection_not_found`), `send_message {bot_id,chat_id,text,parse_mode?}` (the bot's centralized rate-limited sender; `parse_mode` HTML/MarkdownV2 to MATCH the REST send surface; `bot_not_running`). **Error model:** tool-level problems (unknown bot/not-found/bad-args/bot-scope) set `isError:true` on the *result* (the call SUCCEEDED, the tool reports a failure — the MCP-correct shape); protocol problems (malformed JSON-RPC / unknown method / unknown tool) return a JSON-RPC `error` (`-32600`/`-32601`/`-32602`/`-32603`). **Wiring:** `v1.ts` mounts `registerMcpApi` inside its bearer-auth `scope` (passing `tokenBotId:(req)=>apiToken.botId` + `deps.collectionStore`) and gained a `collectionStore?` dep; `app.ts` passes `opts.engine.collectionStore`. No scheduler reconcile is wired here — no MCP tool toggles the ACTIVE flow set (create makes a draft, trigger runs an existing flow), so `onFlowsChanged` stays referenced for parity but is intentionally never fired. **Docs:** PROTOCOL.md gained a full "§MCP server — CTB as a tool surface" chapter (transport, auth, methods, the six tools, a Claude-Desktop quick-start) + the route-list line + the top capability-table row; PLAN2.md PC-T3 ✅. **Tests (`apps/server/test/mcp.test.ts`, +23):** a dedicated World that passes `sqlite` to `wireEngine` (so `query_collection` is live) with the fake Telegram transport + an `rpc()`/`callTool()` helper pair. Four describe blocks — handshake+auth (missing/garbage bearer → 401×2; `initialize` protocol/serverInfo/tools; `ping`→`{}`; notification→202-no-body; bad-JSON-RPC `-32600`; unknown-method `-32601`), `tools/list` (exactly the 6 sorted names, each object schema), `tools/call` (list_nodes deep-equal to the REST `nodeTypeInfos` catalog; validate good→ok / no-trigger→problems / malformed→isError; create persists a draft the REST `/validate` then reads `ok:true` — proving no drift; create unknown_bot; trigger spawns a real execution row visible via REST executions; trigger flow_not_found; send invokes the fake sender with the right chat/text; send bot_not_running; query reads 2 records; query where-filter narrows to 1; query collection_not_found; unknown-tool `-32602`), and a bot-scope sweep (a bot-A token gets `token_not_authorized_for_bot` on create/trigger/query/send against bot B, while list_nodes still works). **Verify:** all 6 packages typecheck GREEN package-by-package (the slow post-reset sandbox times out on a single whole-repo `npm run verify`/`typecheck`); `@ctb/server` suite GREEN **340/340** (was 317; +23). Additive server+shared+docs change → no node/registry/i18n touch → no guard bumps; other packages unchanged (shared 73 / sandbox 50 / nodes 504 / core 16 / editor 143). Next: PC-T4 (`tool.mcp` client node — but `ai.mcpClient` already exists from P5-T3, so verify whether PC-T4 is already satisfied before doing work). |
| 2026-06-18 | PC-T2 (flow authoring API) | **The open builder can now BUILD + ACTIVATE flows, not just read the catalog.** After the user said "continue", I ran the session-start protocol (STATE2 → PLAN2 PC-T2 → ARCHITECTURE I3/I5), confirmed PR #54 (PC-T1) MERGED (`origin/main` → `d816b33`), re-cut `genspark_ai_developer`, and built **PC-T2**: five flow-authoring routes promoted from the panel's internal `apps/server/src/api/flows.ts` onto the public bearer-auth v1 surface (`apps/server/src/api/v1.ts`). **Routes:** `POST /api/v1/flows` (create a draft — reuses the shared `CreateFlowBodySchema`, accepts a snake_case `bot_id` alias for REST friendliness, defaults to an empty graph + `defaultFlowSettings()`), `PATCH /api/v1/flows/:id` (edit name/graph/settings via `UpdateFlowBodySchema`; a graph change snapshots a new row into `flowVersions` exactly like the panel), `POST /api/v1/flows/:id/validate` (DRY-RUN — runs `validateFlowForActivation` and returns `problemStrings()`, saves nothing), `POST /…/activate` (validates first → `422 not_activatable` with the problem list, else flips to `active` + fires `onFlowsChanged`), and `POST /…/deactivate`. **Reuse-first (I5):** all five share the SAME shared Zod schemas + the SAME `validateFlowForActivation` + the SAME `paramSchemas`/`nodeMeta` maps the panel uses, so the public surface can never drift from the editor (a flow that activates via the API activates identically in the panel). The error-handler same-bot rule (`error_handler_self` / `error_handler_not_same_bot`) and `unknown_bot` checks are carried over verbatim. **Auth:** every route goes through `tokenAllowsBot(req, botId)` — a bot-scoped token gets `403` touching another bot's flow; `flow_not_found` is a `404`. **Wiring (`app.ts`):** added an `onFlowsChanged` dep → `opts.engine.scheduler.reconcile()` so an API activation reconciles the running scheduler just like a panel activation. **Tests +13** in `api-v1.test.ts` (two describe blocks; total file now 35): create + `bot_id` alias + empty-graph default, PATCH name/graph/settings + version snapshot, cross-bot `403` on create/patch, validate dry-run (a `noTriggerGraph()` helper added next to `triggerGraph()` returns a valid-but-unactivatable graph → problems but nothing saved), activate happy-path (active + reconcile fired) vs `422` with problems, deactivate, and `flow_not_found`/`unknown_bot` edges. PROTOCOL.md (route list + a new "Flow authoring (PC-T2)" subsection), PLAN2.md (PC-T2 ✅), and STATE2.md updated in the SAME commit. **Verify:** the slow post-reset sandbox times out on the whole-monorepo `npm run verify`/`typecheck`, so I ran them package-by-package — shared / sandbox / nodes / core / **server / editor all typecheck GREEN (exit 0)** — and the server suite separately: `cd apps/server && npx vitest run` → **39 files, 317 tests passed** (was 304; +13). Additive server+docs change → no node/registry/i18n touch → no guard bumps; other packages unchanged (shared 73 / sandbox 50 / nodes 504 / core 16 / editor 143). Next: PC-T3 (CTB as an MCP server). |
| 2026-06-18 | PC-T1 (node catalog API) + sandbox fix | **Phase C begins — the open builder surface.** After the user said "continue development", I ran the session-start protocol: STATE2 said Phase B was COMPLETE (PR #53 merged) and the next task is Phase C. STEP-4 verify caught a **broken sandbox** (not code): `@ctb/server`'s whole suite failed with "Could not locate the bindings file" — the `better-sqlite3` native addon was missing after a sandbox reset. `npm rebuild better-sqlite3` restored it and the full `npm run verify` went GREEN (shared 73 / sandbox 50 / nodes 504 / core 16 / server 298 / editor 143). Then I cut `genspark_ai_developer` from `origin/main` `1b10073` and built **PC-T1**: promoted the panel's internal `/api/node-types` to the public bearer-auth v1 surface as `GET /api/v1/node-types`. The implementation is deliberately tiny and reuse-first — `v1.ts` imports the existing `nodeTypeInfos(registry)` and serves it as a once-computed `nodeCatalogPayload` const inside the SAME bearer-auth scope as the other v1 routes, so the public catalog is the exact same projection the editor already gets (it can never advertise a node the engine can't run). No `tokenAllowsBot` gate — the node library is identical for every bot, so any valid token (instance-wide or bot-scoped) may read it. **6 new tests** in `api-v1.test.ts`: missing/garbage bearer → 401×2, valid → 200 with `nodeTypes` deep-equal to `nodeTypeInfos(engine.registry)` + a `tg.sendMessage` spot-check, the AI typed-slot surface (`ai.agent` `inputSlots` + `ai.modelOpenai` `role`/`provides`), a bot-scoped read, and byte-identical-to-internal. PROTOCOL.md + PLAN2.md + STATE2.md updated in the same commit. Verify GREEN; additive (no node/registry change → no guard bumps). Next: PC-T2 (flow authoring API). |
| 2026-06-18 | PB-T8 (Phase-B voice demo) | **🎬 The voice screenshot, rebuilt end-to-end — Phase B COMPLETE.** After the user merged PR #52 (Phase-E refinement, `origin/main` → `1018e72`), I synced local `main`, re-cut `genspark_ai_developer`, and built the last Phase-B task: a documented demo proving the whole AI tier in one run, from ONLY shipped nodes wired by the PB-T1 typed-slot contract. **Flow** (`packages/shared/test/fixtures/phase-b-voice-flow.json`, 9 nodes): `tg.trigger` → `ai.speechToText` (PB-T7 — it owns the `ctx.tg.getFile` voice download, no separate getFile node) → `ai.agent` (PB-T5) consuming FOUR providers via its typed slots — `ai.modelOpenai` (`ai:model`), `ai.memoryPostgres` (`ai:memory`), and `tool.think` + `tool.httpRequest` (`ai:tool` ×2, repeatable) — → `ai.textToSpeech` (PB-T7 — `ctx.ai.speech` → `ctx.files.write`) → `tg.sendMedia` (`source:'file'`, `kind:'audio'`). **Test** (`apps/server/test/e2e-phaseB-voice-demo.test.ts`, 2 tests) runs through the REAL `Executor` + `MemoryExecutionStore` + slot resolver + agent tool loop (like PA-T8) with fake scripted transports — a shared in-memory file store backing both `ctx.files.write` and `ctx.tg.getFile`, a scripted `ctx.ai` (transcribe/chat/speech), and a fake `ctx.db` honouring exactly the three statements the chat-memory runtime issues (CREATE/INSERT-pair/SELECT-window) with rows persisting across runs. **Test 1** (full loop) asserts: the voice `file_id` reached `getFile`; the downloaded bytes + the derived `voice_abc.oga` filename reached `transcribe`; the `ai:model` PROVIDER supplied `openai/gpt-4o-mini` + temp `0.3` (not the agent's inline params); BOTH tools were advertised; the scripted model called `order_status` and the host ran the REAL `ctx.http.request` (the arg merged into the query → `…/orders?orderId=A-17`); the reply was synthesized (`format:'opus'`), stored, and uploaded as `kind:'audio'` BYTES (the synthesized audio, asserted by decoding); AND the turn was persisted to the fake Postgres. **Test 2** (cross-call memory replay) runs a 2nd voice call in the same chat sharing the db, and asserts the 1st `user`/`assistant` exchange is replayed into the model prompt BEFORE the new question — proving the agent "remembers" across calls. **Debugging notes:** the trigger `event` must be `any_message` (not `message`); the chat-memory runtime binds via the `params` field (not `values`); a GET tool merges the model's args into the query string (so the URL stays a literal, args ride as `?orderId=`); and for `source:'file'` `tg.sendMedia` uploads BYTES (read host-side) not a `ref`. **Docs:** `docs/demos/phase-B-voice.md` walkthrough; PLAN2.md PB-T8 marked ✅ "Phase B COMPLETE". **Verify:** new e2e GREEN (2/2); `@ctb/server` typecheck GREEN; the change is additive (docs + fixture + one test) — no node/registry/i18n change, so no guard bumps and the rest of the suite is unaffected. **Phase B (PB-T1…PB-T8) is COMPLETE; the next code task is Phase C (open builder API / MCP surface).** |
| 2026-06-18 | Phase E refinement (settings-driven) | **Phase E rewritten so every technical fork is a NODE/CREDENTIAL SETTING, not a roadmap decision.** The user (non-technical) asked me to make the internal technical calls myself but to "put the choice on the nodes, not here" — i.e. instead of the roadmap committing to ONE path that locks out the others, build **multi-purpose nodes** whose behaviour the user picks from the node's own settings; and to support **companion/helper bot accounts added alongside our own bot in a group** (the `@myidbot` pattern). This is exactly CTB's own I2 (generic-only) philosophy applied to voice. I synced local `main` to the merged PR #51 (`5ad777c`), re-cut `genspark_ai_developer`, and refined Phase E in PLAN2.md: (1) the credential `telegramUserSession` → **`voiceConnection`** gains a **`kind` setting** (`userbot` shipped first; `companion`/`external` forward-shaped to cover the helper-account idea), so adding a companion account is a credential choice, not a new feature; (2) E.1 gained two rules — **"one interface, many adapters"** (the JS-native-vs-Python-sidecar media engine sits behind ONE `ctx.call` interface as a swappable INTERNAL adapter, never exposed to a flow) and **"behaviour = config"** (group vs 1:1 is the `target` setting; support-agent vs livestream-moderator is the `mode` setting `support`/`lineup`; turn order is the `order` setting `sequential`/`random`); (3) E.0 retitled "turned into SETTINGS, not forks"; (4) E.2 tasks PE-T1…PE-T5 rewritten as setting-driven; (5) **E.3 changed from "open questions to confirm with the user" to "implementation choices CTB makes internally (so the user doesn't have to)"** — directly honouring "calculate it yourself"; (6) the 3 Phase-E risk-register rows + sequencing note #6 updated to match. **Net effect:** building Phase E narrows nothing — picking `userbot` first doesn't block `companion`/`external` later, picking one media engine doesn't lock the interface, and both end-user scenarios come from the SAME nodes differing only in settings. STATE2.md updated (Phase/Roadmap lines, the What-exists Phase E row promoted to the settings-driven version, this log entry). **Verify:** docs-only change — no code touched, so the per-workspace build stays GREEN from PB-T7 (shared 73 / core 50 / nodes 504 / editor 143 / server 296). Still no PE-T* code until the user says to start Phase E; meanwhile PB-T8 (Phase-B demo) remains the next *code* task. |
| 2026-06-18 | Phase E design | **New pillar designed: live voice AI (real-time Telegram calls).** The user confirmed PR #50 (PB-T7 speech nodes) is MERGED (`origin/main` → `1bfb030`) and asked to formalize their new feature idea: a node connecting an **AI to LIVE Telegram voice conversations** — two scenarios, an **AI online voice-support agent** (answers callers in real time) and a **channel-livestream Q&A moderator** (grants listeners speaking turns, `sequential`/`random`, configurable, answers one by one). I synced local `main` to the merged remote, then RESEARCHED feasibility before designing: confirmed (Latenode May-2025 + Telegram docs) that **the Bot API exposes NO voice/video-call or group-voice-chat methods** — joining a call requires **MTProto via a userbot (a user account + session, NOT a bot token)** plus a native **WebRTC** media layer (`tgcalls`/`pytgcalls`/`ntgcalls`) streaming raw PCM. The user's "add the bot as admin" model is only partly right: admin rights manage the group call, but a **separate user session** carries the audio. **Design decision (fits CTB's invariants):** rather than a node that holds a socket open for the whole call (impossible in the stateless executor), the realtime loop lives in a host-side **Call Session Service** (a new long-lived runtime, sibling to the scheduler); a live call becomes discrete **events** that TRIGGER a flow (`trigger.callEvent`: callJoined / utteranceFinal / turnOpened / callLeft, with a `support` vs `lineup` moderation mode) and a few **actions** the flow invokes back (`call.speak` / `call.grantTurn` / `call.endTurn` / `call.mute` / `call.leave` via a new `ctx.call` capability). The native MTProto/WebRTC dep is isolated in `apps/server` behind that interface (I3) so `core`/`nodes` stay clean. **Crucially, Phase E REUSES PB-T5…PB-T7** (agent + STT + TTS) directly — it only adds the *transport* + *glue* + a `telegramUserSession` credential (encrypted, I7). Wrote the full **Phase E** into PLAN2.md: E.0 feasibility table, E.1 architecture diagram, E.2 tasks PE-T1…PE-T5, E.3 four open questions (userbot OK? Node-native vs Python sidecar? group-only v1 vs +1:1? pull earlier vs keep last?), plus 3 new rows in the risk register and a sequencing note. STATE2.md updated (Phase/Roadmap lines, the What-exists row promoted from CANDIDATE to a DESIGNED Phase E, Open PR cleared, this log entry). **No PE-T* code starts until the user answers E.3.** **Verify:** docs-only change — no code touched, so the per-workspace build stays GREEN from PB-T7 (shared 73 / core 50 / nodes 504 / editor 143 / server 296). Awaiting the user's sign-off on E.3 + whether to do PB-T8 (Phase-B demo) or Phase E next. |
| 2026-06-18 | PB-T7 | **Speech nodes — the bot learns to hear and speak.** PB-T7 ships the transcribe + TTS legs of the voice flow (the PLAN2 screenshot) as two plain DATA nodes (NOT providers — they carry `main` in/out and run in the data stream). Both choose an OpenAI-compatible provider via the existing `openAiApi` credential, resolved host-side (I7 — the node only passes a `credentialId`). **The architectural choice:** rather than have the nodes do HTTP themselves, I added two NEW *optional* capabilities to `ctx.ai` — `transcribe?(req)` and `speech?(req)` — mirroring the existing `ctx.ai.chat`. They are nullable/optional so an older host (or a unit test) that doesn't wire them makes the node fail loudly with a clear message (I6 — no ambient authority). Shared also gained the four request/result types `AiTranscribeRequest`/`AiTranscribeResult` + `AiSpeechRequest`/`AiSpeechResult` in `node-def.ts`. **`ai.speechToText`:** `source` selects how the audio is found — `telegram` (default; `audio_source` is a Telegram `file_id` the host downloads via `ctx.tg.getFile`, e.g. `{{ $json.message.voice.file_id }}`) or `file` (a CTB file id read via `ctx.files.read`). It derives a filename (the basename of the Telegram `file_path`, or `audio.<ext>` from the MIME), then calls `ctx.ai.transcribe` and merges `{text, language?, duration?}` onto every item under `save_as` (default `transcript`). Optional `language` (ISO-639-1) + `prompt` bias the decoding (blank values are omitted). **`ai.textToSpeech`:** calls `ctx.ai.speech` with `text`/`voice`/`format` (`mp3|opus|aac|flac|wav|pcm` — `opus` for Telegram voice notes) + optional `speed`, then STORES the returned bytes via `ctx.files.write` and merges `{fileId, mime, size, url}` under `save_as` (default `speech`); feed `fileId` to `tg.sendMedia` `source:'file'`. Both nodes run ONCE per node run (a speech call is expensive external work) and fail loud on absent capability / empty audio / download error / provider error. **Server (`engine/wire.ts`, I3):** refactored `makeAi` to share a `resolveOpenAi(credentialId)` helper (decrypt → assert `openAiApi` → `{baseUrl, apiKey}`), then added `transcribe` (multipart `POST {base}/audio/transcriptions` built from a `FormData`/`Blob`, `response_format:verbose_json`, 120s abort; `parseTranscription` reads text/language/duration, falling back to raw text) and `speech` (`POST {base}/audio/speech` JSON, reads `arrayBuffer()` → bytes + `speechMime(format)`); the decrypted key never leaves the host (I7). Registered the two nodes in `index.ts` (registry 51→53). **Editor:** fa/en i18n (parity compile-enforced) — `nodes.ai.{speechToText,textToSpeech}.label` + `nodeDesc.ai.{…}` + the full `param/paramDesc.ai.{speechToText,textToSpeech}.*` families; the namespaced keys satisfy the i18n-namespace parity test. **Docs:** NODES.md §AI gained a "Speech nodes" subsection. **Tests:** NEW `ai-speech-nodes.test.ts` (20) — registration + the data-node contract (main in/out, no provider role) + schema defaults; for STT: telegram-download (asserts the file_id reached `getFile`, the bytes + derived `file_3.oga` filename + mime reached `transcribe`, and the result merged), file-store read (`source:file`, `audio.mp3` from mime), language/prompt forwarding + blank-omission, multi-item fan-out (one call, result on every item), and fail-loud for absent `ctx.ai` / absent `ctx.ai.transcribe` / download error / provider error / empty audio; for TTS: synthesize→store→surface (asserts credential/model/input/voice/format reached `speech`, bytes reached `files.write`, `{fileId,mime,size}` merged), format+speed plumbing, multi-item fan-out (stored once, shared id), and fail-loud for absent `ctx.ai.speech` / no file store / provider error / empty audio. The node-harness gained `transcribe`/`speech` mocks driven by `transcribeResult`/`speechResult` (object | function | `{error}`) + `dropTranscribe`/`dropSpeech` toggles + `transcribeCalls`/`speechCalls` recorders. All 15 registry-count guards bumped 51→53. **Verify:** per-workspace GREEN — shared 73, core 50, nodes 504 (was 484: +20), editor 143 (i18n parity), server 296. **Recovery note:** the sandbox was reset mid-PB-T7; all edits to existing files (shared types/schemas, index.ts, wire.ts, i18n, docs, test guards, node-harness mocks) survived in a user-provided backup (`fa7c734` base on `genspark_ai_developer`), but the THREE new files — `ai/speech-to-text.ts`, `ai/text-to-speech.ts`, `ai-speech-nodes.test.ts` — had not yet been flushed to disk and were re-created from the recorded contract; verification re-run GREEN. **Also this session:** rebased the branch onto `origin/main` (= `fa7c734`) after the user MERGED PR #49 (PB-T6). The user ALSO requested a NEW feature — AI ⇄ live Telegram voice calls (group/channel video-chats + private calls; an AI voice-support agent + a livestream Q&A turn-taking moderator) — recorded as a CANDIDATE in What-exists (feasibility: needs MTProto + tgcalls, not the Bot API → proposed as a dedicated Phase C track, pending the user's confirmation before implementation). Per the standing instruction, continuing into PB-T8 (Phase-B demo) after this commit + PR. |
| 2026-06-18 | PB-T6 | **AI Agent tool nodes — the agent's `ai:tool` slot gets real, runnable tools.** PB-T5 made `ai.agent` a consumer that DECLARES an `ai:tool` slot but, lacking dedicated tool nodes, could only accept providers whose params already looked like an inline `AgentToolSource`. PB-T6 ships the four real tool *provider* sub-nodes (`role:'provider'`, `provides:'ai:tool'`, no data ports, one dashed `provider` wire) and teaches the agent to turn each into a callable tool DIRECTLY. **The contract:** every tool node carries a `tool_name` + `description` (what the model reads to pick a tool) plus, for three of them, a `params` table of typed argument rows (`{name, type, description, required}`) that `buildToolParameters` compiles into the tool's JSON-Schema `parameters` (no rows → an open object). **The four nodes:** `tool.httpRequest` — the model calls an external API; the author fixes method/url/headers/credential, and at call time `buildHttpTool` merges the model's args into the QUERY (GET/HEAD) or a JSON BODY (other methods, over the optional body template), runs `ctx.http.request` (host-limited, I6), injects credential auth headers host-side (I7), and feeds `{statusCode, body}` back. `tool.code` — sandboxed JS via `ctx.code.run` with the model's args as `$json` and the program's `return` as the result, under the same 10s cap as `data.code`; `code` is `rawParamKeys` so `{{ }}` reaches the sandbox verbatim (DL #16). `tool.think` — the PLAN2-screenshot "Think" tool: a NO-OP scratchpad whose runner just echoes the model's `thought` back (needs no capability; measurably improves multi-step reasoning). `tool.subflow` — the n8n "Workflow Tool": another flow of the same bot exposed as one tool via `ctx.subflow.run` (same-bot + depth guards, P3-T1); a blank `tool_name` is derived as `flow_<id>`. **Agent wiring (`ai/agent.ts`):** new `resolveSlotTools` walks the `ai:tool` providers, routes each `tool.*` type through `buildSlotTool` → a per-type builder returning a `ResolvedTool {spec, run}`, dedupes by tool name (first wins, warns on a dup); `toolSourcesFromSlots` now SKIPS the `tool.*` types (handled directly) while keeping the forward-compat mcp/subflow-shape path; the execute() merges `[...inline tools, ...resolved sources, ...slot tools]`. Tool errors during a run stay `error: …` strings fed back to the model (never a node failure) so the agent can recover. **Shared (I5):** `ToolNameSchema`, `ToolParamRowSchema`, and `Tool{HttpRequest,Code,Think,Subflow}ParamsSchema` in `node-params.ts`. Registered all four in `index.ts` (registry 47→51). **Editor:** fa/en i18n (parity compile-enforced) — `nodes.tool.*.label` + `nodeDesc.tool.*` + the full `param/paramDesc.tool.*.*` families; the namespaced keys satisfy the i18n-namespace parity test. **Docs:** NODES.md §AI gained an "AI Agent tool nodes" subsection and the `ai:tool` slot note was updated. **Tests:** NEW `ai-tool-nodes.test.ts` (21) covers registration + the provider contract (role/provides/empty-inputs/`provider` output, fail-loud execute, `tool.code` rawParamKeys), the param schemas (required fields, name validation, bad identifier rejection), and END-TO-END runners driven through the REAL `ai.agent` with a scripted model: http GET sends the arg as a query param + feeds the body back; http POST sends a JSON body + injects credential headers; code runs in the sandbox and returns the sum; think echoes the thought and touches no capability; subflow runs the child + derives `flow_abc-123`; and a multi-tool case advertises several tools and drops a duplicate name with a warn. Server `node-types.test.ts` back-compat guard extended to skip the four providers + a new positive assertion that they surface `role:provider`/`provides:ai:tool`/`inputs:[]`/`outputs:[provider]`. All 15 registry-count guards bumped 47→51. **Verify:** per-workspace GREEN — shared/core/nodes/editor/server typecheck clean; nodes 484 (was 463: +21), editor 143 (i18n parity), server 295. Per the standing instruction, continuing straight into PB-T7 (speech nodes) after this commit + PR update. |
| 2026-06-18 | PB-T5 | **`ai.agent` orchestrator with typed slots + the `ai.modelOpenai` model provider — the AI sub-node canvas comes alive.** PB-T1 built the *contract* for typed sub-connections (a consumer's `inputSlots`, a provider's `role`/`provides`), PB-T4 shipped the first concrete providers (chat memory). PB-T5 closes the loop: it evolves the existing `ai.agent` (the P5-T4 tool-calling loop) into a real **consumer** that DECLARES `inputSlots` — `ai:model` (required-shape), `ai:memory` (optional), `ai:tool` (repeatable) — and CONSUMES the providers attached to them, plus it adds the first model provider so the required `ai:model` slot can actually be filled. The mechanism is a NEW capability `ctx.slots: AttachedProviders` (`Partial<Record<SlotKind, AttachedProvider[]>>`, where `AttachedProvider = {nodeId, type, params}`). The executor resolves it in a new private `resolveSlots(exec, graph, node, def)`: for a consumer that declares slots, it walks `graph.edges`, finds edges whose `to.node` is the consumer and whose `to.port` is a declared slot kind, looks up the source node, confirms it is a `role:'provider'` that `provides` that exact kind, validates the provider's params against its OWN schema (exactly like a data node is parsed), and groups them under the slot kind in graph order — skipping disabled / unknown-type providers and logging a warn on invalid params (a misconfigured provider must not silently vanish). `executeNode` now threads the `graph` through and calls `buildCtx(…, slots)`; `buildCtx` gained a `slots` param defaulting to `{}`, so every Phase-A node (which declares no slots) gets an empty map and is byte-for-byte unchanged. **The agent's three slot behaviours, each backward-compatible:** (1) **model** — `resolveModel` reads `ctx.slots['ai:model'][0]` (the new `ai.modelOpenai` provider's validated `credentialId`/`model`/`temperature`/`max_tokens`); when no model slot is attached it falls back to the agent's inline `credentialId`/`model` params (so every existing P5-T4 agent flow keeps working), and with NEITHER source it fails loudly (`no model: attach an ai:model provider or set an inline credential`). To make the slot the primary path, `AiAgentParamsSchema.credentialId` was relaxed from `.min(1)` to `.default('')` (optional), with the run-time `resolveModel` now enforcing presence. (2) **memory** — `resolveMemory` turns an attached `ai:memory` provider (`ai.memoryKv` or `ai.memoryPostgres`) into a `ChatMemoryConfig` and drives the PB-T4 shared runtime: `loadChatHistory` replays the rolling window BEFORE the first model turn (seeded into `messages` between the system prompt and the user turn) and `appendChatTurn` persists the new user+assistant pair AFTER the final answer (best-effort — a store hiccup is logged, never loses the reply already produced). A blank provider `session_key` defaults to `<nodeId>:<chatId>` (per-node, per-chat isolation, matching the old `ai.llmChat` convention). No memory slot → stateless, exactly as before. (3) **tools** — `toolSourcesFromSlots` converts attached `ai:tool` providers into `AgentToolSource`s and merges them with the inline `tools` param through the SAME `resolveTools` path; since the dedicated tool *nodes* land in PB-T6, a provider whose validated params already match an `AgentToolSource` (`{type:'mcp'|'subflow', …}`) is accepted and anything else is skipped with a warn (forward-compatible, never crashes the agent). **NEW `ai.modelOpenai` provider** (`packages/nodes/src/ai/model-openai.ts`): `role:'provider'`, `provides:'ai:model'`, `ports {inputs:[], outputs:['provider']}`, fail-loud `execute()` (a provider is resolved as config, never run); its `AiModelOpenaiParamsSchema` (credentialId `openAiApi`, model default `gpt-4o-mini`, optional temperature 0–2 / max_tokens) is the n8n "OpenAI Chat Model" sub-node — it never touches the network, it only tells the agent which model + credential to use; the agent calls the LLM through `ctx.ai` (I3/I6/I7). Registered in `index.ts` (registry 46→47). **Editor:** fa/en i18n (parity compile-enforced) — `nodes.ai.modelOpenai.label`, `nodeDesc.ai.modelOpenai`, and `param/paramDesc.ai.modelOpenai.{credentialId,model,temperature,max_tokens}`. **Tests:** NEW `ai-model-openai.test.ts` (7 — provider registration/contract + schema defaults/validation) and a new slots block in `ai-agent.test.ts` (model slot wins over inline / inline fallback / no-model fail-loud / KV memory replay+persist / per-node+chat default key when session_key blank / stateless without a memory slot / subflow tool via the `ai:tool` slot + skip-unknown-provider-with-warn) plus an `inputSlots` assertion; the server `node-types.test.ts` back-compat guard updated to exclude the new provider + the agent's slots, with two added assertions surfacing them. All registry-count guards bumped 46→47. **Verify:** per-workspace GREEN — shared 73, core 50, nodes 463, editor 143 (incl. i18n parity), server 295. **Note:** the sandbox was reset mid-task; state (commit `e0e6468` + all WIP) was restored from a user-provided backup and `ai/model-openai.ts` (the one untracked file) recreated, then PB-T5 finished. |
| 2026-06-17 | PB-T4 | **Chat-memory providers — `ai.memoryKv` (default) + `ai.memoryPostgres`; Phase B's first concrete *provider* sub-nodes.** PB-T1 added the typed sub-connection *contract* (a consumer's `ai:memory` slot, a provider's `role:'provider'`/`provides`) but shipped no real provider — these two are the first. They are the n8n "Chat Memory" nodes, kept strictly generic (I2 — memory is a capability, never a domain concept): a flow author attaches one to an AI Agent's `ai:memory` slot (a dashed sub-connection) and the Agent gains a **rolling conversation memory**. The crucial design point: **a provider is never executed as a data step** — the PB-T1 executor already skips `role:'provider'` nodes, so a provider's *params* ARE its entire contract. The consumer (the AI Agent, landing in **PB-T5**) reads the attached provider's resolved params, turns them into a `ChatMemoryConfig`, and drives a shared runtime. That runtime is the heart of this task: NEW `packages/shared/src/chat-memory.ts` exposes `loadChatHistory(cfg, {kv, db})` (replay the rolling last-N turns before a model call) and `appendChatTurn(cfg, {kv, db}, {user, assistant})` (persist the new pair after), plus `kvMemoryKey()`, `quotePgIdent()` and `toAiMessages()`. It touches the world ONLY through the injected `ctx.kv`/`ctx.db` capabilities (I3/I6 — it imports no database driver and never sees a decrypted secret; the host resolves `credentialId`→pool), and it is the SAME logic `ai.llmChat`'s `memory:'conversation'` already used, now generalized. **Two backings, one contract:** `ai.memoryKv` (the DEFAULT) persists the trimmed window as a single JSON value in KV (`scope:'user'`, key `__ai_mem__:<session>`), so a bot with **no database** still remembers — params `session_key` (blank → keyed per node+chat) + `memory_window` (turns, default 10). `ai.memoryPostgres` persists turns as rows via `ctx.db`: `loadChatHistory` issues a `SELECT role, content FROM (… ORDER BY id DESC LIMIT $2) ORDER BY id ASC` (newest window, replayed oldest-first), `appendChatTurn` writes the pair as **two bound rows in one `INSERT … VALUES ($1,$2,$3),($1,$4,$5)`** — params `credentialId` (a `postgres` credential), `table` (default `ctb_chat_memory`, validated as a SQL identifier), `session_key`, `memory_window`, `auto_create` (issues `CREATE TABLE IF NOT EXISTS` before first use). **SQL-injection safety carried over from PB-T2:** every value is a bound `$N` parameter (a test fires `'; DROP TABLE users; --` as the user message and asserts it stays a single param, never in the SQL text); the only interpolated text is the table identifier, validated per dot-segment against `/^[A-Za-z_][A-Za-z0-9_]*$/` and double-quoted by `quotePgIdent` (a hostile or backtick-bearing table throws loudly). Both paths fail LOUDLY when the needed capability is absent (`ctx.kv` for kv, `ctx.db` for postgres). **Shared (I5):** `chat-memory.ts` (runtime + `ChatMemoryConfig` discriminated union + `ChatMemoryMessage`) exported from the index; `AiMemoryKvParamsSchema`/`AiMemoryPostgresParamsSchema` + `ChatMemoryWindowSchema` in `node-params.ts`. **Nodes:** `ai/memory-kv.ts` + `ai/memory-postgres.ts` — both `role:'provider'`, `provides:'ai:memory'`, `ports {inputs:[], outputs:['provider']}`, with an `execute()` that fails loud (defensive — providers are config, never run) — registered in `index.ts`. **Editor:** fa/en i18n in parity (compile-enforced; `i18n-namespace.test.ts`): `nodes.ai.memory{Kv,Postgres}.label` + `nodeDesc.ai.memory{Kv,Postgres}` + the full `param.ai.memory{Kv,Postgres}.*`/`paramDesc.*` families; the `credentialRef` widget already filters `ai.memoryPostgres` to `postgres` credentials, so no widget change. NODES.md §AI gained a "Chat memory providers (PB-T4)" subsection. **Gotcha (carried from PB-T1):** `NodeTypeSchema` is EXACTLY two dot-parts (`/^[a-z][a-zA-Z0-9]*\.[a-z][a-zA-Z0-9]*$/`), so the *registered* types are `ai.memoryKv`/`ai.memoryPostgres` (camelCase second segment, like `ai.llmChat`) even though the docs/PLAN refer to them conceptually as `ai.memory.kv`/`ai.memory.postgres`. Registry **44→46** types; all 13 nodes-registry-count guards bumped 44→46. **Server back-compat fix:** `node-types.test.ts`'s "omits role/inputSlots/provides for plain data nodes" guard (written when EVERY builtin was `role:'data'`) now skips the two memory providers, and a NEW sibling test asserts they DO surface `role:'provider'` + `provides:'ai:memory'` + `inputs:[]`/`outputs:['provider']` over the wire — confirming PB-T1's conditional `node-types.ts` emission works for a real provider, not just a synthetic test fixture. **Tests (+18):** nodes `ai-memory.test.ts` (17 — registry-46; both register as `ai:memory` providers with no data input; provider `execute` fails loud; param defaults + hostile-table rejection in the schema; KV runtime: fresh→empty, append+replay newest-last + stored under the namespaced key, window×2 trim drops the oldest turn, corrupt stored value ignored not thrown, KV-absent throws; Postgres runtime: auto-create THEN windowed `SELECT` bound by `$1/$2` (=`[session, window*2]`), rows mapped oldest-first with `autoCreate:false` skipping the CREATE, append as two bound rows with the hostile value staying a single param + no `DROP TABLE` in the SQL, `quotePgIdent('app.turns')='"app"."turns"'` + rejects `x; DROP TABLE y`, db-absent throws, `toAiMessages` shape) + server `node-types.test.ts` (+1). **`npm run verify` GREEN** (run per-workspace; full script times out at 120s in this sandbox — typecheck all 6 workspaces clean; shared 73 / core 50 / sandbox 16 / nodes 447 / server 293 / editor 143 incl. i18n parity). Next: PB-T5 (`ai.agent` orchestrator — consume the `ai:model`/`ai:memory`/`ai:tool` slots, wiring the memory providers shipped here into the agent loop). |
| 2026-06-17 | PB-T3 | **`db.mysql` — a generic MySQL / MariaDB database node; Phase B's second DB connector.** The exact mirror of PB-T2's `db.postgres`, so a flow author who learned the Postgres node already knows this one — it shares the SAME injected `ctx.db` capability, the same `query`/`select`/`insert`/`update`/`delete` operations, the same `return_mode`/`confirm_many`/`save_as` semantics, the same "values are ALWAYS bound, never concatenated" injection-safety rule, and the same "runs ONCE per node run, fails LOUDLY when `ctx.db` is null" contract. Kept strictly generic (I2 — "MySQL" is infrastructure, never a domain concept) and the `mysql2` driver + connection POOL live ONLY in `apps/server` (I3 — `core`/`nodes` import no DB driver). The connection comes from a NEW `mysql` credential (host/port[default 3306]/database/user/password/ssl) encrypted at rest (I7); the node only carries a `credentialId`. **Three real dialect differences from Postgres, each handled deliberately:** (1) placeholders are positional `?` rather than numbered `$1,$2,…` — so `buildWhere` no longer threads an index, the values-array order *is* the placeholder order; (2) identifiers are **backtick**-quoted (`` `col` ``) not double-quoted, while the same strict `/^[A-Za-z_][A-Za-z0-9_]*$/` per-dot-segment validation guards against injection (a test fires a backtick-bearing AND a `; DROP TABLE` identifier and asserts both throw, while a hostile *value* stays a single bound `?` parameter); (3) MySQL has **no `RETURNING *`** — a write returns the driver's OK packet, so the node emits no RETURNING and the host's `mysqlPoolFactory` normalizes the OK packet to a synthetic row `{affectedRows, insertId?}` so the `rows`/`single` return modes still behave like Postgres'. **Dialect routing:** `DbQueryRequest` gained an optional `dialect?: 'postgres'|'mysql'` (defaults `postgres`, so the PB-T2 node and its tests are byte-for-byte unchanged); the node sends `dialect:'mysql'`, and the host's `makeDb()` became dialect-aware (now a 4-arg `makeDb(db, key, pgFactory, mysqlFactory)`) — `resolvePool(credentialId, dialect)` decrypts the credential, THROWS if `data.type !== dialect` (so a flow can't aim a Postgres node at a MySQL credential or vice-versa — "credential … is not a {MySQL|Postgres} credential"), picks the matching factory, and caches one pool per credentialId. `mysqlPoolFactory` lazily `createRequire(import.meta.url)`-loads `mysql2` (apps/server is pure ESM — a bare `require` is absent at runtime), wraps its callback pool via `.promise()`, and adapts `pool.query → [rows|okPacket, fields]` to the `DbPool` contract (SELECT → the row array; a write → the normalized single row). It is INJECTABLE via `WireOptions.mysqlPoolFactory`, so the server e2e tests pass a recording fake (and assert the pg factory is NEVER called for a mysql credential) — no real MySQL needed. **Shared (I5):** `MysqlSchema` added to the `CredentialDataSchema` discriminatedUnion (and `CredentialTypeSchema` enum / `CREDENTIAL_TYPE_LABELS` / `credentialHint()` / `credentialAuthHeaders()` all kept exhaustive — each was a TS compile error until handled); `DbMysqlParamsSchema`/`DbMysqlParams` mirror `DbPostgres` but stamp `credentialType:'mysql'` on the `credentialRef` widget and reuse the shared `DbOperationSchema`/`DbWhereRowSchema`/`DbValueRowSchema`. **Nodes:** `packages/nodes/src/db/mysql.ts` + registered in `index.ts`. **Server:** `mysql2 ^3.15.4` runtime dep (3.22.5 installed) — justified per CLAUDE.md §6: a generic MySQL node cannot speak the wire protocol without the official driver, and `mysql2` ships its own TypeScript types so no `@types` is needed. **Editor:** `CredentialsPage` gained a `mysql` credential form (host/port/database/user/password/ssl, blank fields omitted); the `credentialRef` widget already filters options by the param's `credentialType` meta, so the node automatically offers ONLY `mysql` credentials with no widget change; fa/en i18n added in parity (compile-enforced; `i18n-namespace.test.ts` asserts fa/en carry the same namespaced override-key set): `credentials.type.mysql` + `credentials.field.my{Host,Port,Database,User,Password,Ssl}` + `nodes.db.mysql.label` + `nodeDesc.db.mysql` + the full `param.db.mysql.*` / `paramDesc.db.mysql.*` families + `option.db.mysql.{operation,order_dir,return_mode}.*`. NODES.md §Database connectors §MySQL filled in (was a planned stub). Registry **43→44** types; all 12 registry-count guards (the 11 from PB-T2 + `db-postgres.test.ts` itself) bumped 43→44. **Tests (+36):** nodes `db-mysql.test.ts` (31 — mirrors db-postgres at registry-44: schema validation; `query` parameterized forward + `dialect:'mysql'` asserted / blank-params→[] / non-array rejected; `select` SELECT * with backtick idents + WHERE/ORDER BY/LIMIT + positional `?` + in/is_null; `insert`/`update`/`delete` with `?` and NO RETURNING; confirm_many guard single-eq-allowed/broad-refused/confirmed-allowed; identifier safety unsafe+backtick-bearing table/column fail, hostile value stays bound; return_mode=single merge + custom save_as + preserves binary; runs-once; ctx.db-null; transport-error) + server `db-mysql.test.ts` (5 e2e via the wired engine with an injected fake `mysqlPoolFactory`: routes to the mysql factory not pg + runs the `?`-parameterized query (asserts cfg.host/port/database/user/password/ssl + sql+params verbatim + rows on output + password absent); backtick SELECT cached per credentialId (run twice → 1 pool, 2 queries); a write OK-packet row `{insertId,affectedRows}` surfaces on the node output with NO RETURNING in the SQL; `closeDbPools` drains every pool; a non-mysql (postgres) credential is rejected with "not a MySQL credential" before any pool is built). **`npm run verify` GREEN** (run as per-workspace passes since the full script times out at 120s in this sandbox — typecheck all 6 workspaces clean; shared 73 / core 50 / sandbox 16 / nodes 430 / server 292 / editor 143 incl. the i18n-parity tests). **Recovery note:** this task survived a mid-task sandbox reset (credits/freeze) — the workspace was restored from a tar backup that captured all committed + uncommitted PB-T3 work EXCEPT the three then-untracked files (`packages/nodes/src/db/mysql.ts`, `packages/nodes/test/db-mysql.test.ts`, `apps/server/test/db-mysql.test.ts`), which were re-created verbatim from session memory, `npm install` re-ran (mysql2 3.22.5), and the full suite re-verified green before commit. Next: PB-T4 (Postgres/KV chat-memory providers — `ai.memory.postgres` + a default `ai.memory.kv`). |
| 2026-06-17 | PB-T2 | **`db.postgres` — a generic Postgres database node; Phase B's first DB connector.** n8n has a "Postgres" node; CTB needs the same, kept strictly generic (I2 — "Postgres" is infrastructure, never a domain concept). The node speaks SQL through a NEW injected `ctx.db` capability and the actual `pg` driver + connection POOL live ONLY in `apps/server` — `core` and `nodes` never import a database driver (I3, the same host-injects-capability pattern as `ctx.ai`/`ctx.mcp`/`ctx.files`). The connection comes from a NEW `postgres` credential (host/port/database/user/password/ssl) encrypted at rest (I7); the node only ever carries a `credentialId` and the host resolves it to a decrypted secret + a pooled connection — the password never reaches node code or the flow JSON. **SQL-injection safety is the heart of the node:** values are ALWAYS bound as `$1,$2,…` parameters and never string-concatenated into the SQL; the `query` operation passes the author's SQL + a parsed params array straight through, while the `select`/`insert`/`update`/`delete` helpers BUILD the statement — table/column identifiers are validated against `/^[A-Za-z_][A-Za-z0-9_]*$/` (per dot-segment) and double-quoted by `quoteIdent` (a hostile identifier throws loudly), and every WHERE/SET/VALUES value is threaded through `buildWhere`/`buildStatement` as a bound placeholder. A test fires a hostile value (`'; DROP TABLE …`) and asserts it stays a single bound parameter, never reaching the SQL text. `return_mode` chooses rows (one output item per result row) or single (`{rows,rowCount}` merged under `save_as`); `confirm_many` is a guard rail on a multi-row UPDATE/DELETE (a single-equality WHERE is allowed without it, a broad one is refused unless `confirm_many` is set). The node runs ONCE per node run (a query shouldn't multiply by item count) and fails LOUDLY when `ctx.db` is null (host didn't inject it). **Shared (I5):** `PostgresSchema` added to the `CredentialDataSchema` discriminatedUnion (and the `CredentialTypeSchema` enum / `CREDENTIAL_TYPE_LABELS` / `credentialHint()` / `credentialAuthHeaders()` all kept exhaustive — each was a TS compile error until handled), `NodeCtx.db` capability + `DbQueryRequest`/`DbQueryResult` types, and `DbWhereOpSchema`/`DbWhereRowSchema`/`DbValueRowSchema`/`DbOperationSchema`/`DbPostgresParamsSchema` in `node-params.ts`. **Core:** `ExecutorServices.db` (optional → `ctx.db` null without it) + `buildCtx` wiring. **Server (`wire.ts`):** `makeDb(db, key, poolFactory)` owns a per-`credentialId` pool cache — `resolvePool` decrypts the `postgres` credential, THROWS if the credential isn't `postgres`-typed (a misconfigured flow fails before any pool is built), and caches the pool so repeated runs reuse one connection pool; `closeAll()` drains every pool via `Promise.allSettled(end())`. The actual `pg.Pool` is built by `pgPoolFactory`, which lazily `createRequire(import.meta.url)`-loads `pg` (apps/server is `"type":"module"` with `module:ESNext`, so a bare `require` is absent at runtime — `createRequire` is the sanctioned ESM→CJS bridge). The factory is INJECTABLE via `WireOptions.dbPoolFactory`, so the server e2e tests supply a fake pool that records the connection config + every query + whether it was ended — no real Postgres needed. `Engine.closeDbPools()` is exposed and `main.ts` calls it in graceful shutdown (after the sandbox pool). New runtime dep `pg ^8.21.0` (+ `@types/pg ^8.15.0` devDep) — justified per CLAUDE.md §6: a generic Postgres node cannot speak the wire protocol without the official driver, and a hand-rolled protocol implementation would be both unsafe and far larger than the dependency. **Editor:** `CredentialsPage` gained a `postgres` credential form (host/port/database/user/password/ssl, blank fields omitted from the saved data); the `credentialRef` widget already filters its options by the param's `credentialType` meta, so the node automatically offers ONLY `postgres` credentials with no widget change; fa/en i18n added in parity (compile-enforced — a missing en key is a COMPILE error, and `i18n-namespace.test.ts` asserts fa/en carry the same namespaced override-key set): `credentials.type.postgres` + `credentials.field.pg{Host,Port,Database,User,Password,Ssl}` + `nodes.db.postgres.label` + `nodeDesc.db.postgres` + the full `param.db.postgres.*` / `paramDesc.db.postgres.*` families + `option.db.postgres.{operation,order_dir,return_mode}.*`. NODES.md gained a "Database connectors" section (Postgres spec + a planned MySQL stub). Registry **42→43** types; all 11 registry-count guards bumped 42→43. **Tests (+34):** nodes `db-postgres.test.ts` (30 — registry-43 + data/main→main ports; schema validation; `query` parameterized forward / blank-params→[] / non-array params rejected; `select` SELECT * with WHERE/ORDER BY/LIMIT + placeholder threading + in/is_null; `insert` INSERT…RETURNING * bound; `update` placeholder order / single-eq allowed / broad-where refused without confirm_many / allowed with; `delete` DELETE…RETURNING * / multi-condition refused; identifier safety: unsafe table/column fail, hostile value stays bound; return_mode=single merge + custom save_as + preserves binary; runs-once; ctx.db-null fails; transport-error surfaces) + server `db-postgres.test.ts` (4 — e2e via the wired engine with an injected fake `DbPoolFactory`: builds a pool from the decrypted credential + runs a parameterized query (asserts cfg.host/database/user/password/ssl + sql+params verbatim + rows on output + password absent from the response); SELECT helper + pool cached per credentialId (run twice → 1 pool, 2 queries); `closeDbPools` drains every pool; rejects a non-postgres credential before building a pool). The 11 bumped registry guards: ai-agent, ai-classify-extract, ai-mcp-client, ai-nodes, collection-node, data-date-time, data-edit-fields, data-filter, data-sort-limit-dedupe, data-split-aggregate, user-profile-node. **`npm run verify` GREEN** (the full `verify` script times out at 120s in this sandbox, so it was run as separate per-workspace passes — typecheck all 6 workspaces clean; nodes 399 / server 287 / editor 143 incl. the 8 i18n-parity tests; core/shared/sandbox clean). Next: PB-T3 (the next Phase-B item per PLAN2.md — Postgres/KV chat-memory providers / ai.agent sub-nodes / speech). |
| 2026-06-17 | PB-T1 | **Typed sub-connection contract — Phase B begins.** Built the foundation that lets "provider" sub-nodes (a Chat Model, a Memory, Tools) ATTACH to a consumer's typed input SLOT, modelled distinctly from a data edge — the n8n "AI" canvas pattern (model/memory/tool plugged in *below* a node), kept fully generic (I2; no domain named). The key insight that made it cheap: a sub-connection is just an ordinary `FlowEdge` whose `to.port` **is** the slot kind. Since `PortNameSchema` already allows `:`, `ai:model`/`ai:memory`/`ai:tool` are valid port names, so the existing FlowGraph schema, dangling-edge refinement, executor routing, and editor handle plumbing all keep working unchanged — a slot is "just another input handle" that happens to be type-checked. **Shared (I5):** added `SlotKindSchema`/`InputSlotSchema`(`{kind,required,repeatable}`)/`NodeRoleSchema`(`'data'`\|`'provider'`) to `node-def.ts`, and three OPTIONAL fields on `NodeDef` (`role?`, `inputSlots?`, `provides?`) mirrored onto `NodeTypeInfo` in `api.ts`. **Validation** (`flow-validate.ts`): new `NodeSlotMeta` + `validateSubConnections` enforce the provider-port rules — a slot edge must come from a provider of the SAME kind, a provider may attach ONLY to a matching slot (never a data port), a `required` slot must be connected, a non-`repeatable` slot accepts ≤1 provider, disabled nodes are ignored, and a provider never counts as a trigger. Crucially these rules fire ONLY when an OPTIONAL 3rd `nodeMeta` arg is passed to `validateFlowForActivation`, so every existing Phase-A activation is byte-for-byte unchanged. **Server:** `node-types.ts` now conditionally emits `role`/`inputSlots`/`provides` (OMITTED entirely for plain data nodes — `role:'data'` is the absent default — so the palette payload for every current node is identical to before); `flows.ts` builds a registry-derived `nodeMeta` map (constructed with omitted keys, not `undefined`, to satisfy `exactOptionalPropertyTypes`) and passes it to activation. **Executor:** an `isProviderNode` guard — if a malformed graph ever parks the cursor on a provider, the executor ends that branch quietly (emits nothing) instead of calling its `execute()`; a correctly-wired graph never routes data into one anyway. **Editor:** `canConnect` gained the same slot/role rules as three new verdict reasons (and I ordered the duplicate-edge check FIRST so re-wiring the identical edge reads as `duplicate`, not `slotNotRepeatable` — that was the one failing test from last session, now fixed); `flowToRfEdges(graph,selected,byType?)` renders slot edges DASHED (`ctb-slot-edge`, AI accent colour, labelled with the kind); `FlowCanvas` delegates `isValidConnection` to the shared `canConnect` so the drag preview matches the commit logic exactly; `CtbNode` renders bottom-edge slot handles (`*` = required) plus a provider output handle; `styles.css` adds the handle/label/dashed-edge styling; and `fake-fetch.ts` builds the same `NODE_META` the server does so the editor's activation mirror can't drift (I5). **No behavior change to existing flows** — every new field is optional and no shipped node declares one yet (the actual `ai.agent` consumer + Chat-Model/Memory/Tool providers arrive in later Phase-B tasks). Registry stays **42** types; NODES.md gained a "Sub-connections — typed provider slots (PB-T1)" section documenting the contract, rules table, back-compat strategy, executor behaviour and canvas rendering. **Tests (+19):** shared `flow-validate.test.ts` +9 (well-wired agent; required-slot-empty; wrong-kind provider; data-node-into-slot; provider-into-data-port; non-repeatable arity; repeatable tool slot; provider-not-a-trigger; disabled-provider-unfilled — plus a "without nodeMeta the slot rules are inert" back-compat test); editor `canvas-graph.test.ts` +8 (matching provider plugs in; wrong-kind rejected; data-node rejected; provider-into-data rejected; non-repeatable rejects a 2nd provider [and re-wiring the same edge is `duplicate`]; repeatable accepts many; `flowToRfEdges` marks a dashed slot edge; without `byType` edges stay plain); server `node-types.test.ts` +2 (omits the keys for plain data nodes; surfaces role+slots+provides when a node opts in); core `executor.test.ts` +1 (a provider parked on the cursor is skipped, never executed). A test-only gotcha worth remembering: `NodeTypeSchema` is EXACTLY two dot-parts, so the synthetic providers in tests are `ai.modelOpenai`/`ai.memoryKv`/`tool.think`, NOT `ai.model.openai`. STATE.md + NODES.md updated in this same commit. `npm run verify` GREEN (full suite; server 36f·283, editor 14f·143, typecheck + lint clean). **Next: Phase B PB-T2.** |
| 2026-06-17 | PA-T8 | **🎬 Phase-A end-to-end demo — and with it, Phase A is COMPLETE.** A single flow assembled from ONLY Phase-A nodes does everything an n8n "core nodes" workflow can, for the conversational domain — no AI, no external service. The flow (`packages/shared/test/fixtures/phase-a-demo-flow.json`, 13 nodes / 12 edges) answers `/report`: `tg.trigger` → `data.editFields` seeds a `tasks[]` array (JSON value-mode parses the string into a real array — in a live bot this would come from a Collection / HTTP / the user's message) → `data.splitOut` (5 items, one per task, one a duplicate) → `data.removeDuplicates`(title+due → 4) → `data.sort`(due asc) → `data.limit`(first 3) → `data.aggregate` collapses back to ONE item with `titles[]`/`dues[]` → `data.dateTime`(`source:now`, `calendar:jalali`, Persian digits → `report_date.formatted`) → `data.editFields` composes `count` + the reply `text` from the aggregated arrays + the Jalali stamp → `data.filter`(`count>0` → kept | discarded) → kept: `tg.sendMessage`(summary) → `tg.sendMedia`(2-photo album) / discarded: `tg.sendMessage`("nothing to report"). Scripted e2e `apps/server/test/e2e-phaseA-demo.test.ts` drives it through the **REAL** `UpdateRouter` + `Executor` + `MemoryExecutionStore` with a fake Telegram transport (recording sendMessage + sendMedia), exactly like the P1 e2e (`e2e-nodes-flow.test.ts`) — so it exercises the genuine engine, not a node harness. The clock is pinned to `2026-06-11T08:00:00.000Z` (= 11:30 Asia/Tehran → Jalali `۱۴۰۵/۰۳/۲۱`). **Test 1** asserts the run completes (no error), exactly ONE text send (the summary, NOT the empty notice), the header `📋 گزارش کارها — ۱۴۰۵/۰۳/۲۱`, the count `(3 مورد، به‌ترتیب سررسید)`, the deduped→sorted→limited task list (`کدنویسی`(09) < `طراحی`(11) < `استقرار`(12), with `تست`(14) dropped by the limit), and the album (`[{kind:'photo', ref:'…chart-1.png'}, {kind:'photo', ref:'…chart-2.png'}]`, caption `نمودارهای گزارش 📷`). **Test 2** asserts durability (I4): the flow has no `tg.waitForReply`, so a single dispatch runs it end-to-end and `findWaiting()` returns []. **Discovered + documented the key executor gotcha:** `buildNodeScope` binds `$json` to `items[0]?.json` ONLY, and the executor pre-resolves `{{ }}` in non-raw params against item[0] BEFORE a node runs — so a per-item field expression in a node param collapses to item[0]'s value over a multi-item batch. That's why the demo AGGREGATES first then formats (by `compose`/`stamp` time there is a single item so `$json.titles[i]` resolves correctly), and uses `data.dateTime` `source:now` (reads `ctx.now()` directly, unaffected) for the headline Jalali stamp rather than a per-item `value`. Likewise `data.filter` is batch-level (conditions resolved once against item[0]; the same verdict routes the whole batch). Also captured: `tg.sendMedia` resolves a url/file_id source into a `TgInputMedia` carrying `ref` (not `value`) — the e2e records + asserts `ref`. Demo doc **`docs/demos/phase-A-data.md`** (🎬, mirrors the phase-3.5-shop / phase-4-n8n format: header, "exercised automatically by…" run command, step-by-step run, "What this demo proves", and a "Gotchas worth remembering" section). PA-T8 adds NO new node (registry stays **42**; NODES.md node tables unchanged — the Phase-A "NODES.md updated" acceptance was satisfied by PA-T1…T7). STATE.md updated this same commit. `npm run verify` GREEN (full suite; server 36f·281, editor 14f·135). **Phase A complete — next is Phase B, PB-T1 (typed sub-connection contract: provider sub-nodes / typed input slots).** |
| 2026-06-17 | PA-T7 | **`data.dateTime` — Date & Time with IANA timezones AND the Jalali (Persian) calendar; the data-transform library's first calendar-aware node.** Mirrors n8n's "Date & Time" node but adds a first-class need for our Iranian users: formatting `۱۴۰۴/۰۳/۲۴` (Persian digits, Jalali year). PURE — the only clock is the injected `ctx.now()`, and the Gregorian↔Jalali conversion is a small hand-written **dependency-free** algorithm in `packages/nodes/src/lib/jalali.ts` (Borkowski method, the one popularised by `jalaali-js`) rather than a new runtime dep (CLAUDE.md: "no new runtime dependency without justification"); I verified that hand-rolled algorithm against the platform's own `Intl` Persian calendar over **1,836 dates (2000–2050) with ZERO mismatches** before trusting it. Runs **per item**, merging the result under `save_as` (default `datetime`) onto a CLONE of each input item (never mutated; seeds one empty item if input is empty). Three operations: **`format`** → `{ formatted, iso, epoch }` (the timezone-adjusted wall-clock rendered in the chosen calendar via a token pattern `YYYY/MM/DD/HH/mm/ss` + `MMMM` month-name, with `digits:'persian'` mapping `0-9`→`۰-۹`); **`add`** → `{ iso, epoch }` (shift by `amount`·`unit`, negative subtracts; `years`/`months` use UTC calendar math, smaller units are exact millis); **`diff`** → `{ diff, unit }` (`source − to_value` in `diff_unit`, may be fractional). The wall-clock is extracted via `Intl.DateTimeFormat(..).formatToParts` in the chosen IANA `timezone` (empty = UTC) — so e.g. `2026-06-11T22:00Z` formatted in `Asia/Tehran` correctly rolls to the NEXT Jalali day `1405/03/22`. Lenient `parseDate` accepts epoch-millis, ISO-8601, and `YYYY-MM-DD` / `YYYY/MM/DD`; `source:'value'` runs the raw string through `ctx.eval` first so `{{ }}` expressions resolve. Shared (I5): `DataDateTimeParamsSchema` with a `superRefine` that requires `value` when `source=value`, `amount` when `operation=add`, and `to_value` when `operation=diff`, plus an identifier-only `save_as`; `amount` is `z.coerce.number()` so a widget string coerces. Node `packages/nodes/src/data/date-time.ts` (registered, palette order after `removeDuplicates`, before `dataKv`). Editor: auto-surfaces in the registry-driven palette; fa/en i18n added to BOTH catalogs (parity compile-enforced) — label, `nodeDesc.data.dateTime`, all `paramDesc.data.dateTime.*`, and translated enum options for operation/source/calendar/digits/unit. NODES.md §Date & Time documents the shape. Registry **41→42** types; all 10 registry-count guard tests bumped 41→42. **Tests (+27):** `data-date-time.test.ts` — registry + ports; lib/jalali (known anchors vs Intl oracle incl. PLAN2's `۱۴۰۴/۰۳/۲۴`, Nowruz `1403/01/01`, round-trip G→J→G, leap-year, 12 month names); `parseDate` (epoch / ISO / slash / junk); format (Gregorian default / Jalali / Persian month-name + digits / time tokens in tz / tz rolls Jalali day / custom save_as / empty-input runs once); add (days / negative hours / calendar months / coerced string amount); diff (days from now / fractional hours between two values); expressions ({{ }} via ctx.eval) + errors (unparseable date / invalid tz / schema rejects missing value·amount·to_value / non-identifier save_as). STATE.md updated this same commit. `npm run verify` GREEN (shared 8f / sandbox 6f / nodes 22f·369 / core 1f / server 35f·279 / editor 14f·135). Next: PA-T8 (🎬 Phase-A demo flow + scripted e2e). |
| 2026-06-17 | PA-T6 | **`data.sort` + `data.limit` + `data.removeDuplicates` — small, pure, high-value item-list ops; the data-transform library continues.** Three n8n core "Sort / Limit / Remove Duplicates" siblings, all pure functions over the item list. **`data.sort`** does a STABLE multi-key sort — the first `fields` row is the primary key, later rows break ties; values that are numbers or numeric strings compare numerically (so `'9' < '21' < '100'`, not lexically), everything else compares as a locale-aware string; a missing / null / empty value always sorts LAST regardless of asc/desc, and crucially that "missing last" rule is resolved BEFORE the `desc` negation (a naïve `-cmp` would have flipped empties to the top under descending — a subtle bug avoided by handling missing explicitly in `compareKey`); the input array is never mutated (we sort a `.slice()`). **`data.limit`** keeps the first N (default) or last N items — `max_items` is `z.coerce.number().int().min(0).default(10)` so a string from a widget coerces, `0` means no limit, and `≥ length` passes everything through; same item references survive in their original relative order. **`data.removeDuplicates`** drops repeats keeping the FIRST occurrence (n8n's behaviour) with survivor order preserved; `compare:'all_fields'` (default) compares the WHOLE `$json` via a recursive key-SORTED stable serialization (so `{a:1,b:2}` and `{b:2,a:1}` dedupe), `compare:'selected_fields'` keys off the combined value at the chosen dotted `fields`, and a missing field is encoded as a distinct `\u0000undefined` sentinel so it never collides with a literal `null`; it `fail()`s loudly when `selected_fields` is chosen but no `fields` are supplied. Shared (I5): `SortFieldRowSchema` (field min-1 + order asc|desc default asc) + `DataSortParamsSchema` (fields min-1); `DataLimitParamsSchema` (max_items coerce/int/min0/default10 + keep first|last); `DataRemoveDuplicatesParamsSchema` (compare all_fields|selected_fields + fields string[] optional). Nodes `packages/nodes/src/data/{sort,limit,remove-duplicates}.ts` (registered, palette order after aggregate). Editor: all three auto-surface in the registry-driven palette; fa/en i18n keys added to BOTH catalogs (parity is compile-enforced, so a missing en key is a TS error) — labels, `nodeDesc.*`, `paramDesc.*`, and translated enum options for order/keep/compare. NODES.md gained §Sort + §Limit + §Remove Duplicates. Registry **38→41** types; all 9 registry-count guard tests bumped. **Tests (+27):** `data-sort-limit-dedupe.test.ts` — sort (numeric-asc / numeric-string / desc / multi-key tie-break / locale-string / missing-last in BOTH directions / stable / no-mutate / empty / reject-empty-fields), limit (first / last / 0=no-limit / ≥length / coerce-string / empty / reject-negative), removeDuplicates (all-fields dedupe / key-order-irrelevant / selected-fields / multi-field key / missing≠null / order-preserved / empty / fail-when-no-fields). STATE.md updated this same commit. `npm run verify` GREEN (shared 63 / sandbox 49 / nodes 21·342 / core 16 / server 35·279 / editor 14·135). Next: PA-T7 (`data.dateTime` w/ Jalali). |
| 2026-06-17 | PA-T4 + PA-T5 | **`data.filter` (PA-T4) + `data.splitOut` + `data.aggregate` (PA-T5) — the n8n data-transform node library grows.** PA-T4: `data.filter` partitions items into `kept`/`discarded` ports by re-using the `flow.if` condition engine (compareValues) exactly — same operators (equals/notEquals/contains/regex/gt/gte/lt/lte/exists/is_empty), same AND/OR combine, same loose-numeric equality — so a user who knows IF already knows Filter. Both ports always emitted. `DataFilterParamsSchema` (conditions min-1 + combine). Registry 35→36. Tests +15. PA-T5: `data.splitOut` splits one item with an array field into one item per element (`main` port) or sends the original to `empty` when the array is empty/missing — n8n Split Out semantics: non-array value treated as single-element array, `all_fields` mode replaces the array field with the element, `selected_field_only` mode extracts only the element (objects pass through, primitives wrapped in `{value}`), original items never mutated. `data.aggregate` (the inverse pair) merges many items into one: `aggregate_all_items` wraps every item's `$json` into an array under `dest_field`; `aggregate_individual_fields` collects named fields across items, first item's other fields carried through. Both handle empty input gracefully. `DataSplitOutParamsSchema` + `DataAggregateParamsSchema` + `AggregateFieldRowSchema`. Registry 36→38. Tests +19 (data-split-aggregate.test.ts). All registry-count guards updated. `npm run typecheck` green (6 pkgs). nodes 315 tests green. |
| 2026-06-17 | PA-T3 | **`data.editFields` — the n8n "Edit Fields / Set" power node; the data-transform library begins.** PLAN2 Phase A pairs Telegram media completeness (PA-T1/PA-T2, now merged) with a generic data-transform node library mirroring n8n's core nodes; PA-T3 is its first member. We already had a basic `data.setFields`, but n8n's "Edit Fields (Set)" is a power node — so rather than overload/relabel the existing node (which other flows may already reference), PA-T3 adds a NEW node `data.editFields`, labelled **"Edit Fields (Set)"** in the palette so n8n users find it, leaving `data.setFields` UNTOUCHED for backward-compat. It keeps the same safe, immutable per-item model as `data.setFields` — input items are never mutated (each output is a fresh object via `structuredClone`), `target:'vars'` rows write `$vars` exactly ONCE per run (not per item), and an empty input still emits one shaped item — then adds three powers per row: (1) **op `rename`** moves a value from one dotted path (the row's `name`) to another (the row's `value`, interpreted as a destination path) and deletes the source — and it works in `keep_only_set` mode too, reading the source from the ORIGINAL item even though the output started empty; (2) **`value_mode:'json'`** treats a STRING `value` as JSON and `JSON.parse`s it into a real value / array / object (a non-string value passes straight through, so a pre-built object isn't double-encoded); (3) **`enabled`** (default true) lets a row stay in the saved config but be skipped on this run. Dotted `name`s build nested objects on set; `keep_only_set` makes the output `$json` start empty (only set/renamed fields survive). It fails LOUDLY — but does so by validating EVERYTHING UP FRONT in a `planRow` pass (inside a try/catch that returns `fail()`) BEFORE any item is touched: a `value_mode:'json'` value that isn't valid JSON, or a `rename` with a blank destination path, aborts the whole node cleanly instead of throwing mid-item-map (the bug found+fixed this session — `planRow` returns a pre-validated `PlannedRow {row, value, dest?}` that the per-item loop just applies). **Shared (I5):** `EditFieldRowSchema` (`name` min-1, `value` optional unknown, `target` json\|vars default json, `op` set\|remove\|rename default set, `value_mode` value\|json default value, `enabled` default true) + `DataEditFieldsParamsSchema` (`fields` min-1 + `keep_only_set` default false) + `EditFieldRow`/`DataEditFieldsParams` types; the existing `SetFieldRowSchema`/`DataSetFieldsParamsSchema` left exactly as-is. **Node:** NEW `packages/nodes/src/data/edit-fields.ts` (`dataEditFields: NodeDef`, type `data.editFields`, category `data`, pure helpers `planRow`/`getPath`/`setPath`/`removePath`); registered in `packages/nodes/src/index.ts` (import + export + `builtinNodes`). **Editor:** the node auto-surfaces in the registry-driven palette; fa (source-of-truth) + en (parity, `Record<keyof fa,string>`) gained `nodes.data.editFields.label` ("Edit Fields (Set)"), `nodeDesc.data.editFields`, `option.op.rename`, namespaced `paramDesc.data.editFields.{fields,value_mode,enabled,keep_only_set}`, and `option.editFields.value_mode.{value,json}`. NODES.md gained a §Edit Fields (Set) subsection. Registry **35** types. **Tests (+14):** NEW `packages/nodes/test/data-edit-fields.test.ts` (registry-35 assertion; set dotted+immutable / json-mode array+object / json-mode non-string passthrough; remove dotted; rename json + rename in keep_only_set + rename vars; keep_only_set drops the rest; disabled-row skipped; vars written once-per-run; empty-input seeds one item; failures empty-fields / blank-name → `/invalid params/`, bad json value → `/not valid JSON/i`, empty rename dest → `/destination path is empty/i`). The 6 registry-count assertions bumped 34→35. **verify GREEN:** typecheck clean (6 pkgs) + shared 63 / sandbox 49 / nodes 18(281) / core 16 / server 35(279) / editor 14(135); editor vite build OK. Next: PA-T4 (`data.filter` — kept/discarded ports over the flow.if condition engine), then PA-T5 (`data.splitOut` + `data.aggregate`). |
| 2026-06-17 | PA-T2 | **`tg.getFile` — Telegram media RECEIVE (download + store); the symmetric sibling of PA-T1.** PA-T1 added media SEND (`tg.sendMedia`); PA-T2 completes the pair with media RECEIVE: given a Telegram `file_id` (captured by the trigger from a photo/voice/document/video the user sent), the node DOWNLOADS the bytes and OPTIONALLY STORES them so downstream nodes (`tg.sendMedia` `source:file`, the future `ai.speechToText`) can reuse them — this is the "Get a file" left-half of the AI-voice screenshot needed for Phase B. The node touches neither the bot token, the network, nor disk (invariants I3/I6): a NEW nullable `ctx.tg.getFile(file_id)` host capability returns `{bytes, filePath, size, mime}` (the host calls the Bot-API `getFile` through the EXISTING rate-limited `TgSender.call` to learn the `file_path`/size, then `fetch`-downloads the raw bytes from `https://api.telegram.org/file/bot<token>/<file_path>` — the token lives host-side in a NEW `BotHandle.token` field, never reaching node code), and when `store` is on the node hands the bytes to a NEW `ctx.files.write(bytes, mime)` capability (host `putLocal` writes them under the file-store, stamping the run's bot). `file_id` defaults to a `$json` lookup — it tries `$json.file_id`, then the common nested shapes a trigger/normalizer emits (`reply`/`photo`/`voice`/`document`/`audio`/`video` `.file_id`) — so the node drops in right after a media trigger with NO config; an explicit param (literal or `{{ }}`) always wins. It runs ONCE per node run (one download), merging a result object `{file_id, path, url, mime, size, stored_file_id?}` under `save_as` (default `file`) onto every input item (seeding one empty item if input is empty); `stored_file_id` + the file-store `url` appear only when `store` is on, otherwise `url` is the temporary Telegram download URL. Fails LOUDLY only on misconfiguration / honest errors: no `ctx.tg` or no `ctx.tg.getFile`, `store:true` with no `ctx.files` wired, no resolvable `file_id`, or a download/transport error. **Shared (I5):** `TgGetFileParamsSchema` (`file_id` default `''`, `store` default `true`, `save_as` identifier-regex default `'file'`, `.strict()`) + `TgGetFileParams`; a `TgStoredFile` interface (the public projection `ctx.files.write` returns — the subset of `FilePublic` the node needs); an OPTIONAL `ctx.tg.getFile?(fileId)→{bytes,filePath,size,mime}`; and `ctx.files` extended from `{read}` to `{read, write}`. **Core:** `ExecutorServices.files` changed from a plain object to a **per-bot factory** `(botId)=>{read,write}` (the DL #15 pattern shared with `tg`/`users`/`collections`) — `write` needs the run's bot to stamp ownership, while `read` ignores it (file ids are globally unique); `buildCtx` now calls `services.files(exec.botId)`. **Server:** `gateway.ts` adds a `token: string` to `BotHandle` (set in `registerBot`); `wire.ts` `makeFiles(fileStore, botId)` gains `write(bytes,mime)`→`fileStore.putLocal(botId, Buffer.from(bytes), mime)`→mapped to `TgStoredFile`, `services.files` becomes `(botId)=>makeFiles(fileStore, botId)`, and the `ctx.tg` factory gains `getFile` (rate-limited `sender.call('getFile',{file_id})` → build the file URL with `handle.token` → `(opts.fetchImpl ?? fetch)(url)` → bytes, with loud errors on a missing `file_path` or a non-ok response). **Editor:** the node auto-surfaces in the registry-driven palette; fa (source-of-truth) + en (parity, `Record<keyof fa,string>`) gained `nodes.tg.getFile.label`, `nodeDesc.tg.getFile`, and namespaced `paramDesc.tg.getFile.{file_id,store,save_as}`. NODES.md gained a §Get a File subsection. Registry **34** types. **Tests (+12):** NEW `packages/nodes/test/tg-get-file.test.ts` (resolve from explicit param / `$json.file_id` / nested `voice.file_id` / param-wins-over-item / custom `save_as`; `store:true`→`stored_file_id`+file-store url, `store:false`→telegram url with no disk write, runs-once-per-run merging on EVERY item; failures no-tg-context / host-didn't-inject-getFile / no-resolvable-file_id / `store` with no file store / download-transport-error). The node-harness `makeCtx` gained a `getFile` recorder on `ctx.tg` (with `getFileResult` / `getFileError` / `noGetFile` overrides) + a `files.write` that records into a `storedFiles` array and round-trips into `filesBag` (so a stored file is `read`-able). The 6 registry-count assertions bumped 33→34. **verify GREEN:** typecheck clean (6 pkgs) + shared 63 / sandbox 49 / nodes 17(267) / core 16 / server 35(279) / editor 14(135); editor vite build OK. Next: the rest of PLAN2 Phase A (the data-transform node library). |
